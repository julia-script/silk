---
'@silk-lang/lsp': minor
---

Add the Silk Effect language server: a stdio LSP binary serving whole-phase diagnostics, expression
type hover, document symbols, and canonical formatting from one compiler analysis snapshot per
change, with `silk.toml` project discovery and open-document overlay resolution for imports.
