{
  "$schema": "https://turborepo.com/schema.json",
  "globalPassThroughEnv": ["NPM_REGISTRY", "PUBLISHED_PACKAGES"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/tsconfig.base.json",
        "!benchmarks/**",
        "!docs/**",
        "!fixtures/**",
        "!parity/**",
        "!scripts/**",
        "!test/**",
        "!*.md"
      ],
      "outputs": ["dist/**"]
    },
    "dev": {
      "dependsOn": ["^build", "build"],
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@silklang/compiler#typecheck": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["build"],
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/vitest.shared.ts"],
      "outputs": []
    },
    "@silklang/compiler#test": {
      "dependsOn": ["build", "@silklang/docgen#build", "@silklang/cli#build"],
      "inputs": [
        "$TURBO_DEFAULT$",
        "$TURBO_ROOT$/vitest.shared.ts",
        "$TURBO_ROOT$/apps/docs/content/language/diagnostics.md",
        "$TURBO_ROOT$/apps/docs/content/language/stdlib/**"
      ],
      "outputs": []
    }
  }
}
