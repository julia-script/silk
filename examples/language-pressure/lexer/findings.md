# Lexer pressure findings

The categories are intentionally stable so later pressure programs can be compared without turning
their local annoyances into language doctrine. Evidence and dispositions are completed alongside
the acceptance gates.

| Category             | Finding                                                                                                                                                                                                                                                              | Evidence                                                                                                                                                                                                                      | Disposition                                                                                                                                                                                                                                                                                              |
| -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Language             | Token identity wants a closed named-value vocabulary. The lexer and stack VM independently needed reusable scalar names, while neither exercise yet justifies enum cases or exhaustiveness.                                                                          | `main.silk` (`Token.kind`, `keywordKind`, and punctuation classifiers) plus the exhaustive mapping in `LexerPressure.test.ts`                                                                                                 | Completed as literal-only, explicitly typed scalar constants. Token codes and byte classifiers now use ordinary `const` declarations; enum design remains separate.                                                                                                                                      |
| Standard library     | `Vector.get` originally required `&mut Vector<T>` even for Copy reads, so observation consumed `Lexed`, destructured it, and rebound each vector as mutable.                                                                                                         | `fingerprint`, `fingerprintVectors`, the `silk.vector` public `get` signature, and structural `RawBuffer.read` evidence                                                                                                       | Repaired after the stack VM independently confirmed the wall. `fingerprint` now shared-matches `Lexed`, and both nominal Copy vectors are read through `&Vector<T>` without moving storage, allocating, or changing cleanup. The VM subsequently proved the same surface for all-Copy structural unions. |
| Compiler defect      | LLVM's private cleanup/conditional inliner removed blocks using structural incoming counts even when an outcome-lowered branch still referenced them, producing `Backend branch to missing block`.                                                                   | The valid and invalid native gates in `LexerPressure.test.ts`; fixed in LLVM lowering by retaining rewritten references and the entry                                                                                         | Repaired here as a general CFG correctness fix with structural and independently pinned native evidence.                                                                                                                                                                                                 |
| Compiler defect      | Concrete call and pipeline parameters already select exact integer literal types, but a `bool` result expectation suppressed homogeneous operand refinement in expressions such as `return byte == 13`, leaving `13` as `i32` instead of `u8`.                       | Focused direct-call, explicit-generic, pipeline, return-comparison, overflow, and mismatch cases in `IntegerScalars.test.ts`; the lexer now compares bytes and literals as `u8` without a classifier-wide conversion boundary | Repaired by allowing a resolved first scalar operand to refine the remaining operands even when the enclosing expression supplied a different result expectation. Structured compiler checks and independently pinned native lexer artifacts cover the result.                                           |
| Tooling / ergonomics | Raw numeric token codes made the Silk source harder to navigate than the TypeScript `TokenKind` union even though stdlib go-to-definition worked.                                                                                                                    | Side-by-side `main.silk`, `packages/compiler/src/Token.ts`, and the centralized TypeScript mapping                                                                                                                            | Completed for the scalar-name boundary: constant definitions and references participate in hover, completion, occurrences, and go-to-definition without compiler-known lexer aliases.                                                                                                                    |
| Performance / cost   | Keyword classification performs a visible linear sequence of at most 25 byte-slice comparisons, while vector growth moves initialized records element by element. The cost is pay-for-use and no scheduler/runtime abstraction appears, but it is not yet optimized. | `keywordKind`, `silk.vector`, general-operation MIR assertion, four allocation ordinals, and fresh-process artifacts                                                                                                          | Accept for bootstrap correctness. Benchmark before proposing tables, bulk moves, or another primitive; none is justified by this corpus alone.                                                                                                                                                           |

The contextual-literal follow-up corrected the initial diagnosis: calls were already sound;
enclosing operator-result context was the live defect. Typed scalar constants are now complete
after the stack VM supplied independent evidence, without selecting enum syntax or scheduling a
parser port. Shared Vector reads are now complete for recursively Copy values: the lexer proves
nominal records independently, and the VM proves ordered structural-union events.
