# Local shared SLP-1 pressure findings

These categories match the lexer and stack-VM pressure reports. Claims below are limited to the
checked-in witness and its assertions; a pressure actor is evidence, not automatically a public API.

| Category             | Finding                                                                                                                                                                                                                                                                                                            | Evidence                                                                                                                                                                                        | Disposition                                                                                                                              | Smallest follow-up                                                                                 |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- |
| Language             | Allocation-backed `Shared<T>` is sufficient for this fixed-capacity shared-state slice. Ordinary structs, affine moves, exact named once-callables, Effects, generic wrappers, and `Intrinsic.replace` express registration, one publication, callback extraction, and state-held borrowed observation.            | `main.silk` functions `connected`, `publish`, and `observePayload`; independently pinned native result `42`; zero diagnostics and verified MIR in `LocalSharedPressure.test.ts`.                | Complete for this slice. No new syntax, compiler-known actor, hidden allocation, or repeated-publication policy is justified.            | Keep the witness non-canonical and apply the same evidence bar to any later Deferred proposal.     |
| Standard library     | `silk.shared` is deep enough for this witness: `make`, `clone`, `withMut`, and `with` support short registration/publication/observation accesses, while extracted callbacks run afterward.                                                                                                                        | Two `Shared.make` sites; three registered real callables; three `observePayload` calls through waiter state handles; intrinsic inventory restricted to sealed `Intrinsic.SharedCore` consumers. | Accept the existing wrapper surface; do not promote the inbox, state, producer, waiter, or callback registry.                            | Revisit only if an unrelated witness repeats a missing safe wrapper operation.                     |
| Compiler defect      | MIR verification confused an existing zero-slot callable environment with no environment, and did not require `CallableCleanup.type` to equal its local type.                                                                                                                                                      | `Shared.withMut` callback lowering in the witness; malformed-cleanup case in `StoredEffectCleanupVerification.test.ts`.                                                                         | Repaired generally: environment presence selects `CallableCleanup`, and type equality is verified.                                       | Retain both focused regressions.                                                                   |
| Compiler defect      | Callable cleanup initially lacked owner roots and callable-environment materialization for hook-bearing `Shared.withMut` callbacks.                                                                                                                                                                                | Canonical publication callbacks and quota paths are covered structurally in MIR and through independently pinned native cases.                                                                  | Repaired in general frame planning/lowering with no spelling recognition.                                                                | Retain the pressure witness as the multi-lane callback-cleanup regression.                         |
| Compiler defect      | Hook cleanup of an `EffectBorrow<T>` incorrectly demanded an owner frame root instead of using the incoming environment pointer.                                                                                                                                                                                   | The dormant publication Effect owns an unpublished `Payload` with a Drop hook; MIR cleanup and the native boundary case exercise it.                                                            | Repaired by invoking hooks at the borrow pointer and reloading before reclaim.                                                           | Keep the dormant publication Effect in the witness.                                                |
| Compiler defect      | Callable cleanup stored environment lanes before hooks but reclaimed from stale locals afterward, allowing a hook that mutates a captured lane to double-release the old owner.                                                                                                                                    | Structural cleanup tests cover the reload after hooks; native acceptance pins the resulting balanced lifecycle.                                                                                 | Repaired by reloading every callable-environment lane after hooks.                                                                       | Keep the mutation regression; extend only when another nested executable shape differs materially. |
| Tooling / ergonomics | Exact callable identities resolve in declaration signatures, while spelling `typeof(named)` again inside explicit generic call arguments produced `SEM0108`; ordinary call inference avoids the repetition. Open representation parameters also do not specialize through this higher-order shared callback shape. | Implementation investigation; final source uses concrete signature types and inferred `Shared.with/withMut` arguments.                                                                          | The witness is expressible without privilege, but the diagnostics are an ergonomics gap rather than evidence for a new Shared primitive. | Add a small independent exact-representation/inference issue if this recurs outside pressure code. |
| Performance / cost   | The success path has fixed-capacity storage and performs no access-time allocation.                                                                                                                                                                                                                                | Structural MIR inventory and the independently pinned native fingerprint are correctness observations, not timing or throughput claims.                                                         | Accept as correctness evidence only.                                                                                                     | Benchmark before proposing batching, alternate storage, or another primitive.                      |

## Acceptance matrix

| OpenSpec scenario                                                      | Concrete assertion                                                                                                                                                                                                             |
| ---------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Enqueue from dormant callbacks / publish after extracting waiters      | `main.silk` stores actual `notifyReady` once-callables, extracts them in `publishState`, invokes them after `withMut`, records order `123`, and has three waiter `Shared.with` observations of the stored payload.             |
| Retain and drop dormant Effects / unpublished state / dormant callback | Two unrun state Effects coexist before either is dropped; structural MIR cleanup proves the retained owners are released at their lexical exits.                                                                               |
| Preserve runtime semantics                                             | Independently pinned native success and renamed cases require `42`. That result is reachable only after order `123` and cleanup fingerprint `15263`, so the program compares public outcomes rather than a hidden event trace. |
| Roll back construction failures                                        | Quotas 0 and 1 return distinct recovery result `142`; structural allocation cleanup plus native boundary cases prove recovery without retaining a scripted host trace.                                                         |
| Rename every witness actor                                             | Canonical and `renamed-main.silk` compare normalized instance signatures, per-function MIR local/result types and operation sequences, and verified MIR. Intrinsic inventory contains no witness spelling.                     |
| Record/classify the remaining boundary                                 | This report classifies language, stdlib, compiler, ergonomics, and cost findings and records disposition/follow-up for each.                                                                                                   |

The lexer and stack VM found pressure around named numeric vocabularies and shared reads of growable
Copy collections. This witness repeats neither wall: identifiers remain ordinary values, the inbox
is deliberately fixed-capacity, and the retained affine payload is observed by borrow. It instead
exposed general executable-cleanup defects, repaired without recognizing `Shared`, Deferred, inbox,
producer, waiter, or callback spellings. As in those reports, visible linear work and explicit
allocation remain preferable to unmeasured compiler/runtime privilege.

The removed wall is local shared ownership only. Callbacks run synchronously in registration order
after the state borrow ends. This does not prove SLP-0001 execution transfer, parking, wake
selection/order, cancellation, fairness, or scheduler integration. Evidence therefore points next
to the separate SLP-0001 execution-transfer/parking work; this witness remains its shared-state
building block rather than becoming a runtime API.
