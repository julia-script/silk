# In-place quicksort

Lomuto partitioning sorts eight signed integers in place and recursively processes the two
partitions. The expected result is `[-8, -3, 0, 1, 2, 5, 7, 9]`, whose rolling fingerprint is `50`.
