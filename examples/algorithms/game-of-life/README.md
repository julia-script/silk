# Conway's Game of Life

A horizontal three-cell oscillator advances by one toroidal generation on a fixed 5×5 board. The
program returns the final board's row-major bit fingerprint modulo 251 (`7`); the expected board is
the centered vertical oscillator.
