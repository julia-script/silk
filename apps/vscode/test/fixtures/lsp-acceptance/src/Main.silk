import silk.vector as Vector
import Util

fn outer() -> Effect<i32> {
  let n = 123
  return effect {
    return n
  }
}

pub fn main() -> i32 {
  let mut values = Vector.make<i32>()
  let answer = Util.answer()

  return run outer()
}
