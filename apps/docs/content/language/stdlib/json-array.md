# `silk/json_array`

<!-- Generated by packages/compiler/scripts/generate-documentation.mjs. Do not edit by hand. -->

Profiles: `aarch64-apple-darwin`, `aarch64-unknown-linux-gnu`, `aarch64-unknown-linux-gnu-no-libc`, `wasm32-unknown-unknown`, `x86_64-unknown-linux-gnu`, `x86_64-unknown-linux-gnu-no-libc`.

Effectful JSON array construction through the ambient Writer.

## When to use

Use [`JsonArray`](#declaration-73696c6b2f6a736f6e5f61727261793a3a4a736f6e4172726179) to write a slice of values with a [`Serialize`](./json-codec.md#declaration-73696c6b2f6a736f6e5f636f6465633a3a53657269616c697a65) implementation.

## Details

The array owns its comma and depth state. It does not own or borrow the Writer.
A failed write can leave an accepted prefix in the Writer.

Import as `JsonArray` with `import silk.json_array { JsonArray }`.

Public declarations: 1.

<a id="declaration-73696c6b2f6a736f6e5f61727261793a3a4a736f6e4172726179"></a>

## `JsonArray`

```silk
pub struct JsonArray
```

Owned formatting state for one open JSON array.

<a id="declaration-73696c6b2f6a736f6e5f61727261793a3a4a736f6e41727261792e626567696e"></a>

### Associated function `JsonArray.begin`

```silk
pub effect<'life0> fn begin<'life0>(options: &'life0 silk/json_output.JsonOptions) -> JsonArray ! JsonError | WriterError ? &mut Writer
```

Opens an array after checking its depth limit.

<a id="declaration-73696c6b2f6a736f6e5f61727261793a3a4a736f6e41727261792e6974656d73"></a>

### Associated function `JsonArray.items`

```silk
pub effect<'env1> fn items<'env: 'env1, T: 'env1, E, ?R, 'env1>(self: once Effect<'env; silk/json_array.JsonArray ! E ? R>, values: &'env [T]) -> JsonArray ! E | JsonError | WriterError ? R | &mut Writer
```

Writes all values in source order and returns the open array state.

#### Details

The incoming Effect runs once. If it fails, this operation writes nothing.

<a id="declaration-73696c6b2f6a736f6e5f61727261793a3a4a736f6e41727261792e656e64"></a>

### Associated function `JsonArray.end`

```silk
pub effect<'env> fn end<'env, E, ?R>(self: once Effect<'env; silk/json_array.JsonArray ! E ? R>) -> () ! E | JsonError | WriterError ? R | &mut Writer
```

Closes an array and returns the completed write.

#### Details

The incoming Effect runs once. If it fails, the closing bracket is not written.
