# `silk/i8`

<!-- Generated by packages/compiler/scripts/generate-documentation.mjs. Do not edit by hand. -->

Eight-bit signed integers for compact values, byte-level formats, and narrow arithmetic.

## When to use

Use `i8` when an external format or data structure requires exactly eight signed bits. Prefer a
wider integer for general arithmetic: the small range makes accidental overflow easy.

## Details

Ordinary arithmetic, narrowing conversions, division by zero, and invalid shift counts trap.
Choose `checked*` when failure is data, `wrapping*` for arithmetic modulo 2^8, or `saturating*`
when results should clamp to [`MIN`](#declaration-73696c6b2f69383a3a4d494e) or [`MAX`](#declaration-73696c6b2f69383a3a4d4158). Signed right shifts preserve the sign bit.

Decimal [`parse`](#declaration-73696c6b2f69383a3a7061727365) consumes the complete input and distinguishes malformed text from a value
outside the i8 range. Use `silk.format.Display` for allocation-free presentation.

## Gotchas

[`MIN`](#declaration-73696c6b2f69383a3a4d494e) has no positive i8 counterpart, so ordinary [`negate`](#declaration-73696c6b2f69383a3a6e6567617465) traps for that one value.

## Examples

### Select the boundary policy for negation

```silk
import silk.i8

pub fn main() -> i32 {
  if i8.wrappingNegate(i8.MIN) != i8.MIN {
    return 1
  }
  if i8.saturatingNegate(i8.MIN) != i8.MAX {
    return 2
  }
  return 42
}
```

Import as `i8` with `import silk.i8`.

Public declarations: 59.

<a id="declaration-73696c6b2f69383a3a4d4158"></a>

## `MAX`

```silk
pub const MAX: i8
```

The largest `i8` value.

<a id="declaration-73696c6b2f69383a3a4d494e"></a>

## `MIN`

```silk
pub const MIN: i8
```

The smallest `i8` value.

<a id="declaration-73696c6b2f69383a3a42495453"></a>

## `BITS`

```silk
pub const BITS: u32
```

The fixed width of `i8`, in bits.

<a id="declaration-73696c6b2f69383a3a6e6567617465"></a>

## `negate`

```silk
pub fn negate(value: i8) -> i8
```

Returns the arithmetic negation of `value` and traps when `value` is [`MIN`](#declaration-73696c6b2f69383a3a4d494e). Use
this function when that boundary is a program error.

<a id="declaration-73696c6b2f69383a3a7772617070696e674e6567617465"></a>

## `wrappingNegate`

```silk
pub fn wrappingNegate(value: i8) -> i8
```

Returns the arithmetic negation of `value`, wrapped to the `i8` range. [`MIN`](#declaration-73696c6b2f69383a3a4d494e)
stays [`MIN`](#declaration-73696c6b2f69383a3a4d494e). Use this function for deliberate modulo arithmetic.

<a id="declaration-73696c6b2f69383a3a73617475726174696e674e6567617465"></a>

## `saturatingNegate`

```silk
pub fn saturatingNegate(value: i8) -> i8
```

Returns the arithmetic negation of `value`, clamped to the `i8` range. [`MIN`](#declaration-73696c6b2f69383a3a4d494e)
becomes [`MAX`](#declaration-73696c6b2f69383a3a4d4158). Use this function when the positive boundary is required.

<a id="declaration-73696c6b2f69383a3a746f5538"></a>

## `toU8`

```silk
pub fn toU8(value: i8) -> u8
```

Converts `value` to `u8`. Traps if `value` is outside the `u8` range. Use
this function when an out-of-range value is a program error.

<a id="declaration-73696c6b2f69383a3a636865636b6564546f5538"></a>

## `checkedToU8`

```silk
pub fn checkedToU8(value: i8) -> silk/option.Option<u8>
```

Converts `value` to `u8`, or returns `None` if `value` is outside the `u8`
range. Use this function when an out-of-range value is input data.

<a id="declaration-73696c6b2f69383a3a746f553136"></a>

## `toU16`

```silk
pub fn toU16(value: i8) -> u16
```

Converts `value` to `u16`. Traps if `value` is outside the `u16` range. Use
this function when an out-of-range value is a program error.

<a id="declaration-73696c6b2f69383a3a636865636b6564546f553136"></a>

## `checkedToU16`

```silk
pub fn checkedToU16(value: i8) -> silk/option.Option<u16>
```

Converts `value` to `u16`, or returns `None` if `value` is outside the `u16`
range. Use this function when an out-of-range value is input data.

<a id="declaration-73696c6b2f69383a3a746f553332"></a>

## `toU32`

```silk
pub fn toU32(value: i8) -> u32
```

Converts `value` to `u32`. Traps if `value` is outside the `u32` range. Use
this function when an out-of-range value is a program error.

<a id="declaration-73696c6b2f69383a3a636865636b6564546f553332"></a>

## `checkedToU32`

```silk
pub fn checkedToU32(value: i8) -> silk/option.Option<u32>
```

Converts `value` to `u32`, or returns `None` if `value` is outside the `u32`
range. Use this function when an out-of-range value is input data.

<a id="declaration-73696c6b2f69383a3a746f553634"></a>

## `toU64`

```silk
pub fn toU64(value: i8) -> u64
```

Converts `value` to `u64`. Traps if `value` is outside the `u64` range. Use
this function when an out-of-range value is a program error.

<a id="declaration-73696c6b2f69383a3a636865636b6564546f553634"></a>

## `checkedToU64`

```silk
pub fn checkedToU64(value: i8) -> silk/option.Option<u64>
```

Converts `value` to `u64`, or returns `None` if `value` is outside the `u64`
range. Use this function when an out-of-range value is input data.

<a id="declaration-73696c6b2f69383a3a746f5573697a65"></a>

## `toUsize`

```silk
pub fn toUsize(value: i8) -> usize
```

Converts `value` to `usize`. Traps if `value` is outside the `usize` range. Use
this function when an out-of-range value is a program error.

<a id="declaration-73696c6b2f69383a3a636865636b6564546f5573697a65"></a>

## `checkedToUsize`

```silk
pub fn checkedToUsize(value: i8) -> silk/option.Option<usize>
```

Converts `value` to `usize`, or returns `None` if `value` is outside the `usize`
range. Use this function when an out-of-range value is input data.

<a id="declaration-73696c6b2f69383a3a746f4938"></a>

## `toI8`

```silk
pub fn toI8(value: i8) -> i8
```

Returns `value` unchanged as `i8`. Use this function when generic conversion code
can select `i8` as both source and destination.

<a id="declaration-73696c6b2f69383a3a636865636b6564546f4938"></a>

## `checkedToI8`

```silk
pub fn checkedToI8(value: i8) -> silk/option.Option<i8>
```

Returns `Some` with `value` unchanged as `i8`. Use this function when generic
checked-conversion code can select the same source and destination type.

<a id="declaration-73696c6b2f69383a3a746f493136"></a>

## `toI16`

```silk
pub fn toI16(value: i8) -> i16
```

Converts `value` exactly to `i16`. Every `i8` value is representable.

<a id="declaration-73696c6b2f69383a3a636865636b6564546f493136"></a>

## `checkedToI16`

```silk
pub fn checkedToI16(value: i8) -> silk/option.Option<i16>
```

Converts `value` exactly to `i16` and returns `Some`. Every `i8` value is
representable.

<a id="declaration-73696c6b2f69383a3a746f493332"></a>

## `toI32`

```silk
pub fn toI32(value: i8) -> i32
```

Converts `value` exactly to `i32`. Every `i8` value is representable.

<a id="declaration-73696c6b2f69383a3a636865636b6564546f493332"></a>

## `checkedToI32`

```silk
pub fn checkedToI32(value: i8) -> silk/option.Option<i32>
```

Converts `value` exactly to `i32` and returns `Some`. Every `i8` value is
representable.

<a id="declaration-73696c6b2f69383a3a746f493634"></a>

## `toI64`

```silk
pub fn toI64(value: i8) -> i64
```

Converts `value` exactly to `i64`. Every `i8` value is representable.

<a id="declaration-73696c6b2f69383a3a636865636b6564546f493634"></a>

## `checkedToI64`

```silk
pub fn checkedToI64(value: i8) -> silk/option.Option<i64>
```

Converts `value` exactly to `i64` and returns `Some`. Every `i8` value is
representable.

<a id="declaration-73696c6b2f69383a3a746f4973697a65"></a>

## `toIsize`

```silk
pub fn toIsize(value: i8) -> isize
```

Converts `value` exactly to `isize`. Every `i8` value is representable.

<a id="declaration-73696c6b2f69383a3a636865636b6564546f4973697a65"></a>

## `checkedToIsize`

```silk
pub fn checkedToIsize(value: i8) -> silk/option.Option<isize>
```

Converts `value` exactly to `isize` and returns `Some`. Every `i8` value is
representable.

<a id="declaration-73696c6b2f69383a3a746f463332"></a>

## `toF32`

```silk
pub fn toF32(value: i8) -> f32
```

Converts `value` to the nearest `f32` value, with ties to even.

<a id="declaration-73696c6b2f69383a3a746f463634"></a>

## `toF64`

```silk
pub fn toF64(value: i8) -> f64
```

Converts `value` to the nearest `f64` value, with ties to even.

<a id="declaration-73696c6b2f69383a3a616464"></a>

## `add`

```silk
pub fn add(left: i8, right: i8) -> i8
```

Returns `left + right` and traps if the result is outside the `i8` range. Use this function
when overflow is a program error.

<a id="declaration-73696c6b2f69383a3a7375627472616374"></a>

## `subtract`

```silk
pub fn subtract(left: i8, right: i8) -> i8
```

Returns `left - right` and traps if the result is outside the `i8` range. Use this function
when overflow is a program error.

<a id="declaration-73696c6b2f69383a3a6d756c7469706c79"></a>

## `multiply`

```silk
pub fn multiply(left: i8, right: i8) -> i8
```

Returns `left * right` and traps if the result is outside the `i8` range. Use this function
when overflow is a program error.

<a id="declaration-73696c6b2f69383a3a646976696465"></a>

## `divide`

```silk
pub fn divide(left: i8, right: i8) -> i8
```

Returns `left / right`, rounded toward zero. Traps if `right` is zero or [`MIN`](#declaration-73696c6b2f69383a3a4d494e) is
divided by `-1`. Use this function when an invalid quotient is a program error.

<a id="declaration-73696c6b2f69383a3a72656d61696e646572"></a>

## `remainder`

```silk
pub fn remainder(left: i8, right: i8) -> i8
```

Returns the remainder with the sign of `left`. Traps if `right` is zero or [`MIN`](#declaration-73696c6b2f69383a3a4d494e) is
divided by `-1`. Use this function when invalid division is a program error.

<a id="declaration-73696c6b2f69383a3a626974416e64"></a>

## `bitAnd`

```silk
pub fn bitAnd(left: i8, right: i8) -> i8
```

Returns the bitwise AND of `left` and `right`.

<a id="declaration-73696c6b2f69383a3a6269744f72"></a>

## `bitOr`

```silk
pub fn bitOr(left: i8, right: i8) -> i8
```

Returns the bitwise OR of `left` and `right`.

<a id="declaration-73696c6b2f69383a3a626974586f72"></a>

## `bitXor`

```silk
pub fn bitXor(left: i8, right: i8) -> i8
```

Returns the bitwise exclusive OR of `left` and `right`.

<a id="declaration-73696c6b2f69383a3a6269744e6f74"></a>

## `bitNot`

```silk
pub fn bitNot(value: i8) -> i8
```

Returns `value` with each bit inverted.

<a id="declaration-73696c6b2f69383a3a73686966744c656674"></a>

## `shiftLeft`

```silk
pub fn shiftLeft(left: i8, right: i8) -> i8
```

Shifts `left` bits left by `right` positions. Traps if `right` is negative or not less than
[`BITS`](#declaration-73696c6b2f69383a3a42495453).

<a id="declaration-73696c6b2f69383a3a73686966745269676874"></a>

## `shiftRight`

```silk
pub fn shiftRight(left: i8, right: i8) -> i8
```

Shifts `left` bits right by `right` positions and preserves its sign. Traps if `right` is
negative or not less than [`BITS`](#declaration-73696c6b2f69383a3a42495453).

<a id="declaration-73696c6b2f69383a3a726f746174654c656674"></a>

## `rotateLeft`

```silk
pub fn rotateLeft(left: i8, right: i8) -> i8
```

Rotates the bits of `left` left by `right` positions.

<a id="declaration-73696c6b2f69383a3a726f746174655269676874"></a>

## `rotateRight`

```silk
pub fn rotateRight(left: i8, right: i8) -> i8
```

Rotates the bits of `left` right by `right` positions.

<a id="declaration-73696c6b2f69383a3a7772617070696e67416464"></a>

## `wrappingAdd`

```silk
pub fn wrappingAdd(left: i8, right: i8) -> i8
```

Returns `left + right`, wrapped to the `i8` range. Use this function for
deliberate modulo arithmetic.

<a id="declaration-73696c6b2f69383a3a7772617070696e675375627472616374"></a>

## `wrappingSubtract`

```silk
pub fn wrappingSubtract(left: i8, right: i8) -> i8
```

Returns `left - right`, wrapped to the `i8` range. Use this function for
deliberate modulo arithmetic.

<a id="declaration-73696c6b2f69383a3a7772617070696e674d756c7469706c79"></a>

## `wrappingMultiply`

```silk
pub fn wrappingMultiply(left: i8, right: i8) -> i8
```

Returns `left * right`, wrapped to the `i8` range. Use this function for
deliberate modulo arithmetic.

<a id="declaration-73696c6b2f69383a3a73617475726174696e67416464"></a>

## `saturatingAdd`

```silk
pub fn saturatingAdd(left: i8, right: i8) -> i8
```

Returns `left + right`, clamped to [`MIN`](#declaration-73696c6b2f69383a3a4d494e) or [`MAX`](#declaration-73696c6b2f69383a3a4d4158). Use this function when a
boundary value is the required overflow result.

<a id="declaration-73696c6b2f69383a3a73617475726174696e675375627472616374"></a>

## `saturatingSubtract`

```silk
pub fn saturatingSubtract(left: i8, right: i8) -> i8
```

Returns `left - right`, clamped to [`MIN`](#declaration-73696c6b2f69383a3a4d494e) or [`MAX`](#declaration-73696c6b2f69383a3a4d4158). Use this function when a
boundary value is the required overflow result.

<a id="declaration-73696c6b2f69383a3a73617475726174696e674d756c7469706c79"></a>

## `saturatingMultiply`

```silk
pub fn saturatingMultiply(left: i8, right: i8) -> i8
```

Returns `left * right`, clamped to [`MIN`](#declaration-73696c6b2f69383a3a4d494e) or [`MAX`](#declaration-73696c6b2f69383a3a4d4158). Use this function when a
boundary value is the required overflow result.

<a id="declaration-73696c6b2f69383a3a636865636b6564416464"></a>

## `checkedAdd`

```silk
pub fn checkedAdd(left: i8, right: i8) -> silk/option.Option<i8>
```

Returns `Some` with `left + right`, or `None` if the result is outside the `i8` range.
Use this function when overflow is input data.

<a id="declaration-73696c6b2f69383a3a636865636b65645375627472616374"></a>

## `checkedSubtract`

```silk
pub fn checkedSubtract(left: i8, right: i8) -> silk/option.Option<i8>
```

Returns `Some` with `left - right`, or `None` if the result is outside the `i8` range.
Use this function when overflow is input data.

<a id="declaration-73696c6b2f69383a3a636865636b65644d756c7469706c79"></a>

## `checkedMultiply`

```silk
pub fn checkedMultiply(left: i8, right: i8) -> silk/option.Option<i8>
```

Returns `Some` with `left * right`, or `None` if the result is outside the `i8` range.
Use this function when overflow is input data.

<a id="declaration-73696c6b2f69383a3a636865636b6564446976696465"></a>

## `checkedDivide`

```silk
pub fn checkedDivide(left: i8, right: i8) -> silk/option.Option<i8>
```

Returns `Some` with `left / right`, or `None` if `right` is zero or [`MIN`](#declaration-73696c6b2f69383a3a4d494e) is
divided by `-1`. Use this function when an invalid quotient is input data.

<a id="declaration-73696c6b2f69383a3a636865636b656452656d61696e646572"></a>

## `checkedRemainder`

```silk
pub fn checkedRemainder(left: i8, right: i8) -> silk/option.Option<i8>
```

Returns `Some` with the remainder, or `None` if `right` is zero or [`MIN`](#declaration-73696c6b2f69383a3a4d494e) is
divided by `-1`. Use this function when invalid division is input data.

<a id="declaration-73696c6b2f69383a3a657175616c73"></a>

## `equals`

```silk
pub fn equals(left: i8, right: i8) -> bool
```

Returns `true` when `left` and `right` are equal.

<a id="declaration-73696c6b2f69383a3a6e6f74457175616c73"></a>

## `notEquals`

```silk
pub fn notEquals(left: i8, right: i8) -> bool
```

Returns `true` when `left` and `right` are not equal.

<a id="declaration-73696c6b2f69383a3a6c6573735468616e"></a>

## `lessThan`

```silk
pub fn lessThan(left: i8, right: i8) -> bool
```

Returns `true` when `left` is less than `right`.

<a id="declaration-73696c6b2f69383a3a6c6573734f72457175616c"></a>

## `lessOrEqual`

```silk
pub fn lessOrEqual(left: i8, right: i8) -> bool
```

Returns `true` when `left` is less than or equal to `right`.

<a id="declaration-73696c6b2f69383a3a677265617465725468616e"></a>

## `greaterThan`

```silk
pub fn greaterThan(left: i8, right: i8) -> bool
```

Returns `true` when `left` is greater than `right`.

<a id="declaration-73696c6b2f69383a3a677265617465724f72457175616c"></a>

## `greaterOrEqual`

```silk
pub fn greaterOrEqual(left: i8, right: i8) -> bool
```

Returns `true` when `left` is greater than or equal to `right`.

<a id="declaration-73696c6b2f69383a3a7061727365"></a>

## `parse`

```silk
pub fn parse(text: string) -> silk/result.Result<i8, silk/format.ParseError>
```

Reads the complete text as a signed decimal `i8`.

### Details

A failure contains `silk.format.NotANumber` for empty text, a leading `+`, a non-digit, or
trailing bytes. It contains `silk.format.OutOfRange` if the number is outside the `i8` range.
