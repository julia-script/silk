# `silk/native_resolver`

<!-- Generated by packages/compiler/scripts/generate-documentation.mjs. Do not edit by hand. -->

Profiles: `aarch64-apple-darwin`, `aarch64-unknown-linux-gnu`, `x86_64-unknown-linux-gnu`.

Selected synchronous libc host resolution with one affine result-chain owner.

## When to use

Install NativeSystemResolver at a native application edge when blocking system DNS/NSS is
acceptable. Portable code should depend only on the Resolver service.

## Details

A deadline-free domain request makes exactly one getaddrinfo call and copies a finite result.
Every successful non-null chain is owned immediately and freed exactly once on every later exit.

## Gotchas

NativeSystemResolver rejects every domain deadline before observable work. Once getaddrinfo is
entered it has no cancellation, timeout, scheduler-parking, elapsed-time, or memory bound.

Import as `NativeSystemResolver` with `import silk.native_resolver { NativeSystemResolver }`.

Public declarations: 1.

<a id="declaration-73696c6b2f6e61746976655f7265736f6c7665723a3a4e617469766553797374656d5265736f6c766572"></a>

## `NativeSystemResolver`

```silk
pub struct NativeSystemResolver
```

The selected synchronous system Resolver provider.

<a id="declaration-73696c6b2f6e61746976655f7265736f6c7665723a3a4e617469766553797374656d5265736f6c7665722e6d616b65"></a>

### Associated function `NativeSystemResolver.make`

```silk
pub fn make() -> NativeSystemResolver
```

Creates a stateless synchronous native provider.

<a id="declaration-73696c6b2f6e61746976655f7265736f6c7665723a3a696d706c656d656e746174696f6e3a32"></a>

## Implementation `Resolver for NativeSystemResolver`

```silk
impl Resolver for NativeSystemResolver
```

<a id="declaration-73696c6b2f6e61746976655f7265736f6c7665723a3a696d706c656d656e746174696f6e3a323a3a6f7065726174696f6e3a30"></a>

### Operation `resolveDomain`

```silk
resolveDomain = NativeSystemResolver.resolveDomain
```
