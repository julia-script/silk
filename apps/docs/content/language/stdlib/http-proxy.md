# `silk/http_proxy`

<!-- Generated by packages/compiler/scripts/generate-documentation.mjs. Do not edit by hand. -->

Profiles: `aarch64-apple-darwin`, `aarch64-unknown-linux-gnu`, `aarch64-unknown-linux-gnu-no-libc`, `wasm32-unknown-unknown`, `x86_64-unknown-linux-gnu`, `x86_64-unknown-linux-gnu-no-libc`.

Bounded HTTP proxy policy, pure route selection, and owned CONNECT rejection metadata.

## Details

Proxy policy owns its prepared authentication token and exact-origin bypass entries. Selection
does not allocate or use the network. A selected route supplies its configured authentication
only through bounded output. CONNECT rejection copies have fixed field and owned-byte limits.

Import as `ProxyConfig` with `import silk.http_proxy { ProxyConfig }`.

Public declarations: 15.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6d706f6e656e74"></a>

## `ProxyComponent`

```silk
pub enum ProxyComponent
```

The proxy value rejected during admission.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6d706f6e656e743a3a6d656d6265723a30"></a>

### `ProxyUri`

```silk
ProxyUri = 0
```

The proxy URI text.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6d706f6e656e743a3a6d656d6265723a31"></a>

### `ProxyEndpoint`

```silk
ProxyEndpoint = 1
```

The programmatic proxy endpoint.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6d706f6e656e743a3a6d656d6265723a32"></a>

### `Authentication`

```silk
Authentication = 2
```

The prepared authentication token.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6d706f6e656e743a3a6d656d6265723a33"></a>

### `Bypass`

```silk
Bypass = 3
```

The exact-origin bypass policy.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6d706f6e656e743a3a6d656d6265723a34"></a>

### `Configuration`

```silk
Configuration = 4
```

The complete proxy configuration.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6d706f6e656e743a3a6d656d6265723a35"></a>

### `Route`

```silk
Route = 5
```

A selected route used by the wrong operation or with a different origin.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f78794c696d6974"></a>

## `ProxyLimit`

```silk
pub enum ProxyLimit
```

A bounded proxy configuration resource.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f78794c696d69743a3a6d656d6265723a30"></a>

### `AuthenticationBytes`

```silk
AuthenticationBytes = 0
```

The maximum encoded authentication-token byte count.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f78794c696d69743a3a6d656d6265723a31"></a>

### `BypassOrigins`

```silk
BypassOrigins = 1
```

The maximum exact-origin bypass-entry count.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f78794c696d69743a3a6d656d6265723a32"></a>

### `OwnedConfigurationBytes`

```silk
OwnedConfigurationBytes = 2
```

The maximum total owned configuration byte count.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879526561736f6e"></a>

## `ProxyReason`

```silk
pub union ProxyReason
```

The reason proxy policy admission failed.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879526561736f6e3a3a76617269616e743a30"></a>

### `InvalidProxyUri`

```silk
ProxyReason.InvalidProxyUri: ProxyReason
```

The proxy URI has invalid syntax or a prohibited URI component.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879526561736f6e3a3a76617269616e743a31"></a>

### `InvalidProxyPort`

```silk
ProxyReason.InvalidProxyPort: ProxyReason
```

The programmatic proxy endpoint has port zero.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879526561736f6e3a3a76617269616e743a32"></a>

### `InvalidAuthentication`

```silk
ProxyReason.InvalidAuthentication: ProxyReason
```

The prepared Basic token is empty or is not canonical strict Base64.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879526561736f6e3a3a76617269616e743a33"></a>

### `SizeOverflow`

```silk
ProxyReason.SizeOverflow: ProxyReason
```

The required owned byte count does not fit in `usize`.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879526561736f6e3a3a76617269616e743a34"></a>

### `LimitExceeded`

```silk
ProxyReason.LimitExceeded { limit: ProxyLimit, allowed: usize, attempted: usize }: ProxyReason
```

A resource exceeds its admitted maximum.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879526561736f6e3a3a76617269616e743a343a3a6669656c643a30"></a>

#### Field `limit`

```silk
pub limit: ProxyLimit
```

The resource that exceeds its limit.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879526561736f6e3a3a76617269616e743a343a3a6669656c643a31"></a>

#### Field `allowed`

```silk
pub allowed: usize
```

The inclusive admitted maximum.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879526561736f6e3a3a76617269616e743a343a3a6669656c643a32"></a>

#### Field `attempted`

```silk
pub attempted: usize
```

The rejected resource value.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879526561736f6e3a3a76617269616e743a35"></a>

### `UnsupportedProxyTransport`

```silk
ProxyReason.UnsupportedProxyTransport: ProxyReason
```

The proxy URI uses the unsupported `https` scheme.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879526561736f6e3a3a76617269616e743a36"></a>

### `InvalidRoute`

```silk
ProxyReason.InvalidRoute: ProxyReason
```

The selected route mode or original origin is incompatible with the requested operation.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f787941646d697373696f6e4572726f72"></a>

## `ProxyAdmissionError`

```silk
pub struct ProxyAdmissionError
```

A copyable proxy-policy admission failure containing no identity or credential bytes.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f787941646d697373696f6e4572726f723a3a6669656c643a30"></a>

### Field `component`

```silk
pub component: ProxyComponent
```

The proxy value that failed admission.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f787941646d697373696f6e4572726f723a3a6669656c643a31"></a>

### Field `reason`

```silk
pub reason: ProxyReason
```

The admission rule that failed.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f78794572726f72"></a>

## `ProxyError`

```silk
pub union ProxyError
```

A typed proxy failure whose response variants own only bounded, nonsecret metadata.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f78794572726f723a3a76617269616e743a30"></a>

### `Admission`

```silk
ProxyError.Admission { error: ProxyAdmissionError }: ProxyError
```

Configuration, route, or credential admission failed without retaining input bytes.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f78794572726f723a3a76617269616e743a303a3a6669656c643a30"></a>

#### Field `error`

```silk
pub error: ProxyAdmissionError
```

The structured copyable admission failure.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f78794572726f723a3a76617269616e743a31"></a>

### `ProxyMetadataLimit`

```silk
ProxyError.ProxyMetadataLimit: ProxyError
```

A complete CONNECT response exceeded the fixed owned-metadata budget.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f78794572726f723a3a76617269616e743a32"></a>

### `ProxyAuthenticationRequired`

```silk
ProxyError.ProxyAuthenticationRequired { response: OwnedResponseHead }: ProxyError
```

A 407 response retaining status, reason, and ordered `Proxy-Authenticate` fields only.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f78794572726f723a3a76617269616e743a323a3a6669656c643a30"></a>

#### Field `response`

```silk
pub response: OwnedResponseHead
```

The bounded independently owned response metadata.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f78794572726f723a3a76617269616e743a33"></a>

### `ProxyRejected`

```silk
ProxyError.ProxyRejected { response: OwnedResponseHead }: ProxyError
```

Another final non-2xx response retaining status, reason, and all ordered fields.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f78794572726f723a3a76617269616e743a333a3a6669656c643a30"></a>

#### Field `response`

```silk
pub response: OwnedResponseHead
```

The bounded independently owned response metadata.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f78794572726f723a3a76617269616e743a34"></a>

### `InvalidConnectResponse`

```silk
ProxyError.InvalidConnectResponse: ProxyError
```

Response classification was attempted before a final CONNECT response existed.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f78794572726f722e696e76616c6964526f757465"></a>

### Associated function `ProxyError.invalidRoute`

```silk
pub fn invalidRoute() -> ProxyError
```

Returns the credential-free failure for an incompatible sealed route.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6e6669674964"></a>

## `ProxyConfigId`

```silk
pub struct ProxyConfigId
```

An opaque configuration identity made from a caller-supplied nonsecret `u64`.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6e66696749642e6d616b65"></a>

### Associated function `ProxyConfigId.make`

```silk
pub fn make(value: u64) -> ProxyConfigId
```

Preserves a caller-supplied nonsecret identity payload opaquely.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6e66696749642e657175616c73"></a>

### Method `ProxyConfigId.equals`

```silk
pub fn equals<'life0, 'life1>(self: &'life0 ProxyConfigId, other: &'life1 ProxyConfigId) -> bool
```

Reports exact caller-payload equality without exposing the payload.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f787941757468436f6e746578744964"></a>

## `ProxyAuthContextId`

```silk
pub struct ProxyAuthContextId
```

An opaque authentication-context identity made from a caller-supplied nonsecret `u64`.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f787941757468436f6e7465787449642e6d616b65"></a>

### Associated function `ProxyAuthContextId.make`

```silk
pub fn make(value: u64) -> ProxyAuthContextId
```

Preserves a caller-supplied nonsecret identity payload opaquely.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f787941757468436f6e7465787449642e657175616c73"></a>

### Method `ProxyAuthContextId.equals`

```silk
pub fn equals<'life0, 'life1>(self: &'life0 ProxyAuthContextId, other: &'life1 ProxyAuthContextId) -> bool
```

Reports exact caller-payload equality without exposing the payload.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f787941757468"></a>

## `ProxyAuth`

```silk
pub union ProxyAuth
```

Prepared proxy authentication owned by a proxy configuration.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879417574683a3a76617269616e743a30"></a>

### `None`

```silk
ProxyAuth.None { contextIdValue: ProxyAuthContextId }: ProxyAuth
```

An authentication context that has no credential token.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879417574683a3a76617269616e743a31"></a>

### `Basic`

```silk
ProxyAuth.Basic { contextIdValue: ProxyAuthContextId, tokenValue: Bytes }: ProxyAuth
```

An authentication context that owns a prepared Basic token.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879417574682e6e6f6e65"></a>

### Associated function `ProxyAuth.none`

```silk
pub fn none(contextId: ProxyAuthContextId) -> ProxyAuth
```

Creates an authentication context with no credential bytes.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879417574682e70726570617265644261736963"></a>

### Associated function `ProxyAuth.preparedBasic`

```silk
pub effect<'life0> fn preparedBasic<'life0>(contextId: ProxyAuthContextId, token: &'life0 [u8]) -> silk/result.Result<silk/http_proxy.ProxyAuth, silk/http_proxy.ProxyError> ! OutOfMemoryError ? &mut Allocator
```

Strictly validates and copies one nonempty prepared Basic token.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879417574682e636f6e746578744964"></a>

### Method `ProxyAuth.contextId`

```silk
pub fn contextId<'life0>(self: &'life0 ProxyAuth) -> ProxyAuthContextId
```

Returns the caller-supplied authentication-context identity.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879417574682e6f776e65644279746573"></a>

### Method `ProxyAuth.ownedBytes`

```silk
pub fn ownedBytes<'life0>(self: &'life0 ProxyAuth) -> usize
```

Returns retained credential bytes without exposing them.

<a id="declaration-73696c6b2f687474705f70726f78793a3a427970617373506f6c696379"></a>

## `BypassPolicy`

```silk
pub struct BypassPolicy
```

A bounded list of exact normalized origins that bypass the proxy.

<a id="declaration-73696c6b2f687474705f70726f78793a3a427970617373506f6c6963792e636f7079"></a>

### Associated function `BypassPolicy.copy`

```silk
pub effect<'life0> fn copy<'life0>(origins: &'life0 [silk/http_origin.Origin]) -> silk/result.Result<silk/http_proxy.BypassPolicy, silk/http_proxy.ProxyError> ! OutOfMemoryError ? &mut Allocator
```

Copies at most 64 exact normalized HTTP origins into immutable owned policy.

<a id="declaration-73696c6b2f687474705f70726f78793a3a427970617373506f6c6963792e636f6e7461696e73"></a>

### Method `BypassPolicy.contains`

```silk
pub fn contains<'life0, 'life1>(self: &'life0 BypassPolicy, origin: &'life1 silk/http_origin.Origin) -> bool
```

Reports exact scheme, normalized-host, and effective-port membership.

<a id="declaration-73696c6b2f687474705f70726f78793a3a427970617373506f6c6963792e6f776e65644279746573"></a>

### Method `BypassPolicy.ownedBytes`

```silk
pub fn ownedBytes<'life0>(self: &'life0 BypassPolicy) -> usize
```

Returns copied bypass-entry storage size.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6e666967"></a>

## `ProxyConfig`

```silk
pub struct ProxyConfig
```

Immutable owned proxy configuration.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6e6669672e6d616b65"></a>

### Associated function `ProxyConfig.make`

```silk
pub fn make(id: ProxyConfigId, proxyHost: Host, proxyPort: Port, authentication: ProxyAuth, bypass: BypassPolicy, maxOwnedConfigBytes: usize) -> silk/result.Result<silk/http_proxy.ProxyConfig, silk/http_proxy.ProxyError>
```

Admits a plain programmatic proxy endpoint under a total owned-byte budget.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6e6669672e66726f6d557269"></a>

### Associated function `ProxyConfig.fromUri`

```silk
pub fn fromUri<'text>(id: ProxyConfigId, text: string<'text>, authentication: ProxyAuth, bypass: BypassPolicy, maxOwnedConfigBytes: usize) -> silk/result.Result<silk/http_proxy.ProxyConfig, silk/http_proxy.ProxyError>
```

Parses only a plain `http` authority with no userinfo, path, query, or fragment.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6e6669672e6964"></a>

### Method `ProxyConfig.id`

```silk
pub fn id<'life0>(self: &'life0 ProxyConfig) -> ProxyConfigId
```

Returns the caller-supplied configuration identity.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6e6669672e61757468436f6e746578744964"></a>

### Method `ProxyConfig.authContextId`

```silk
pub fn authContextId<'life0>(self: &'life0 ProxyConfig) -> ProxyAuthContextId
```

Returns the caller-supplied authentication-context identity.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6e6669672e70726f7879486f7374"></a>

### Method `ProxyConfig.proxyHost`

```silk
pub fn proxyHost<'life0>(self: &'life0 ProxyConfig) -> Host
```

Returns the checked proxy host.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6e6669672e70726f7879506f7274"></a>

### Method `ProxyConfig.proxyPort`

```silk
pub fn proxyPort<'life0>(self: &'life0 ProxyConfig) -> Port
```

Returns the checked nonzero proxy port.

<a id="declaration-73696c6b2f687474705f70726f78793a3a50726f7879436f6e6669672e6f776e65644279746573"></a>

### Method `ProxyConfig.ownedBytes`

```silk
pub fn ownedBytes<'life0>(self: &'life0 ProxyConfig) -> usize
```

Returns the copied host, token, and bypass-entry byte count.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574654d6f6465"></a>

## `RouteMode`

```silk
pub enum RouteMode
```

The route mode selected for one origin.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574654d6f64653a3a6d656d6265723a30"></a>

### `Direct`

```silk
Direct = 0
```

Contact the origin directly.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574654d6f64653a3a6d656d6265723a31"></a>

### `Forward`

```silk
Forward = 1
```

Contact the proxy and use an absolute-form HTTP request.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574654d6f64653a3a6d656d6265723a32"></a>

### `Tunnel`

```silk
Tunnel = 2
```

Contact the proxy and use CONNECT before origin TLS.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f757465"></a>

## `Route`

```silk
pub union Route<'configuration>
```

The pure route selected for one original HTTP origin.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574653a3a76617269616e743a30"></a>

### `Direct`

```silk
Route<'configuration>.Direct { identity: RouteIdentity, configuration: &'configuration silk/http_proxy.ProxyConfig, origin: Origin }: Route<'configuration>
```

A route that contacts an exact bypass origin directly.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574653a3a76617269616e743a303a3a6669656c643a32"></a>

#### Field `origin`

```silk
pub origin: Origin
```

The original origin that matched bypass policy.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574653a3a76617269616e743a31"></a>

### `Forward`

```silk
Route<'configuration>.Forward { identity: RouteIdentity, configuration: &'configuration silk/http_proxy.ProxyConfig, origin: Origin }: Route<'configuration>
```

A route that sends plaintext HTTP through the proxy.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574653a3a76617269616e743a313a3a6669656c643a32"></a>

#### Field `origin`

```silk
pub origin: Origin
```

The original plaintext HTTP origin.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574653a3a76617269616e743a32"></a>

### `Tunnel`

```silk
Route<'configuration>.Tunnel { identity: RouteIdentity, configuration: &'configuration silk/http_proxy.ProxyConfig, origin: Origin }: Route<'configuration>
```

A route that establishes a proxy tunnel for origin TLS.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574653a3a76617269616e743a323a3a6669656c643a32"></a>

#### Field `origin`

```silk
pub origin: Origin
```

The original HTTPS origin for TLS authentication.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574652e6d6f6465"></a>

### Method `Route.mode`

```silk
pub fn mode<'configuration, 'life1>(self: &'life1 Route<'configuration>) -> RouteMode
```

Returns the selected route mode.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574652e6f726967696e616c4f726967696e"></a>

### Method `Route.originalOrigin`

```silk
pub fn originalOrigin<'configuration, 'life1>(self: &'life1 Route<'configuration>) -> Origin
```

Returns the original origin that policy evaluated.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574652e70726f7879486f7374"></a>

### Method `Route.proxyHost`

```silk
pub fn proxyHost<'configuration, 'life1>(self: &'life1 Route<'configuration>) -> Host
```

Returns the proxy host retained when policy selected the route.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574652e70726f7879506f7274"></a>

### Method `Route.proxyPort`

```silk
pub fn proxyPort<'configuration, 'life1>(self: &'life1 Route<'configuration>) -> Port
```

Returns the proxy port retained when policy selected the route.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574652e706879736963616c50656572"></a>

### Method `Route.physicalPeer`

```silk
pub fn physicalPeer<'configuration, 'life1>(self: &'life1 Route<'configuration>) -> silk/result.Result<silk/http_origin.Origin, silk/http_origin.OriginError>
```

Returns the plaintext proxy peer retained by the sealed route.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574652e70726f7879417574686f72697a6174696f6e4c656e677468"></a>

### Method `Route.proxyAuthorizationLength`

```silk
pub fn proxyAuthorizationLength<'configuration, 'life1>(self: &'life1 Route<'configuration>) -> usize
```

Returns the complete configured `Proxy-Authorization` value length, or zero when absent.

#### Details

The prepared token remains private; possessing a policy-selected route is the capability to
request its bounded wire representation.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574652e777269746550726f7879417574686f72697a6174696f6e496e746f"></a>

### Method `Route.writeProxyAuthorizationInto`

```silk
pub fn writeProxyAuthorizationInto<'configuration, 'life1, 'life2>(self: &'life1 Route<'configuration>, output: &'life2 mut [u8]) -> silk/option.Option<usize>
```

Writes the complete configured `Proxy-Authorization` value without exposing token storage.
Returns `None` without changing `output` when it is too small.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574652e636f6e6669674964"></a>

### Method `Route.configId`

```silk
pub fn configId<'configuration, 'life1>(self: &'life1 Route<'configuration>) -> ProxyConfigId
```

Returns the configuration identity retained when policy selected the route.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574652e61757468436f6e746578744964"></a>

### Method `Route.authContextId`

```silk
pub fn authContextId<'configuration, 'life1>(self: &'life1 Route<'configuration>) -> ProxyAuthContextId
```

Returns the authentication-context identity retained when policy selected the route.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574652e6b6579"></a>

### Method `Route.key`

```silk
pub fn key<'configuration, 'life1>(self: &'life1 Route<'configuration>) -> RouteKey
```

Returns a stable key without reading or formatting authentication bytes.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574652e7265636f6d70757465"></a>

### Method `Route.recompute`

```silk
pub fn recompute<'configuration, 'life1>(self: &'life1 Route<'configuration>, origin: Origin) -> silk/http_proxy.Route<'configuration>
```

Recomputes policy for a new origin under the current immutable configuration.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574654b6579"></a>

## `RouteKey`

```silk
pub struct RouteKey
```

Stable route identity for later pooling and redirect recomputation.

<a id="declaration-73696c6b2f687474705f70726f78793a3a526f7574654b65792e657175616c73"></a>

### Method `RouteKey.equals`

```silk
pub fn equals<'life0, 'life1>(self: &'life0 RouteKey, other: &'life1 RouteKey) -> bool
```

Compares route mode, original origin, proxy endpoint, and both opaque identities.

<a id="declaration-73696c6b2f687474705f70726f78793a3a696d706c656d656e746174696f6e3a30"></a>

## Implementation `Copy for ProxyReason`

```silk
impl Copy for ProxyReason
```

<a id="declaration-73696c6b2f687474705f70726f78793a3a696d706c656d656e746174696f6e3a31"></a>

## Implementation `Copy for ProxyAdmissionError`

```silk
impl Copy for ProxyAdmissionError
```

<a id="declaration-73696c6b2f687474705f70726f78793a3a696d706c656d656e746174696f6e3a32"></a>

## Implementation `Copy for ProxyConfigId`

```silk
impl Copy for ProxyConfigId
```

<a id="declaration-73696c6b2f687474705f70726f78793a3a696d706c656d656e746174696f6e3a33"></a>

## Implementation `Copy for ProxyAuthContextId`

```silk
impl Copy for ProxyAuthContextId
```

<a id="declaration-73696c6b2f687474705f70726f78793a3a696d706c656d656e746174696f6e3a35"></a>

## Implementation `Copy for silk/http_proxy.Route<'configuration>`

```silk
impl Copy for silk/http_proxy.Route<'configuration>
```

<a id="declaration-73696c6b2f687474705f70726f78793a3a696d706c656d656e746174696f6e3a36"></a>

## Implementation `Copy for RouteKey`

```silk
impl Copy for RouteKey
```

<a id="declaration-73696c6b2f687474705f70726f78793a3a73656c656374526f757465"></a>

## `selectRoute`

```silk
pub fn selectRoute<'configuration>(configuration: &'configuration silk/http_proxy.ProxyConfig, origin: Origin) -> silk/http_proxy.Route<'configuration>
```

Purely selects Direct, Forward, or Tunnel for one original origin.

<a id="declaration-73696c6b2f687474705f70726f78793a3a636c617373696679436f6e6e656374"></a>

## `classifyConnect`

```silk
pub effect<'env> fn classifyConnect<'life0: 'env, 'life1: 'env, 'env>(head: &'life0 silk/http.ResponseHead<'life1>) -> () ! ProxyError | OutOfMemoryError ? &mut Allocator
```

Classifies a complete final CONNECT response into success or bounded rejection metadata.

### Details

Any 2xx response is accepted. A 407 retains status, reason, and ordered duplicate
`Proxy-Authenticate` fields only; other final non-2xx responses retain all ordered fields.
Complete-head copy failures map to `ProxyMetadataLimit`, while allocation failure remains
`OutOfMemoryError`. Call this only after the shared client's parser has returned a final head.
