# `silk/json_value`

<!-- Generated by packages/compiler/scripts/generate-documentation.mjs. Do not edit by hand. -->

Profiles: `aarch64-apple-darwin`, `aarch64-unknown-linux-gnu`, `aarch64-unknown-linux-gnu-no-libc`, `wasm32-unknown-unknown`, `x86_64-unknown-linux-gnu`, `x86_64-unknown-linux-gnu-no-libc`.

Owned dynamic JSON values parsed from a byte slice and written through the ambient Writer.

## When to use

Use [`Json.parse`](#declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e2e7061727365) when a document needs borrowed field access after parsing. Use
[`Json.write`](#declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e2e7772697465) or [`Json.writeWith`](#declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e2e777269746557697468) to serialize an owned tree. Token-only consumers can use
`silk.json_scanner` without an allocator.

## Details

Arrays and objects retain source order. Numbers retain valid source spelling and decode into
integers or floats only when accessed. String escapes are decoded into owned UTF-8 storage. A failed
parse releases every partially built value.

## Gotchas

A borrowed field or string cannot outlive its owning [`Value`](#declaration-73696c6b2f6a736f6e5f76616c75653a3a56616c7565). JSON output can leave an
accepted prefix if `Writer` fails; it does not flush the writer.

## Examples

### Read an integer member

```silk
import silk.allocator { Allocator, OutOfMemoryError }
import silk.effect { Effect }
import silk.json_scanner { JsonError }
import silk.json_value { Json }
import silk.option { Option }

effect fn read() -> i32 ! JsonError | OutOfMemoryError {
  let mut allocator = Allocator.systemAllocatorProvider()
  let document = run Json.parse(b"{\"id\":42}")
    |> Effect.provideMut<Allocator>(&mut allocator)
  return Json.field(&document, "id") |> Json.asI32 |> Option.unwrapOr<i32>(0)
}

effect fn recover(error: JsonError | OutOfMemoryError) -> i32 { return 0 }
pub fn main() -> i32 { return run Effect.catchAll(read(), recover) }
```

Import as `Json` with `import silk.json_value { Json }`.

Public declarations: 4.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e456e747279"></a>

## `JsonEntry`

```silk
pub struct JsonEntry
```

One owned key/value pair in an insertion-ordered JSON object.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e456e7472793a3a6669656c643a30"></a>

### Field `key`

```silk
pub key: String
```

The decoded key.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e456e7472793a3a6669656c643a31"></a>

### Field `value`

```silk
pub value: Value
```

The owned member value.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e4368696c64"></a>

## `JsonChild`

```silk
pub union JsonChild
```

One owned child: an array item or an object member with its decoded key.

### Details

Both container kinds use this one recursive storage shape, so cleanup follows one finite
`Box<Vector<JsonChild>>` specialization even when arrays and objects nest in each other.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e4368696c643a3a76617269616e743a30"></a>

### `Item`

```silk
JsonChild.Item { value: Value }: JsonChild
```

An array element without a key.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e4368696c643a3a76617269616e743a303a3a6669656c643a30"></a>

#### Field `value`

```silk
pub value: Value
```

The owned element value.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e4368696c643a3a76617269616e743a31"></a>

### `Member`

```silk
JsonChild.Member { entry: JsonEntry }: JsonChild
```

An object member with a decoded key.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e4368696c643a3a76617269616e743a313a3a6669656c643a30"></a>

#### Field `entry`

```silk
pub entry: JsonEntry
```

The owned key and value.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a56616c7565"></a>

## `Value`

```silk
pub union Value
```

An owned JSON null, boolean, raw number, decoded string, array, or object.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a56616c75653a3a76617269616e743a30"></a>

### `Null`

```silk
Value.Null: Value
```

JSON `null`.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a56616c75653a3a76617269616e743a31"></a>

### `Bool`

```silk
Value.Bool { value: bool }: Value
```

A JSON boolean.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a56616c75653a3a76617269616e743a313a3a6669656c643a30"></a>

#### Field `value`

```silk
pub value: bool
```

The boolean value.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a56616c75653a3a76617269616e743a32"></a>

### `Number`

```silk
Value.Number { text: String }: Value
```

A valid number lexeme retained exactly as read.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a56616c75653a3a76617269616e743a323a3a6669656c643a30"></a>

#### Field `text`

```silk
pub text: String
```

The valid number text in its source spelling.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a56616c75653a3a76617269616e743a33"></a>

### `String`

```silk
Value.String { text: String }: Value
```

Decoded UTF-8 text.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a56616c75653a3a76617269616e743a333a3a6669656c643a30"></a>

#### Field `text`

```silk
pub text: String
```

The decoded UTF-8 text.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a56616c75653a3a76617269616e743a34"></a>

### `Array`

```silk
Value.Array { values: silk/box.Box<silk/vector.Vector<silk/json_value.JsonChild>> }: Value
```

Values in source order.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a56616c75653a3a76617269616e743a343a3a6669656c643a30"></a>

#### Field `values`

```silk
pub values: silk/box.Box<silk/vector.Vector<silk/json_value.JsonChild>>
```

The array elements in source order.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a56616c75653a3a76617269616e743a35"></a>

### `Object`

```silk
Value.Object { entries: silk/box.Box<silk/vector.Vector<silk/json_value.JsonChild>> }: Value
```

Members in source order.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a56616c75653a3a76617269616e743a353a3a6669656c643a30"></a>

#### Field `entries`

```silk
pub entries: silk/box.Box<silk/vector.Vector<silk/json_value.JsonChild>>
```

The object members in source order.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e"></a>

## `Json`

```silk
pub struct Json
```

Operations on owned JSON values.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e2e7061727365"></a>

### Associated function `Json.parse`

```silk
pub effect<'life0> fn parse<'life0>(bytes: &'life0 [u8]) -> Value ! JsonError | OutOfMemoryError ? &mut Allocator
```

Parses one complete strict JSON document into an owned value tree.

#### Details

Every allocation is charged to the ambient allocator. On syntax, duplicate-key, or
allocation failure, the partially built tree is dropped.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e2e7772697465"></a>

### Associated function `Json.write`

```silk
pub effect<'life0> fn write<'life0>(document: &'life0 silk/json_value.Value) -> () ! JsonError | WriterError ? &mut Writer
```

Writes a tree compactly through the ambient Writer.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e2e777269746557697468"></a>

### Associated function `Json.writeWith`

```silk
pub effect<'env> fn writeWith<'life0: 'env, 'life1: 'env, 'env>(document: &'life0 silk/json_value.Value, options: &'life1 silk/json_output.JsonOptions) -> () ! JsonError | WriterError ? &mut Writer
```

Writes a tree with explicit indentation and depth policy.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e2e6669656c64"></a>

### Associated function `Json.field`

```silk
pub fn field<'a, 'life1>(self: &'a silk/json_value.Value, key: string<'life1>) -> silk/option.Option<&'a silk/json_value.Value>
```

Borrows a member of an object, or returns `None` for an absent key or other value kind.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e2e6669656c644174"></a>

### Associated function `Json.fieldAt`

```silk
pub fn fieldAt<'a, 'life1>(self: silk/option.Option<&'a silk/json_value.Value>, key: string<'life1>) -> silk/option.Option<&'a silk/json_value.Value>
```

Chains an object lookup while preserving an absent value.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e2e6173493332"></a>

### Associated function `Json.asI32`

```silk
pub fn asI32<'life0>(self: silk/option.Option<&'life0 silk/json_value.Value>) -> silk/option.Option<i32>
```

Decodes an integer number into `i32`, returning `None` for absence or overflow.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e2e6173493634"></a>

### Associated function `Json.asI64`

```silk
pub fn asI64<'life0>(self: silk/option.Option<&'life0 silk/json_value.Value>) -> silk/option.Option<i64>
```

Decodes an integer number into `i64`, returning `None` for absence or overflow.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e2e6173553634"></a>

### Associated function `Json.asU64`

```silk
pub fn asU64<'life0>(self: silk/option.Option<&'life0 silk/json_value.Value>) -> silk/option.Option<u64>
```

Decodes an integer number into `u64`, returning `None` for absence or overflow.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e2e6173463634"></a>

### Associated function `Json.asF64`

```silk
pub fn asF64<'life0>(self: silk/option.Option<&'life0 silk/json_value.Value>) -> silk/option.Option<f64>
```

Decodes a finite `f64`, or returns `None` for absence, wrong type, invalid text, or overflow.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e2e6173463332"></a>

### Associated function `Json.asF32`

```silk
pub fn asF32<'life0>(self: silk/option.Option<&'life0 silk/json_value.Value>) -> silk/option.Option<f32>
```

Decodes a finite `f32`, or returns `None` for absence, wrong type, invalid text, or overflow.

<a id="declaration-73696c6b2f6a736f6e5f76616c75653a3a4a736f6e2e6173537472696e67"></a>

### Associated function `Json.asString`

```silk
pub fn asString<'a>(self: silk/option.Option<&'a silk/json_value.Value>) -> silk/option.Option<string<'a>>
```

Borrows decoded string text while its owning tree remains alive.
