# `silk/http_basic`

<!-- Generated by packages/compiler/scripts/generate-documentation.mjs. Do not edit by hand. -->

Profiles: `aarch64-apple-darwin`, `aarch64-unknown-linux-gnu`, `aarch64-unknown-linux-gnu-no-libc`, `wasm32-unknown-unknown`, `x86_64-unknown-linux-gnu`, `x86_64-unknown-linux-gnu-no-libc`.

Explicit bounded HTTP Basic authorization from caller-encoded credential bytes.

## Details

The caller chooses the character encoding and supplies scratch and output buffers.
Encoding validates both credentials and both capacities before it changes either buffer.
The HTTP client controls origin binding and permission to send credentials over plain HTTP.

Import as `HttpBasic` with `import silk.http_basic as HttpBasic`.

Public declarations: 3.

<a id="declaration-73696c6b2f687474705f62617369633a3a42617369634572726f72"></a>

## `BasicError`

```silk
pub enum BasicError
```

A credential validation or capacity failure without credential contents.

<a id="declaration-73696c6b2f687474705f62617369633a3a42617369634572726f723a3a6d656d6265723a30"></a>

### `InvalidUsername`

```silk
InvalidUsername = 0
```

The username contains a colon, a control byte, or DEL.

<a id="declaration-73696c6b2f687474705f62617369633a3a42617369634572726f723a3a6d656d6265723a31"></a>

### `InvalidPassword`

```silk
InvalidPassword = 1
```

The password contains a control byte or DEL.

<a id="declaration-73696c6b2f687474705f62617369633a3a42617369634572726f723a3a6d656d6265723a32"></a>

### `SizeOverflow`

```silk
SizeOverflow = 2
```

The credential or encoded size cannot fit in usize.

<a id="declaration-73696c6b2f687474705f62617369633a3a42617369634572726f723a3a6d656d6265723a33"></a>

### `ScratchTooSmall`

```silk
ScratchTooSmall = 3
```

Scratch cannot hold the username, separator, and password.

<a id="declaration-73696c6b2f687474705f62617369633a3a42617369634572726f723a3a6d656d6265723a34"></a>

### `OutputTooSmall`

```silk
OutputTooSmall = 4
```

Output cannot hold the complete authorization value.

<a id="declaration-73696c6b2f687474705f62617369633a3a656e636f6465644c656e677468"></a>

## `encodedLength`

```silk
pub fn encodedLength(usernameLength: usize, passwordLength: usize) -> silk/result.Result<usize, silk/http_basic.BasicError>
```

Returns the exact output length, including the six-byte `Basic ` prefix.

<a id="declaration-73696c6b2f687474705f62617369633a3a656e636f6465496e746f"></a>

## `encodeInto`

```silk
pub fn encodeInto<'life0, 'life1, 'life2, 'life3>(username: &'life0 [u8], password: &'life1 [u8], scratch: &'life2 mut [u8], output: &'life3 mut [u8]) -> silk/result.Result<usize, silk/http_basic.BasicError>
```

Writes `Basic ` and padded Base64 of `username:password`, returning the complete byte count.

### Details

A colon in the username or a control byte or DEL in either credential fails before mutation.
Non-ASCII bytes are accepted without character conversion. Failure leaves output and scratch unchanged.
Scratch retains the combined credential after success; the caller controls its later use and erasure.
