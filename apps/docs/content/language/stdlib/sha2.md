# `silk/sha2`

<!-- Generated by packages/compiler/scripts/generate-documentation.mjs. Do not edit by hand. -->

Profiles: `aarch64-apple-darwin`, `aarch64-unknown-linux-gnu`, `aarch64-unknown-linux-gnu-no-libc`, `wasm32-unknown-unknown`, `x86_64-unknown-linux-gnu`, `x86_64-unknown-linux-gnu-no-libc`.

Portable SHA-2 states for six fixed-output cryptographic digest algorithms.

## When to use

Use [`Sha224`](#declaration-73696c6b2f736861323a3a536861323234) or [`Sha256`](#declaration-73696c6b2f736861323a3a536861323536) for a 28-byte or 32-byte digest with 32-bit processing. Use
[`Sha384`](#declaration-73696c6b2f736861323a3a536861333834), [`Sha512`](#declaration-73696c6b2f736861323a3a536861353132), [`Sha512_224`](#declaration-73696c6b2f736861323a3a5368613531325f323234), or [`Sha512_256`](#declaration-73696c6b2f736861323a3a5368613531325f323536) for SHA-512-family processing.
Use `silk.hash` only for collection keys. It is not a cryptographic hash.

## Details

Each actor supports incremental input through `update`, consuming finalization through
`finish`, and one-shot input through `hash`. The states use only inline storage and allocate no
memory. Each digest uses the standardized big-endian byte order.

## Gotchas

If an update exceeds the algorithm's standard bit-length domain, the program traps. `finish`
consumes its state, so the program cannot update or finish that state again.

## Examples

### Compare incremental and one-shot SHA-256

```silk
import silk.sha2 {Sha256}

pub fn main() -> i32 {
  let message: [u8; 3] = [97, 98, 99]
  let mut state = Sha256.make()
  state.update(&message)
  let incremental = state.finish()
  let oneShot = Sha256.hash(&message)
  if incremental[0] != 186 || oneShot[0] != 186 {
    return 1
  }
  return 0
}
```

Import as `Sha256` with `import silk.sha2 { Sha256 }`.

Public declarations: 6.

<a id="declaration-73696c6b2f736861323a3a536861323234"></a>

## `Sha224`

```silk
pub struct Sha224
```

An allocation-free incremental SHA-224 state that returns a 28-byte digest.

<a id="declaration-73696c6b2f736861323a3a5368613232342e6d616b65"></a>

### Associated function `Sha224.make`

```silk
pub fn make() -> Sha224
```

Creates an empty SHA-224 state.

<a id="declaration-73696c6b2f736861323a3a5368613232342e757064617465"></a>

### Method `Sha224.update`

```silk
pub fn update<'life0, 'life1>(self: &'life0 mut Sha224, bytes: &'life1 [u8]) -> ()
```

Adds `bytes` to this SHA-224 state in source order. An empty slice does not change the state.
If the complete message exceeds the 64-bit bit-length domain, the program traps.

<a id="declaration-73696c6b2f736861323a3a5368613232342e66696e697368"></a>

### Method `Sha224.finish`

```silk
pub fn finish(self: Sha224) -> Array<u8, 28>
```

Consumes this SHA-224 state and returns its 28-byte digest in big-endian byte order.

<a id="declaration-73696c6b2f736861323a3a5368613232342e68617368"></a>

### Associated function `Sha224.hash`

```silk
pub fn hash<'life0>(bytes: &'life0 [u8]) -> Array<u8, 28>
```

Returns the 28-byte SHA-224 digest of `bytes`. If the message is too long, the program traps.

<a id="declaration-73696c6b2f736861323a3a536861323536"></a>

## `Sha256`

```silk
pub struct Sha256
```

An allocation-free incremental SHA-256 state that returns a 32-byte digest.

<a id="declaration-73696c6b2f736861323a3a5368613235362e6d616b65"></a>

### Associated function `Sha256.make`

```silk
pub fn make() -> Sha256
```

Creates an empty SHA-256 state.

<a id="declaration-73696c6b2f736861323a3a5368613235362e757064617465"></a>

### Method `Sha256.update`

```silk
pub fn update<'life0, 'life1>(self: &'life0 mut Sha256, bytes: &'life1 [u8]) -> ()
```

Adds `bytes` to this SHA-256 state in source order. An empty slice does not change the state.
If the complete message exceeds the 64-bit bit-length domain, the program traps.

<a id="declaration-73696c6b2f736861323a3a5368613235362e66696e697368"></a>

### Method `Sha256.finish`

```silk
pub fn finish(self: Sha256) -> Array<u8, 32>
```

Consumes this SHA-256 state and returns its 32-byte digest in big-endian byte order.

<a id="declaration-73696c6b2f736861323a3a5368613235362e68617368"></a>

### Associated function `Sha256.hash`

```silk
pub fn hash<'life0>(bytes: &'life0 [u8]) -> Array<u8, 32>
```

Returns the 32-byte SHA-256 digest of `bytes`. If the message is too long, the program traps.

<a id="declaration-73696c6b2f736861323a3a536861333834"></a>

## `Sha384`

```silk
pub struct Sha384
```

An allocation-free incremental SHA-384 state that returns a 48-byte digest.

<a id="declaration-73696c6b2f736861323a3a5368613338342e6d616b65"></a>

### Associated function `Sha384.make`

```silk
pub fn make() -> Sha384
```

Creates an empty SHA-384 state.

<a id="declaration-73696c6b2f736861323a3a5368613338342e757064617465"></a>

### Method `Sha384.update`

```silk
pub fn update<'life0, 'life1>(self: &'life0 mut Sha384, bytes: &'life1 [u8]) -> ()
```

Adds `bytes` to this SHA-384 state in source order. An empty slice does not change the state.
If the complete message exceeds the 128-bit bit-length domain, the program traps.

<a id="declaration-73696c6b2f736861323a3a5368613338342e66696e697368"></a>

### Method `Sha384.finish`

```silk
pub fn finish(self: Sha384) -> Array<u8, 48>
```

Consumes this SHA-384 state and returns its 48-byte digest in big-endian byte order.

<a id="declaration-73696c6b2f736861323a3a5368613338342e68617368"></a>

### Associated function `Sha384.hash`

```silk
pub fn hash<'life0>(bytes: &'life0 [u8]) -> Array<u8, 48>
```

Returns the 48-byte SHA-384 digest of `bytes`. If the message is too long, the program traps.

<a id="declaration-73696c6b2f736861323a3a536861353132"></a>

## `Sha512`

```silk
pub struct Sha512
```

An allocation-free incremental SHA-512 state that returns a 64-byte digest.

<a id="declaration-73696c6b2f736861323a3a5368613531322e6d616b65"></a>

### Associated function `Sha512.make`

```silk
pub fn make() -> Sha512
```

Creates an empty SHA-512 state.

<a id="declaration-73696c6b2f736861323a3a5368613531322e757064617465"></a>

### Method `Sha512.update`

```silk
pub fn update<'life0, 'life1>(self: &'life0 mut Sha512, bytes: &'life1 [u8]) -> ()
```

Adds `bytes` to this SHA-512 state in source order. An empty slice does not change the state.
If the complete message exceeds the 128-bit bit-length domain, the program traps.

<a id="declaration-73696c6b2f736861323a3a5368613531322e66696e697368"></a>

### Method `Sha512.finish`

```silk
pub fn finish(self: Sha512) -> Array<u8, 64>
```

Consumes this SHA-512 state and returns its 64-byte digest in big-endian byte order.

<a id="declaration-73696c6b2f736861323a3a5368613531322e68617368"></a>

### Associated function `Sha512.hash`

```silk
pub fn hash<'life0>(bytes: &'life0 [u8]) -> Array<u8, 64>
```

Returns the 64-byte SHA-512 digest of `bytes`. If the message is too long, the program traps.

<a id="declaration-73696c6b2f736861323a3a5368613531325f323234"></a>

## `Sha512_224`

```silk
pub struct Sha512_224
```

An allocation-free incremental SHA-512/224 state that returns a 28-byte digest.

<a id="declaration-73696c6b2f736861323a3a5368613531325f3232342e6d616b65"></a>

### Associated function `Sha512_224.make`

```silk
pub fn make() -> Sha512_224
```

Creates an empty SHA-512/224 state.

<a id="declaration-73696c6b2f736861323a3a5368613531325f3232342e757064617465"></a>

### Method `Sha512_224.update`

```silk
pub fn update<'life0, 'life1>(self: &'life0 mut Sha512_224, bytes: &'life1 [u8]) -> ()
```

Adds `bytes` to this SHA-512/224 state in source order. An empty slice does not change the
state. If the complete message exceeds the 128-bit bit-length domain, the program traps.

<a id="declaration-73696c6b2f736861323a3a5368613531325f3232342e66696e697368"></a>

### Method `Sha512_224.finish`

```silk
pub fn finish(self: Sha512_224) -> Array<u8, 28>
```

Consumes this SHA-512/224 state and returns its 28-byte digest in big-endian byte order.

<a id="declaration-73696c6b2f736861323a3a5368613531325f3232342e68617368"></a>

### Associated function `Sha512_224.hash`

```silk
pub fn hash<'life0>(bytes: &'life0 [u8]) -> Array<u8, 28>
```

Returns the 28-byte SHA-512/224 digest of `bytes`. If the message is too long, the program
traps.

<a id="declaration-73696c6b2f736861323a3a5368613531325f323536"></a>

## `Sha512_256`

```silk
pub struct Sha512_256
```

An allocation-free incremental SHA-512/256 state that returns a 32-byte digest.

<a id="declaration-73696c6b2f736861323a3a5368613531325f3235362e6d616b65"></a>

### Associated function `Sha512_256.make`

```silk
pub fn make() -> Sha512_256
```

Creates an empty SHA-512/256 state.

<a id="declaration-73696c6b2f736861323a3a5368613531325f3235362e757064617465"></a>

### Method `Sha512_256.update`

```silk
pub fn update<'life0, 'life1>(self: &'life0 mut Sha512_256, bytes: &'life1 [u8]) -> ()
```

Adds `bytes` to this SHA-512/256 state in source order. An empty slice does not change the
state. If the complete message exceeds the 128-bit bit-length domain, the program traps.

<a id="declaration-73696c6b2f736861323a3a5368613531325f3235362e66696e697368"></a>

### Method `Sha512_256.finish`

```silk
pub fn finish(self: Sha512_256) -> Array<u8, 32>
```

Consumes this SHA-512/256 state and returns its 32-byte digest in big-endian byte order.

<a id="declaration-73696c6b2f736861323a3a5368613531325f3235362e68617368"></a>

### Associated function `Sha512_256.hash`

```silk
pub fn hash<'life0>(bytes: &'life0 [u8]) -> Array<u8, 32>
```

Returns the 32-byte SHA-512/256 digest of `bytes`. If the message is too long, the program
traps.
