# `silk/http`

<!-- Generated by packages/compiler/scripts/generate-documentation.mjs. Do not edit by hand. -->

Profiles: `aarch64-apple-darwin`, `aarch64-unknown-linux-gnu`, `aarch64-unknown-linux-gnu-no-libc`, `wasm32-unknown-unknown`, `x86_64-unknown-linux-gnu`, `x86_64-unknown-linux-gnu-no-libc`.

Validated, byte-preserving HTTP/1.x scalar values, fields, and message heads.

## When to use

Use these values to share HTTP semantics between parsers, clients, and servers. Parsing and
validation borrow caller storage; use the owned head copies when values must outlive that input.

## Details

Methods remain open case-sensitive tokens, fields retain exact octets, and statuses retain every
code from 100 through 599. This module applies no framing, transport, or connection policy.

## Gotchas

`Header.make` receives semantic field values after wire OWS has been removed. It rejects CR,
LF, NUL, DEL, other forbidden controls, and leading or trailing OWS.

## Examples

### Preserve protocol spelling

```silk
import silk.http { Method, Version }

pub fn main() -> i32 {
  let version = Version.Http11
  let method = Method.patch()
  if Version.format(&version) != "HTTP/1.1" { return 1 }
  if Method.format(&method) != "PATCH" { return 2 }
  return 42
}
```

Import as `Version` with `import silk.http { Version }`.

Public declarations: 13.

<a id="declaration-73696c6b2f687474703a3a56616c7565436f6d706f6e656e74"></a>

## `ValueComponent`

```silk
pub enum ValueComponent
```

The component of an HTTP value that failed validation.

<a id="declaration-73696c6b2f687474703a3a56616c7565436f6d706f6e656e743a3a6d656d6265723a30"></a>

### `Version`

```silk
Version = 0
```

An HTTP version token.

<a id="declaration-73696c6b2f687474703a3a56616c7565436f6d706f6e656e743a3a6d656d6265723a31"></a>

### `Method`

```silk
Method = 1
```

A request method token.

<a id="declaration-73696c6b2f687474703a3a56616c7565436f6d706f6e656e743a3a6d656d6265723a32"></a>

### `Status`

```silk
Status = 2
```

A numeric response status.

<a id="declaration-73696c6b2f687474703a3a56616c7565436f6d706f6e656e743a3a6d656d6265723a33"></a>

### `HeaderName`

```silk
HeaderName = 3
```

A field name.

<a id="declaration-73696c6b2f687474703a3a56616c7565436f6d706f6e656e743a3a6d656d6265723a34"></a>

### `HeaderValue`

```silk
HeaderValue = 4
```

A field value.

<a id="declaration-73696c6b2f687474703a3a56616c7565436f6d706f6e656e743a3a6d656d6265723a35"></a>

### `ReasonPhrase`

```silk
ReasonPhrase = 5
```

An optional response reason phrase.

<a id="declaration-73696c6b2f687474703a3a56616c7565436f6d706f6e656e743a3a6d656d6265723a36"></a>

### `Headers`

```silk
Headers = 6
```

An ordered field collection.

<a id="declaration-73696c6b2f687474703a3a56616c7565436f6d706f6e656e743a3a6d656d6265723a37"></a>

### `Target`

```silk
Target = 7
```

A request target.

<a id="declaration-73696c6b2f687474703a3a56616c7565436f6d706f6e656e743a3a6d656d6265723a38"></a>

### `Authority`

```silk
Authority = 8
```

An HTTP authority.

<a id="declaration-73696c6b2f687474703a3a56616c7565436f6d706f6e656e743a3a6d656d6265723a39"></a>

### `Host`

```silk
Host = 9
```

A Host field.

<a id="declaration-73696c6b2f687474703a3a56616c7565436f6d706f6e656e743a3a6d656d6265723a3130"></a>

### `Metadata`

```silk
Metadata = 10
```

A structured metadata field value.

<a id="declaration-73696c6b2f687474703a3a4c696d69744b696e64"></a>

## `LimitKind`

```silk
pub enum LimitKind
```

The finite limit whose allowed count was exceeded.

<a id="declaration-73696c6b2f687474703a3a4c696d69744b696e643a3a6d656d6265723a30"></a>

### `MethodBytes`

```silk
MethodBytes = 0
```

Maximum method bytes.

<a id="declaration-73696c6b2f687474703a3a4c696d69744b696e643a3a6d656d6265723a31"></a>

### `TargetBytes`

```silk
TargetBytes = 1
```

Maximum request-target bytes.

<a id="declaration-73696c6b2f687474703a3a4c696d69744b696e643a3a6d656d6265723a32"></a>

### `NameBytes`

```silk
NameBytes = 2
```

Maximum field-name bytes.

<a id="declaration-73696c6b2f687474703a3a4c696d69744b696e643a3a6d656d6265723a33"></a>

### `ValueBytes`

```silk
ValueBytes = 3
```

Maximum field-value or reason bytes.

<a id="declaration-73696c6b2f687474703a3a4c696d69744b696e643a3a6d656d6265723a34"></a>

### `Fields`

```silk
Fields = 4
```

Maximum field count.

<a id="declaration-73696c6b2f687474703a3a4c696d69744b696e643a3a6d656d6265723a35"></a>

### `FieldBytes`

```silk
FieldBytes = 5
```

Maximum aggregate name-plus-value bytes.

<a id="declaration-73696c6b2f687474703a3a4c696d69744b696e643a3a6d656d6265723a36"></a>

### `OwnedBytes`

```silk
OwnedBytes = 6
```

Maximum owned payload and index bytes.

<a id="declaration-73696c6b2f687474703a3a4c696d69744b696e643a3a6d656d6265723a37"></a>

### `UniqueField`

```silk
UniqueField = 7
```

At most one matching field was required.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e"></a>

## `ValueReason`

```silk
pub union ValueReason
```

A closed semantic HTTP value failure reason.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a30"></a>

### `InvalidToken`

```silk
ValueReason.InvalidToken: ValueReason
```

A token is empty or contains a byte outside `tchar`.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a31"></a>

### `InvalidValueByte`

```silk
ValueReason.InvalidValueByte: ValueReason
```

A value contains a forbidden byte.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a32"></a>

### `UnsupportedVersion`

```silk
ValueReason.UnsupportedVersion: ValueReason
```

A version is not HTTP/1.0 or HTTP/1.1.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a33"></a>

### `InvalidStatus`

```silk
ValueReason.InvalidStatus: ValueReason
```

A response code is outside 100 through 599.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a34"></a>

### `InvalidTarget`

```silk
ValueReason.InvalidTarget: ValueReason
```

A request target has invalid syntax.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a35"></a>

### `InvalidAuthority`

```silk
ValueReason.InvalidAuthority: ValueReason
```

An HTTP authority has invalid syntax.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a36"></a>

### `InvalidPort`

```silk
ValueReason.InvalidPort: ValueReason
```

A port is absent, empty, nonnumeric, or outside its accepted range.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a37"></a>

### `MethodTargetMismatch`

```silk
ValueReason.MethodTargetMismatch: ValueReason
```

The request method cannot use the supplied target form.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a38"></a>

### `MissingHost`

```silk
ValueReason.MissingHost: ValueReason
```

HTTP/1.1 has no Host field.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a39"></a>

### `DuplicateHost`

```silk
ValueReason.DuplicateHost: ValueReason
```

More than one Host field is present.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a3130"></a>

### `LimitExceeded`

```silk
ValueReason.LimitExceeded { limit: LimitKind, allowed: usize, attempted: usize }: ValueReason
```

One explicit finite limit was exceeded.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a31303a3a6669656c643a30"></a>

#### Field `limit`

```silk
pub limit: LimitKind
```

The rejected limit.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a31303a3a6669656c643a31"></a>

#### Field `allowed`

```silk
pub allowed: usize
```

The configured maximum.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a31303a3a6669656c643a32"></a>

#### Field `attempted`

```silk
pub attempted: usize
```

The attempted count.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a3131"></a>

### `SizeOverflow`

```silk
ValueReason.SizeOverflow: ValueReason
```

Checked size arithmetic overflowed `usize`.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a3132"></a>

### `OutputTooSmall`

```silk
ValueReason.OutputTooSmall { required: usize, available: usize }: ValueReason
```

Caller-owned output cannot hold the complete serialization.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a31323a3a6669656c643a30"></a>

#### Field `required`

```silk
pub required: usize
```

Complete required output bytes.

<a id="declaration-73696c6b2f687474703a3a56616c7565526561736f6e3a3a76617269616e743a31323a3a6669656c643a31"></a>

#### Field `available`

```silk
pub available: usize
```

Available caller output bytes.

<a id="declaration-73696c6b2f687474703a3a696d706c656d656e746174696f6e3a30"></a>

## Implementation `Copy for ValueReason`

```silk
impl Copy for ValueReason
```

<a id="declaration-73696c6b2f687474703a3a56616c75654572726f72"></a>

## `ValueError`

```silk
pub struct ValueError
```

A semantic HTTP value failure with source location information.

<a id="declaration-73696c6b2f687474703a3a56616c75654572726f723a3a6669656c643a30"></a>

### Field `reason`

```silk
pub reason: ValueReason
```

The recovery-oriented failure category.

<a id="declaration-73696c6b2f687474703a3a56616c75654572726f723a3a6669656c643a31"></a>

### Field `component`

```silk
pub component: ValueComponent
```

The value component being validated.

<a id="declaration-73696c6b2f687474703a3a56616c75654572726f723a3a6669656c643a32"></a>

### Field `fieldIndex`

```silk
pub fieldIndex: silk/option.Option<usize>
```

The source field ordinal when the error came from a collection.

<a id="declaration-73696c6b2f687474703a3a56616c75654572726f723a3a6669656c643a33"></a>

### Field `offset`

```silk
pub offset: usize
```

The zero-based component byte offset.

<a id="declaration-73696c6b2f687474703a3a56657273696f6e"></a>

## `Version`

```silk
pub enum Version
```

One supported HTTP protocol version.

<a id="declaration-73696c6b2f687474703a3a56657273696f6e3a3a6d656d6265723a30"></a>

### `Http10`

```silk
Http10 = 0
```

HTTP/1.0.

<a id="declaration-73696c6b2f687474703a3a56657273696f6e3a3a6d656d6265723a31"></a>

### `Http11`

```silk
Http11 = 1
```

HTTP/1.1.

<a id="declaration-73696c6b2f687474703a3a56657273696f6e2e7061727365"></a>

### Associated function `Version.parse`

```silk
pub fn parse<'life0>(text: string<'life0>) -> silk/result.Result<silk/http.Version, silk/http.ValueError>
```

Parses exactly `HTTP/1.0` or `HTTP/1.1`.

<a id="declaration-73696c6b2f687474703a3a56657273696f6e2e666f726d6174"></a>

### Method `Version.format`

```silk
pub fn format<'life0>(self: &'life0 Version) -> string<'static>
```

Formats one supported version as its exact protocol token.

<a id="declaration-73696c6b2f687474703a3a4d6574686f64"></a>

## `Method`

```silk
pub struct Method<'text>
```

A borrowed open HTTP method token with preserved spelling.

<a id="declaration-73696c6b2f687474703a3a4d6574686f642e7061727365"></a>

### Associated function `Method.parse`

```silk
pub fn parse<'text>(text: string<'text>, maxBytes: usize) -> silk/result.Result<silk/http.Method<'text>, silk/http.ValueError>
```

Validates a nonempty `tchar` token within `maxBytes` and borrows it unchanged.

<a id="declaration-73696c6b2f687474703a3a4d6574686f642e666f726d6174"></a>

### Method `Method.format`

```silk
pub fn format<'text, 'life1>(self: &'life1 Method<'text>) -> string<'text>
```

Borrows the original method spelling.

<a id="declaration-73696c6b2f687474703a3a4d6574686f642e657175616c73"></a>

### Method `Method.equals`

```silk
pub fn equals<'text, 'life1, 'life2, 'life3>(self: &'life1 Method<'text>, other: &'life2 silk/http.Method<'life3>) -> bool
```

Compares method spelling case-sensitively.

<a id="declaration-73696c6b2f687474703a3a4d6574686f642e676574"></a>

### Associated function `Method.get`

```silk
pub fn get() -> silk/http.Method<'static>
```

Returns the standard GET method value.

<a id="declaration-73696c6b2f687474703a3a4d6574686f642e68656164"></a>

### Associated function `Method.head`

```silk
pub fn head() -> silk/http.Method<'static>
```

Returns the standard HEAD method value.

<a id="declaration-73696c6b2f687474703a3a4d6574686f642e706f7374"></a>

### Associated function `Method.post`

```silk
pub fn post() -> silk/http.Method<'static>
```

Returns the standard POST method value.

<a id="declaration-73696c6b2f687474703a3a4d6574686f642e707574"></a>

### Associated function `Method.put`

```silk
pub fn put() -> silk/http.Method<'static>
```

Returns the standard PUT method value.

<a id="declaration-73696c6b2f687474703a3a4d6574686f642e64656c657465"></a>

### Associated function `Method.delete`

```silk
pub fn delete() -> silk/http.Method<'static>
```

Returns the standard DELETE method value.

<a id="declaration-73696c6b2f687474703a3a4d6574686f642e636f6e6e656374"></a>

### Associated function `Method.connect`

```silk
pub fn connect() -> silk/http.Method<'static>
```

Returns the standard CONNECT method value.

<a id="declaration-73696c6b2f687474703a3a4d6574686f642e6f7074696f6e73"></a>

### Associated function `Method.options`

```silk
pub fn options() -> silk/http.Method<'static>
```

Returns the standard OPTIONS method value.

<a id="declaration-73696c6b2f687474703a3a4d6574686f642e7472616365"></a>

### Associated function `Method.trace`

```silk
pub fn trace() -> silk/http.Method<'static>
```

Returns the standard TRACE method value.

<a id="declaration-73696c6b2f687474703a3a4d6574686f642e7061746368"></a>

### Associated function `Method.patch`

```silk
pub fn patch() -> silk/http.Method<'static>
```

Returns the standard PATCH method value.

<a id="declaration-73696c6b2f687474703a3a696d706c656d656e746174696f6e3a31"></a>

## Implementation `Copy for silk/http.Method<'text>`

```silk
impl Copy for silk/http.Method<'text>
```

<a id="declaration-73696c6b2f687474703a3a537461747573436c617373"></a>

## `StatusClass`

```silk
pub enum StatusClass
```

The protocol class of a response status.

<a id="declaration-73696c6b2f687474703a3a537461747573436c6173733a3a6d656d6265723a30"></a>

### `Informational`

```silk
Informational = 0
```

1xx informational.

<a id="declaration-73696c6b2f687474703a3a537461747573436c6173733a3a6d656d6265723a31"></a>

### `Successful`

```silk
Successful = 1
```

2xx successful.

<a id="declaration-73696c6b2f687474703a3a537461747573436c6173733a3a6d656d6265723a32"></a>

### `Redirection`

```silk
Redirection = 2
```

3xx redirection.

<a id="declaration-73696c6b2f687474703a3a537461747573436c6173733a3a6d656d6265723a33"></a>

### `ClientError`

```silk
ClientError = 3
```

4xx client error.

<a id="declaration-73696c6b2f687474703a3a537461747573436c6173733a3a6d656d6265723a34"></a>

### `ServerError`

```silk
ServerError = 4
```

5xx server error.

<a id="declaration-73696c6b2f687474703a3a537461747573"></a>

## `Status`

```silk
pub struct Status
```

A response status retaining registered and unregistered valid codes.

<a id="declaration-73696c6b2f687474703a3a5374617475732e66726f6d436f6465"></a>

### Associated function `Status.fromCode`

```silk
pub fn fromCode(code: u16) -> silk/result.Result<silk/http.Status, silk/http.ValueError>
```

Constructs a status from any code in the inclusive 100 through 599 range.

<a id="declaration-73696c6b2f687474703a3a5374617475732e636f6465"></a>

### Method `Status.code`

```silk
pub fn code<'life0>(self: &'life0 Status) -> u16
```

Returns the preserved numeric status code.

<a id="declaration-73696c6b2f687474703a3a5374617475732e636c617373"></a>

### Method `Status.class`

```silk
pub fn class<'life0>(self: &'life0 Status) -> StatusClass
```

Returns the status class derived from the hundreds digit.

<a id="declaration-73696c6b2f687474703a3a696d706c656d656e746174696f6e3a32"></a>

## Implementation `Copy for Status`

```silk
impl Copy for Status
```

<a id="declaration-73696c6b2f687474703a3a486561646572"></a>

## `Header`

```silk
pub struct Header<'value>
```

A validated header field borrowing its exact name spelling and raw value octets.

<a id="declaration-73696c6b2f687474703a3a4865616465722e6d616b65"></a>

### Associated function `Header.make`

```silk
pub fn make<'value>(name: string<'value>, value: &'value [u8], limits: Limits) -> silk/result.Result<silk/http.Header<'value>, silk/http.ValueError>
```

Validates one semantic field after wire OWS removal and borrows it unchanged.

<a id="declaration-73696c6b2f687474703a3a4865616465722e6e616d65"></a>

### Method `Header.name`

```silk
pub fn name<'value, 'life1>(self: &'life1 Header<'value>) -> string<'value>
```

Borrows the original field-name spelling.

<a id="declaration-73696c6b2f687474703a3a4865616465722e76616c7565"></a>

### Method `Header.value`

```silk
pub fn value<'value, 'life1>(self: &'life1 Header<'value>) -> &'value [u8]
```

Borrows the exact validated field-value octets.

<a id="declaration-73696c6b2f687474703a3a696d706c656d656e746174696f6e3a33"></a>

## Implementation `Copy for silk/http.Header<'value>`

```silk
impl Copy for silk/http.Header<'value>
```

<a id="declaration-73696c6b2f687474703a3a5265717565737448656164"></a>

## `RequestHead`

```silk
pub struct RequestHead<'value>
```

A validated request head without body, framing, connection, or exchange state.

<a id="declaration-73696c6b2f687474703a3a52657175657374486561642e6d616b65"></a>

### Associated function `RequestHead.make`

```silk
pub fn make<'value>(version: Version, method: silk/http.Method<'value>, target: silk/http_target.RequestTarget<'value>, headers: silk/http_headers.Headers<'value>) -> silk/result.Result<silk/http.RequestHead<'value>, silk/http.ValueError>
```

Combines validated request components and enforces the HTTP/1.1 Host requirement.

<a id="declaration-73696c6b2f687474703a3a52657175657374486561642e76657273696f6e"></a>

### Method `RequestHead.version`

```silk
pub fn version<'value, 'life1>(self: &'life1 RequestHead<'value>) -> Version
```

Returns the request version.

<a id="declaration-73696c6b2f687474703a3a52657175657374486561642e6d6574686f64"></a>

### Method `RequestHead.method`

```silk
pub fn method<'value, 'life1>(self: &'life1 RequestHead<'value>) -> silk/http.Method<'value>
```

Returns the borrowed request method.

<a id="declaration-73696c6b2f687474703a3a52657175657374486561642e746172676574"></a>

### Method `RequestHead.target`

```silk
pub fn target<'value, 'life1>(self: &'life1 RequestHead<'value>) -> silk/http_target.RequestTarget<'value>
```

Returns the borrowed request target.

<a id="declaration-73696c6b2f687474703a3a52657175657374486561642e68656164657273"></a>

### Method `RequestHead.headers`

```silk
pub fn headers<'value, 'life1>(self: &'life1 RequestHead<'value>) -> silk/http_headers.Headers<'value>
```

Returns the borrowed ordered fields.

<a id="declaration-73696c6b2f687474703a3a52657175657374486561642e636f7079"></a>

### Method `RequestHead.copy`

```silk
pub effect<'env> fn copy<'value: 'env, 'life1: 'env, 'env>(self: &'life1 RequestHead<'value>, limits: Limits) -> silk/result.Result<silk/http.OwnedRequestHead, silk/http.ValueError> ! OutOfMemoryError ? &mut Allocator
```

Copies the complete request head into independent bounded storage.

<a id="declaration-73696c6b2f687474703a3a696d706c656d656e746174696f6e3a34"></a>

## Implementation `Copy for silk/http.RequestHead<'value>`

```silk
impl Copy for silk/http.RequestHead<'value>
```

<a id="declaration-73696c6b2f687474703a3a4f776e65645265717565737448656164"></a>

## `OwnedRequestHead`

```silk
pub struct OwnedRequestHead
```

An independently owned request head whose views borrow only this value.

<a id="declaration-73696c6b2f687474703a3a4f776e656452657175657374486561642e76696577"></a>

### Method `OwnedRequestHead.view`

```silk
pub fn view<'value>(self: &'value OwnedRequestHead) -> silk/http.RequestHead<'value>
```

Reconstructs a validated request view tied to this owner without allocation.

<a id="declaration-73696c6b2f687474703a3a526573706f6e736548656164"></a>

## `ResponseHead`

```silk
pub struct ResponseHead<'value>
```

A validated response head without body, framing, connection, or exchange state.

<a id="declaration-73696c6b2f687474703a3a526573706f6e7365486561642e6d616b65"></a>

### Associated function `ResponseHead.make`

```silk
pub fn make<'value>(version: Version, status: Status, reason: silk/option.Option<&'value [u8]>, headers: silk/http_headers.Headers<'value>, limits: Limits) -> silk/result.Result<silk/http.ResponseHead<'value>, silk/http.ValueError>
```

Validates an optional reason phrase and combines response metadata.

<a id="declaration-73696c6b2f687474703a3a526573706f6e7365486561642e76657273696f6e"></a>

### Method `ResponseHead.version`

```silk
pub fn version<'value, 'life1>(self: &'life1 ResponseHead<'value>) -> Version
```

Returns the response version.

<a id="declaration-73696c6b2f687474703a3a526573706f6e7365486561642e737461747573"></a>

### Method `ResponseHead.status`

```silk
pub fn status<'value, 'life1>(self: &'life1 ResponseHead<'value>) -> Status
```

Returns the response status.

<a id="declaration-73696c6b2f687474703a3a526573706f6e7365486561642e726561736f6e"></a>

### Method `ResponseHead.reason`

```silk
pub fn reason<'value, 'life1>(self: &'life1 ResponseHead<'value>) -> silk/option.Option<&'value [u8]>
```

Returns the exact optional reason-phrase bytes.

<a id="declaration-73696c6b2f687474703a3a526573706f6e7365486561642e68656164657273"></a>

### Method `ResponseHead.headers`

```silk
pub fn headers<'value, 'life1>(self: &'life1 ResponseHead<'value>) -> silk/http_headers.Headers<'value>
```

Returns the borrowed ordered fields.

<a id="declaration-73696c6b2f687474703a3a526573706f6e7365486561642e636f7079"></a>

### Method `ResponseHead.copy`

```silk
pub effect<'env> fn copy<'value: 'env, 'life1: 'env, 'env>(self: &'life1 ResponseHead<'value>, limits: Limits) -> silk/result.Result<silk/http.OwnedResponseHead, silk/http.ValueError> ! OutOfMemoryError ? &mut Allocator
```

Copies the complete response head into independent bounded storage.

<a id="declaration-73696c6b2f687474703a3a4f776e6564526573706f6e736548656164"></a>

## `OwnedResponseHead`

```silk
pub struct OwnedResponseHead
```

An independently owned response head whose views borrow only this value.

<a id="declaration-73696c6b2f687474703a3a4f776e6564526573706f6e7365486561642e76696577"></a>

### Method `OwnedResponseHead.view`

```silk
pub fn view<'value>(self: &'value OwnedResponseHead) -> silk/http.ResponseHead<'value>
```

Reconstructs a validated response view tied to this owner without allocation.
