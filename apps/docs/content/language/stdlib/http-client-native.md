# `silk/http_client_native`

<!-- Generated by packages/compiler/scripts/generate-documentation.mjs. Do not edit by hand. -->

Profiles: `aarch64-apple-darwin`, `aarch64-unknown-linux-gnu`, `x86_64-unknown-linux-gnu`.

Native connection acquisition for one HTTP origin and one serial client scope.

## Details

HTTPS authenticates the original origin with explicit trust. Absolute deadlines are unchanged
through resolution, connection, and TLS authentication. Synchronous hostname resolution rejects
an overall deadline before dispatch. Cleanup closes without a flush or TLS shutdown.

Import as `HttpClientNative` with `import silk.http_client_native as HttpClientNative`.

Public declarations: 9.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4e6174697665526f757465"></a>

## `NativeRoute`

```silk
pub struct NativeRoute
```

The immutable transport route retained independently of the HTTP origin.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4e6174697665526f7574652e6973556e6978"></a>

### Method `NativeRoute.isUnix`

```silk
pub fn isUnix<'life0>(self: &'life0 NativeRoute) -> bool
```

Returns whether this route uses an explicit Unix socket path.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4e6174697665526f7574652e70617468"></a>

### Method `NativeRoute.path`

```silk
pub fn path<'a>(self: &'a NativeRoute) -> &'a [u8]
```

Borrows the exact owned Unix path, or an empty slice for direct TCP.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a696d706c656d656e746174696f6e3a30"></a>

## Implementation `Copy for NativeRoute`

```silk
impl Copy for NativeRoute
```

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4e6174697665436c69656e744572726f72"></a>

## `NativeClientError`

```silk
pub union NativeClientError
```

A native HTTP acquisition capability or option admission failure.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4e6174697665436c69656e744572726f723a3a76617269616e743a30"></a>

### `UnsupportedTarget`

```silk
NativeClientError.UnsupportedTarget: NativeClientError
```

The selected target has no native HTTP acquisition implementation.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4e6174697665436c69656e744572726f723a3a76617269616e743a31"></a>

### `InvalidConnectOptions`

```silk
NativeClientError.InvalidConnectOptions: NativeClientError
```

The native polling interval or attempt count is outside its finite accepted range.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4e6174697665436c69656e744572726f723a3a76617269616e743a32"></a>

### `UnsupportedDeadline`

```silk
NativeClientError.UnsupportedDeadline: NativeClientError
```

Synchronous hostname resolution cannot honor the supplied overall deadline.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4e6174697665436c69656e744572726f723a3a76617269616e743a33"></a>

### `AlpnMismatch`

```silk
NativeClientError.AlpnMismatch { error: ConnectionError }: NativeClientError
```

Required HTTP/1.1 application protocol negotiation did not succeed.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4e6174697665436c69656e744572726f723a3a76617269616e743a333a3a6669656c643a30"></a>

#### Field `error`

```silk
pub error: ConnectionError
```

The complete TLS connection failure.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a416c706e506f6c696379"></a>

## `AlpnPolicy`

```silk
pub enum AlpnPolicy
```

The HTTP version policy for TLS application protocol negotiation.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a416c706e506f6c6963793a3a6d656d6265723a30"></a>

### `OptionalHttp11`

```silk
OptionalHttp11 = 0
```

Offers only `http/1.1` and permits an absent selection.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a416c706e506f6c6963793a3a6d656d6265723a31"></a>

### `RequiredHttp11`

```silk
RequiredHttp11 = 1
```

Requires the peer to select `http/1.1`.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a416c706e506f6c6963793a3a6d656d6265723a32"></a>

### `Http10`

```silk
Http10 = 2
```

Sends no ALPN extension for an HTTP/1.0-only peer.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4f7074696f6e73"></a>

## `Options`

```silk
pub struct Options
```

Finite native acquisition and TLS authentication limits.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4f7074696f6e733a3a6669656c643a30"></a>

### Field `maximumAddresses`

```silk
pub maximumAddresses: usize
```

Maximum number of resolved numeric addresses.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4f7074696f6e733a3a6669656c643a31"></a>

### Field `family`

```silk
pub family: FamilySelection
```

Address family admitted by resolution.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4f7074696f6e733a3a6669656c643a32"></a>

### Field `noDelay`

```silk
pub noDelay: bool
```

Whether the socket disables Nagle's algorithm.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4f7074696f6e733a3a6669656c643a33"></a>

### Field `pollIntervalNanoseconds`

```silk
pub pollIntervalNanoseconds: u64
```

Native readiness polling interval in nanoseconds.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4f7074696f6e733a3a6669656c643a34"></a>

### Field `maximumAttempts`

```silk
pub maximumAttempts: usize
```

Maximum native connection attempts.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4f7074696f6e733a3a6669656c643a35"></a>

### Field `deadline`

```silk
pub deadline: silk/option.Option<silk/system_clock.Instant>
```

Absolute overall deadline on the active monotonic timeline.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4f7074696f6e733a3a6669656c643a36"></a>

### Field `handshakeNanoseconds`

```silk
pub handshakeNanoseconds: u64
```

Maximum TLS authentication duration in nanoseconds.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4f7074696f6e733a3a6669656c643a37"></a>

### Field `tls`

```silk
pub tls: ClientLimits
```

TLS protocol storage limits.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4f7074696f6e733a3a6669656c643a38"></a>

### Field `trust`

```silk
pub trust: TrustLoadLimits
```

Explicit trust source load bounds.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4f7074696f6e733a3a6669656c643a39"></a>

### Field `alpn`

```silk
pub alpn: AlpnPolicy
```

HTTP-only application protocol negotiation policy.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4f7074696f6e732e64656661756c7473"></a>

### Associated function `Options.defaults`

```silk
pub fn defaults() -> Options
```

Returns finite defaults with no overall deadline and optional HTTP/1.1 negotiation.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4e61746976655472616e73706f7274"></a>

## `NativeTransport`

```silk
pub struct NativeTransport
```

One affine native HTTP transport that owns its exact socket or TLS connection.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a4e61746976655472616e73706f72742e726f757465"></a>

### Method `NativeTransport.route`

```silk
pub fn route<'life0>(self: &'life0 NativeTransport) -> NativeRoute
```

Returns owned immutable route metadata while leaving the transport exclusively inside its session.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a77697468436f6e6e656374696f6e"></a>

## `withConnection`

```silk
pub effect<'env> fn withConnection<A, E, ?R, H: 'env, 'env>(origin: Origin, options: Options, limits: Limits, handler: H) -> A ! E | ClientError | NativeClientError | ResolverError | NativeSocketError | ConnectionError | IdentityError | TrustSourceError | OutOfMemoryError ? R | &mut Allocator | &mut MonotonicClock | &mut SystemClock | &mut Random | &mut TrustSource where R in Without<R, &HttpTransport>
```

Resolves and acquires one origin, then lends its serial HTTP connection to one handler.

### Details

HTTPS loads trust after socket acquisition under a nonparking release bracket. Host fields
never replace the origin identity. `Http10` disables ALPN; other policies offer only HTTP/1.1.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a77697468556e6978436f6e6e656374696f6e"></a>

## `withUnixConnection`

```silk
pub effect<'env> fn withUnixConnection<A, E, ?R, H: 'env, 'life4: 'env, 'env>(path: &'life4 [u8], origin: Origin, options: Options, limits: Limits, handler: H) -> A ! E | ClientError | NativeClientError | NativeSocketError | ConnectionError | IdentityError | TrustSourceError | OutOfMemoryError ? R | &mut Allocator | &mut MonotonicClock | &mut SystemClock | &mut Random | &mut TrustSource where R in Without<R, &HttpTransport>
```

Connects an explicit Unix socket while retaining a separate HTTP and TLS authority.

### Details

The socket path selects only the transport. HTTPS authenticates `origin` with explicit trust.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a696d706c656d656e746174696f6e3a31"></a>

## Implementation `HttpTransport for NativeTransport`

```silk
impl HttpTransport for NativeTransport
```

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a696d706c656d656e746174696f6e3a313a3a6f7065726174696f6e3a30"></a>

### Operation `readSomeRaw`

```silk
readSomeRaw = NativeTransport.read
```

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a696d706c656d656e746174696f6e3a313a3a6f7065726174696f6e3a31"></a>

### Operation `writeSomeRaw`

```silk
writeSomeRaw = NativeTransport.write
```

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a696d706c656d656e746174696f6e3a313a3a6f7065726174696f6e3a32"></a>

### Operation `flush`

```silk
flush = NativeTransport.flushOutput
```

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a696d706c656d656e746174696f6e3a313a3a6f7065726174696f6e3a33"></a>

### Operation `close`

```silk
close = NativeTransport.closeLease
```

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a707265666c69676874"></a>

## `preflight`

```silk
pub fn preflight<'life0, 'life1>(origin: &'life0 silk/http_origin.Origin, options: &'life1 silk/http_client_native.Options) -> silk/result.Result<(), silk/http_client_native.NativeClientError>
```

Checks native TCP support and rejects a hostname deadline before resolver dispatch.

<a id="declaration-73696c6b2f687474705f636c69656e745f6e61746976653a3a726f757465"></a>

## `route`

```silk
pub fn route<'life0>(connection: &'life0 silk/http_client.Connection<silk/http_client_native.NativeTransport>) -> NativeRoute
```

Returns immutable native route metadata from an owned HTTP session without lending its transport mutably.
