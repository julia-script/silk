# `silk/toml_value`

<!-- Generated by packages/compiler/scripts/generate-documentation.mjs. Do not edit by hand. -->

Profiles: `aarch64-apple-darwin`, `aarch64-unknown-linux-gnu`, `aarch64-unknown-linux-gnu-no-libc`, `wasm32-unknown-unknown`, `x86_64-unknown-linux-gnu`, `x86_64-unknown-linux-gnu-no-libc`.

Owned TOML documents with ordered tables and distinct scalar kinds.

## When to use

Use [`Toml.parse`](#declaration-73696c6b2f746f6d6c5f76616c75653a3a546f6d6c2e7061727365) to validate and own a complete TOML document, then borrow fields from
its root table. Every allocation uses the ambient Allocator and partial trees drop on failure.

## Details

Tables keep key order. Numeric and date/time values keep their validated source text.
String values contain decoded UTF-8 text.

## Gotchas

A borrowed field cannot outlive its document. Use `silk.toml_output` to write the owned tree.

## Examples

### Read a nested string

```silk
import silk.allocator as Allocators {Allocator, OutOfMemoryError}
import silk.effect as Effects {Effect}
import silk.option as Options {Option}
import silk.toml_scanner as Scanners {TomlError}
import silk.toml_value as Values {Toml}

effect fn read() -> i32 ! TomlError | OutOfMemoryError {
  let mut allocator = Allocator.systemAllocatorProvider()
  let document = run Toml.parse(b"[owner]\nname = \"Ada\"\n")
    |> Effect.provideMut<Allocator>(&mut allocator)
  let owner = Toml.field(&document, "owner")
  let name = Toml.fieldAt(move owner, "name")
    |> Toml.asString
  return match move name {
    Option<string>.Some {value} => {
      if value == "Ada" {
        return 0
      }
      return 1
    }
    Option<string>.None => {
      return 2
    }
  }
}

effect fn recover(error: TomlError | OutOfMemoryError) -> i32 {
  return 3
}

pub fn main() -> i32 {
  return run Effect.catchAll(read(), recover)
}
```

Import as `Toml` with `import silk.toml_value { Toml }`.

Public declarations: 5.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a5461626c654b696e64"></a>

## `TableKind`

```silk
pub enum TableKind
```

How a table was first defined. An inline table is closed to later document definitions.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a5461626c654b696e643a3a6d656d6265723a30"></a>

### `Implicit`

```silk
Implicit = 0
```

A table created by a later header path.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a5461626c654b696e643a3a6d656d6265723a31"></a>

### `Dotted`

```silk
Dotted = 1
```

A table created by a dotted key.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a5461626c654b696e643a3a6d656d6265723a32"></a>

### `Explicit`

```silk
Explicit = 2
```

A table defined by a header.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a5461626c654b696e643a3a6d656d6265723a33"></a>

### `Inline`

```silk
Inline = 3
```

A table defined inside braces and closed to later definitions.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a546f6d6c456e747279"></a>

## `TomlEntry`

```silk
pub struct TomlEntry
```

One ordered table member.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a546f6d6c456e7472793a3a6669656c643a30"></a>

### Field `key`

```silk
pub key: String
```

The decoded member key.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a546f6d6c456e7472793a3a6669656c643a31"></a>

### Field `value`

```silk
pub value: Value
```

The owned member value.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a546f6d6c4368696c64"></a>

## `TomlChild`

```silk
pub union TomlChild
```

Recursive storage shared by tables and arrays.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a546f6d6c4368696c643a3a76617269616e743a30"></a>

### `Item`

```silk
TomlChild.Item { value: Value }: TomlChild
```

An array element without a key.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a546f6d6c4368696c643a3a76617269616e743a303a3a6669656c643a30"></a>

#### Field `value`

```silk
pub value: Value
```

The owned element value.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a546f6d6c4368696c643a3a76617269616e743a31"></a>

### `Member`

```silk
TomlChild.Member { entry: TomlEntry }: TomlChild
```

A table member with its key.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a546f6d6c4368696c643a3a76617269616e743a313a3a6669656c643a30"></a>

#### Field `entry`

```silk
pub entry: TomlEntry
```

The owned key and value.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c7565"></a>

## `Value`

```silk
pub union Value
```

An owned TOML value. Numeric and temporal spellings retain their validated source precision.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a30"></a>

### `Integer`

```silk
Value.Integer { text: String }: Value
```

A validated integer in its source spelling.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a303a3a6669656c643a30"></a>

#### Field `text`

```silk
pub text: String
```

The validated integer text.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a31"></a>

### `Float`

```silk
Value.Float { text: String }: Value
```

A validated float in its source spelling.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a313a3a6669656c643a30"></a>

#### Field `text`

```silk
pub text: String
```

The validated float text.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a32"></a>

### `Boolean`

```silk
Value.Boolean { value: bool }: Value
```

A TOML boolean.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a323a3a6669656c643a30"></a>

#### Field `value`

```silk
pub value: bool
```

The boolean value.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a33"></a>

### `OffsetDateTime`

```silk
Value.OffsetDateTime { text: String }: Value
```

A date and time with an explicit offset and retained precision.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a333a3a6669656c643a30"></a>

#### Field `text`

```silk
pub text: String
```

The validated source text.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a34"></a>

### `LocalDateTime`

```silk
Value.LocalDateTime { text: String }: Value
```

A date and time without an offset and with retained precision.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a343a3a6669656c643a30"></a>

#### Field `text`

```silk
pub text: String
```

The validated source text.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a35"></a>

### `LocalDate`

```silk
Value.LocalDate { text: String }: Value
```

A calendar date without a time.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a353a3a6669656c643a30"></a>

#### Field `text`

```silk
pub text: String
```

The validated source text.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a36"></a>

### `LocalTime`

```silk
Value.LocalTime { text: String }: Value
```

A clock time without a date and with retained precision.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a363a3a6669656c643a30"></a>

#### Field `text`

```silk
pub text: String
```

The validated source text.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a37"></a>

### `String`

```silk
Value.String { text: String }: Value
```

Decoded UTF-8 text.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a373a3a6669656c643a30"></a>

#### Field `text`

```silk
pub text: String
```

The decoded text.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a38"></a>

### `Array`

```silk
Value.Array { values: silk/box.Box<silk/vector.Vector<silk/toml_value.TomlChild>>, tables: bool }: Value
```

Ordered values or an array of tables.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a383a3a6669656c643a30"></a>

#### Field `values`

```silk
pub values: silk/box.Box<silk/vector.Vector<silk/toml_value.TomlChild>>
```

The owned elements in source order.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a383a3a6669656c643a31"></a>

#### Field `tables`

```silk
pub tables: bool
```

Whether the array came from `[[table]]` headers.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a39"></a>

### `Table`

```silk
Value.Table { entries: silk/box.Box<silk/vector.Vector<silk/toml_value.TomlChild>>, kind: TableKind }: Value
```

Ordered key/value members.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a393a3a6669656c643a30"></a>

#### Field `entries`

```silk
pub entries: silk/box.Box<silk/vector.Vector<silk/toml_value.TomlChild>>
```

The owned members in source order.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a56616c75653a3a76617269616e743a393a3a6669656c643a31"></a>

#### Field `kind`

```silk
pub kind: TableKind
```

How the table was defined.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a546f6d6c"></a>

## `Toml`

```silk
pub struct Toml
```

Operations on complete owned TOML documents.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a546f6d6c2e7061727365"></a>

### Associated function `Toml.parse`

```silk
pub effect<'life0> fn parse<'life0>(bytes: &'life0 [u8]) -> Value ! TomlError | OutOfMemoryError ? &mut Allocator
```

Parses one complete UTF-8 document into an owned root table.

#### Details

All allocations use the ambient allocator. Syntax errors include a reason and byte offset.
If parsing fails, the partial tree is released.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a546f6d6c2e6669656c64"></a>

### Associated function `Toml.field`

```silk
pub fn field<'a, 'life1>(self: &'a silk/toml_value.Value, name: string<'life1>) -> silk/option.Option<&'a silk/toml_value.Value>
```

Borrows a table member by decoded key, or returns `None` for absence or another value kind.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a546f6d6c2e6669656c644174"></a>

### Associated function `Toml.fieldAt`

```silk
pub fn fieldAt<'a, 'life1>(self: silk/option.Option<&'a silk/toml_value.Value>, name: string<'life1>) -> silk/option.Option<&'a silk/toml_value.Value>
```

Borrows a nested table member through an optional value, or returns `None`.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a546f6d6c2e6974656d"></a>

### Associated function `Toml.item`

```silk
pub fn item<'a>(self: &'a silk/toml_value.Value, index: usize) -> silk/option.Option<&'a silk/toml_value.Value>
```

Borrows an array element, or returns None for an absent index or another value kind.

<a id="declaration-73696c6b2f746f6d6c5f76616c75653a3a546f6d6c2e6173537472696e67"></a>

### Associated function `Toml.asString`

```silk
pub fn asString<'a>(self: silk/option.Option<&'a silk/toml_value.Value>) -> silk/option.Option<string<'a>>
```

Borrows decoded UTF-8 string text, or returns `None` for another value kind.
