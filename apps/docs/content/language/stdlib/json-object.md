# `silk/json_object`

<!-- Generated by packages/compiler/scripts/generate-documentation.mjs. Do not edit by hand. -->

Profiles: `aarch64-apple-darwin`, `aarch64-unknown-linux-gnu`, `aarch64-unknown-linux-gnu-no-libc`, `wasm32-unknown-unknown`, `x86_64-unknown-linux-gnu`, `x86_64-unknown-linux-gnu-no-libc`.

Effectful JSON object construction through the ambient Writer.

## When to use

Use [`JsonObject`](#declaration-73696c6b2f6a736f6e5f6f626a6563743a3a4a736f6e4f626a656374) to write named values with a [`Serialize`](./json-codec.md#declaration-73696c6b2f6a736f6e5f636f6465633a3a53657269616c697a65) implementation.
Each operation keeps the object state in an Effect for the next operation.

## Details

The object owns its comma and depth state. It does not own or borrow the Writer.
A failed write can leave an accepted prefix in the Writer.

Import as `JsonObject` with `import silk.json_object { JsonObject }`.

Public declarations: 1.

<a id="declaration-73696c6b2f6a736f6e5f6f626a6563743a3a4a736f6e4f626a656374"></a>

## `JsonObject`

```silk
pub struct JsonObject
```

Owned formatting state for one open JSON object.

<a id="declaration-73696c6b2f6a736f6e5f6f626a6563743a3a4a736f6e4f626a6563742e626567696e"></a>

### Associated function `JsonObject.begin`

```silk
pub effect<'life0> fn begin<'life0>(options: &'life0 silk/json_output.JsonOptions) -> JsonObject ! JsonError | WriterError ? &mut Writer
```

Opens an object after checking its depth limit.

<a id="declaration-73696c6b2f6a736f6e5f6f626a6563743a3a4a736f6e4f626a6563742e6669656c64"></a>

### Associated function `JsonObject.field`

```silk
pub effect<'env1> fn field<'env: 'env1, T: 'env1, E, ?R, 'env1>(self: once Effect<'env; silk/json_object.JsonObject ! E ? R>, key: string<'env>, value: &'env T) -> JsonObject ! E | JsonError | WriterError ? R | &mut Writer
```

Writes one named value and returns the open object state.

#### Details

The incoming Effect runs once. If it fails, this operation writes nothing.

<a id="declaration-73696c6b2f6a736f6e5f6f626a6563743a3a4a736f6e4f626a6563742e6669656c644966536f6d65"></a>

### Associated function `JsonObject.fieldIfSome`

```silk
pub effect<'env1> fn fieldIfSome<'env: 'env1, T: 'env1, E, ?R, 'env1>(self: once Effect<'env; silk/json_object.JsonObject ! E ? R>, key: string<'env>, value: &'env silk/option.Option<T>) -> JsonObject ! E | JsonError | WriterError ? R | &mut Writer
```

Writes a present named value or leaves an absent member out of the object.

#### Details

The incoming Effect runs once. `None` does not write a key or a separator.

<a id="declaration-73696c6b2f6a736f6e5f6f626a6563743a3a4a736f6e4f626a6563742e656e64"></a>

### Associated function `JsonObject.end`

```silk
pub effect<'env> fn end<'env, E, ?R>(self: once Effect<'env; silk/json_object.JsonObject ! E ? R>) -> () ! E | JsonError | WriterError ? R | &mut Writer
```

Closes an object and returns the completed write.

#### Details

The incoming Effect runs once. If it fails, the closing brace is not written.
