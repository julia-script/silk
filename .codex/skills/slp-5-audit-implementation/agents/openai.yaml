interface:
  display_name: "SLP 5: Audit implementation"
  short_description: "Step 5 — check code against SLP and OpenSpec before archive"
  default_prompt: "Use $slp-5-audit-implementation to audit the completed change against its SLP and OpenSpec contract."
policy:
  allow_implicit_invocation: false
