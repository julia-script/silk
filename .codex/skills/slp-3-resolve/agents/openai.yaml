interface:
  display_name: "SLP 3: Resolve"
  short_description: "Step 3 — author decides forks and records outcome"
  default_prompt: "Use $slp-3-resolve to work through the review ledger and record the outcome."
policy:
  allow_implicit_invocation: false
