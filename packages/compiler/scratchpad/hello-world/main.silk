import greeting { greet }
import silk.effect { Effect }
import silk.writer { WriterError }

effect fn writeFailed(error: WriterError) -> i32 {
  return 1
}

pub fn main() -> i32 {
  return run Effect.catchAll(greet(), writeFailed)
}
