struct Schema {}

interface Decoder<S> {
  effect fn decode(self: &S, encoded: &i32) -> bool
}

effect fn decode(self: &Schema, encoded: &mut i32) -> bool {
  return true
}

impl Decoder<Schema> for Schema {
  decode: Schema.decode
}
