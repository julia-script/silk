pub struct DecodeError {}
pub service Clock {}

pub interface Decoder<Arguments, A, E, ?R> {
  effect fn decode(self: &Self, encoded: Arguments) -> A ! E ? R
}

fn generic<Arguments, A, E, ?R, T: Decoder<Arguments, A, E ? R>>(value: T) -> T {
  return move value
}
