fn sum_array() i32 {
    const arr :[4]i32 = [4]i32 {1, 2, 3, 4}
    var i: i32 = 0;
    var sum: i32 = 0;
    while (i < arr.len:as(i32)) {
        sum = sum + arr[i];
        i = i + 1;
    }
    return sum;
}

fn main() void {
    const sum = comp sum_array();
}
