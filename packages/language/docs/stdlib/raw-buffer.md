# `silk/raw_buffer`

<!-- Generated by packages/compiler/scripts/generate-documentation.mjs. Do not edit by hand. -->

Low-level typed views over owned allocations for implementing collections and storage actors.

## When to use

Prefer `silk.vector.Vector`, `silk.bytes.Bytes`, or `silk.box.Box` in application code. Use
these operations only when building a container that explicitly tracks capacity and which
slots are initialized.

## Details

[`copy`](#declaration-73696c6b2f7261772d6275666665723a3a636f7079) supports overlapping ranges and behaves as though the moved elements passed through
temporary storage. [`fill`](#declaration-73696c6b2f7261772d6275666665723a3a66696c6c) writes raw bytes and therefore operates only on `RawBuffer<u8>`.

## Gotchas

These wrappers are unchecked. The caller must prove that an allocation fits the recorded
element count, every selected range is in bounds, and every read or view covers initialized
elements. Releasing a raw buffer releases its allocation but does not discover and drop values
still stored in it; a container must clear initialized `Slot` values before release.

Import as `RawBuffer` with `import silk.raw-buffer`.

Public declarations: 8.

<a id="declaration-73696c6b2f7261772d6275666665723a3a66726f6d"></a>

## `from`

```silk
pub fn from<T>(allocation: Allocation, count: usize) -> silk/core.RawBuffer<T>
```

Adopts an allocation as typed raw storage. The caller proves the count fits the allocation.

<a id="declaration-73696c6b2f7261772d6275666665723a3a736c6f74"></a>

## `slot`

```silk
pub fn slot<T>(buffer: &mut silk/core.RawBuffer<T>, index: usize) -> silk/core.Slot<T>
```

Selects one unchecked storage slot. The caller proves the index is in bounds.

<a id="declaration-73696c6b2f7261772d6275666665723a3a636f756e74"></a>

## `count`

```silk
pub fn count<T>(buffer: &silk/core.RawBuffer<T>) -> usize
```

Returns the element capacity recorded by a raw buffer.

<a id="declaration-73696c6b2f7261772d6275666665723a3a72656164"></a>

## `read`

```silk
pub fn read<T>(buffer: &silk/core.RawBuffer<T>, index: usize) -> T
```

Reads an unchecked initialized element. The caller proves initialization and bounds.

<a id="declaration-73696c6b2f7261772d6275666665723a3a636f7079"></a>

## `copy`

```silk
pub fn copy<T>(destination: &mut silk/core.RawBuffer<T>, destinationOffset: usize, source: &[T], length: usize) -> ()
```

Moves a caller-proven initialized range into selected storage in one bulk transfer.

### Details

Source and destination may overlap; the result is as if the elements travelled through an
intermediate buffer.

<a id="declaration-73696c6b2f7261772d6275666665723a3a66696c6c"></a>

## `fill`

```silk
pub fn fill(buffer: &mut silk/core.RawBuffer<u8>, offset: usize, length: usize, value: u8) -> ()
```

Sets a caller-proven byte range of raw storage to one repeated byte value.

<a id="declaration-73696c6b2f7261772d6275666665723a3a76696577"></a>

## `view`

```silk
pub fn view<T>(buffer: &silk/core.RawBuffer<T>, offset: usize, length: usize) -> &[T]
```

Forms a shared view over caller-proven initialized elements in the selected range.

<a id="declaration-73696c6b2f7261772d6275666665723a3a766965774d7574"></a>

## `viewMut`

```silk
pub fn viewMut<T>(buffer: &mut silk/core.RawBuffer<T>, offset: usize, length: usize) -> &mut [T]
```

Forms an exclusive view over caller-proven initialized elements in the selected range.
