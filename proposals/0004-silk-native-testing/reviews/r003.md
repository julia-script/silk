# Review r003: SLP-0004 revision 33

Proposal: `proposals/0004-silk-native-testing/proposal.md`
Proposal revision: 33
Proposal digest: `bfa8eed4222bf630c40803d75f5eee2a02bc03eb96a779cc0c95cd0049a724c4`
Date: 2026-08-23
Round: 3 of 3
Result: Cap

## Contract

The original thesis, claims C1–C8, and out-of-scope list are frozen in [r001](r001.md); the Round 1
delta is in [r002](r002.md). Revision 33 replaces the failed service-witness repair with these
contract details:

- **C1:** tests are private, closed zero-parameter Effect functions returning unit. Project test
  roots must resolve inside the existing package source root. The selected runner is a separate
  executable root composed with, but excluded from, inventory roots.
- **C2:** sealed primitives expose inventory, metadata, and closed invocation only. They select no
  Reporter identity. Uniform compiler-generated per-test adapters may erase failure rows into a
  test-compilation-local ordinal dispatch without exposing general function pointers.
- **C3:** Test.run returns Passed or Failed with an owned logical path; no Reporter provision or
  fresh Effect-scope promise participates in invocation.
- **C4:** Test.assert and equalBytes are silent ordinary-source typed failures. Ordinary runner
  source consumes each Outcome into a PassedCase or FailedCase Event and sends it to replaceable
  Reporter service policy.
- **C5:** a closed ordinary zero-parameter runner constructs and provides OsHostInput, Allocator,
  and output services. The command only seeds the existing low-level host adapter.
- **C6:** functions are exposed as a borrowed slice of Copy handles; the standard runner uses
  while/index iteration, exact byte-substring filters, and status 2 for no-match or runner
  infrastructure failure.

C7, C8, and all original out-of-scope items otherwise remain unchanged.

## Findings

| Id | Lens | Claim | Severity | Evidence | State |
| --- | --- | --- | --- | --- | --- |
| S14 | Scope/coherence | C4 | High | Reporter/Event remains independently subtractable after moving to the runner boundary. | DUPLICATE of S7 — the explicit customizable-reporting goal and absence of compiler privilege were already considered in its revision-33 resolution. |
| P10 | Compiler privilege | C3 | Medium | The complete existing logical path contains runner/wrapper frames that the compact standard-report example omits; presentation filtering is not stated explicitly. | VERIFIED |
| P11 | Compiler privilege | C3 | High | Owned variable-length StackPath still needs allocation, cleanup, or retained host-trace storage. | DUPLICATE of P4; coordinator severity remains Medium because this is an explicit representation falsifier, not evidence that no conforming representation exists. |
| E13 | Examples/model | C4 | High | Event variant fields are private under current Silk rules, so the external CountingReporter cannot destructure them and no public accessors are supplied. | VERIFIED |
| E14 | Examples/model | C6 | High | `fillBytes` does not occur case-sensitively in `seededFillBytesMatchesFirstWord`, whose segment is `FillBytes`; the driving command would return status 2. | VERIFIED |
| E15 | Examples/model | C4 | Medium | The custom-reporter fence is a fragment with top-level executable statements, an unbound function, and no complete failing runner case. | VERIFIED |

## Blockers

- E13 — case Event data is inaccessible to the custom reporter as written.
- E14 — the focused-filter driving command contradicts exact case-sensitive matching.

## Revision response

No revision. The bounded third round found E13 and E14; revision 33 is retained as the best
validated Candidate for author resolution.

## Stop reason

Cap — Round 3 completed with two open verified High blockers.

## Next state

Author resolution required through SLP 3; Candidate status is unchanged.
