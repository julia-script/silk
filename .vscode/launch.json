{
  "version": "0.2.0",
  "configurations": [
      {
          "type": "lldb",
          "request": "launch",
          "name": "Zig debug test",
          "program": "${workspaceFolder}/.test/${fileBasenameNoExtension}-test",
          "args": [],
          "cwd": "${workspaceFolder}",
          "preLaunchTask": "test",
      }
  ]
}