/// One half-open byte range in the lexer source file.
pub struct Span {
  pub start: usize
  pub end: usize
}

impl Copy for Span {}

impl Span {
  pub fn make(start: usize, end: usize) -> Span {
    return Span {start: start, end: end}
  }
}
