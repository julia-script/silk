# Tasks

## 1. Type Catalog

- [x] 1.1 Add `Layout.computeTypes` without instance/reachability input and preserve target-relative declared layouts and constants.
- [x] 1.2 Delete the mixed `Layout.catalog` entry point and migrate pre-reachability callers.

## 2. Runtime Plan

- [x] 2.1 Add `Layout.computeRuntime` over explicit catalog, instance graph, declaration, and opaque-realization inputs.
- [x] 2.2 Complete reached generic layouts and preserve runtime environment, storage, calling, literal, and diagnostic planning.

## 3. Verification

- [x] 3.1 Update focused layout/realization fixtures, operation names, exports, and architecture documentation.
