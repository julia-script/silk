## 1. Lock the failing transitions and internal protocol

- [x] 1.1 Add deterministic red tests for requests created after a terminal analysis failure and after a successful commit with no document view; verify both currently time out and the tests require an immediate terminal outcome.
- [x] 1.2 Add a same-document transition test for `effects → effec → Effect. → valid` with overlapping hover, completion, code-action, and diagnostic requests; verify an intermediate spelling can never become the final diagnostic result.
- [x] 1.3 Define nominal document version, project generation, worker epoch, request id, incident id, source-event, generation-state, query-catalog, and `QueryOutcome` actors; verify exhaustive type tests cover every terminal state and query result mapping without casts or non-null assertions.
- [x] 1.4 Define the versioned host/worker message protocol from the query catalog, including analyze, supersede-analysis, query, cancel-query, shutdown, ready, progress, superseded, commit, failure, result, and stopped messages; verify malformed or mismatched messages are rejected with typed protocol failures.

## 2. Build the project-worker seam

- [x] 2.1 Implement a controlled in-process `ProjectWorker` adapter with virtual commit, failure, blocked-query, blocked-observer, progress, and termination gates; verify scheduler tests can drive every transition without wall-clock sleeps.
- [x] 2.2 Move project analysis, committed `ProjectAnalysis` reuse, inventory, document query dispatch, and inspection realization cache behind one worker-owned actor; verify a worker test answers diagnostics, hover, completion, code actions, navigation, structure, formatting, semantic tokens, and inspection without exposing a compiler snapshot to the host.
- [x] 2.3 Implement the production `node:worker_threads` adapter and worker entrypoint; verify real-worker tests initialize one project, commit a revision, answer a query, and shut down cleanly.
- [x] 2.4 Add epoch/generation gating and cooperative `SupersedeAnalysis` handling to every worker message; verify checkpoint acknowledgement preserves the last committed reuse basis, delayed commit/query messages are discarded, and a supersession race retains only the newest pending generation.
- [x] 2.5 Implement graceful worker shutdown followed by forced `Worker.terminate()` and acknowledged exit; verify a deliberately non-cooperative worker is retired while the calling test process remains responsive.
- [x] 2.6 Implement progress, superseded-work acknowledgement, and retirement leases plus the three-consecutive-failure project circuit breaker with configurable virtual-time policy; verify no-progress expiry consumes the failure budget, unacknowledged supersession forces retirement without consuming it, rapid edits coalesce before replacement, three failures without a commit open the circuit, one newer quiescent generation gets a half-open attempt, and only a successful commit or explicit engine restart fully resets the budget.

## 3. Replace synchronization with the workspace engine

- [x] 3.1 Implement `accept` as a synchronous typed-Result ledger transition with an unsafe capacity-one background signal so open/change/close events update authoritative bytes before returning and retain no per-version FIFO; verify `didChange(N) → immediate request` captures N without scheduler advancement, hundreds of rapid versions leave only the newest desired state, and delayed `v29` after `v30` cannot change ledger, generation, or diagnostics.
- [x] 3.2 Implement coalesced asynchronous workspace discovery with provisional URI identity, atomic project merge/reassociation, and cached project association; verify cold typing discovers only the newest version, simultaneous first opens resolving to one manifest create one project, ordinary edits reuse it, and pending queries/pulls settle across provisional-to-manifest and manifest/source-root reassociation.
- [x] 3.3 Implement explicit `Pending`, `Committed`, `Failed`, `Superseded`, and `Closed` project generation transitions with one active and one latest pending generation; verify every transition exhaustively settles affected waiters and a healthy worker deterministically commits the newest generation after input quiesces.
- [x] 3.4 Implement exact-revision `request` acquisition and method-independent terminal outcomes; verify requests started before and after failure, missing views, supersession, close, and worker retirement all settle without an unbounded Deferred.
- [x] 3.5 Make engine requests interruptible Effects whose acquire/release finalizers own waiter and worker-query cancellation, and bridge LSP tokens only at the server adapter; verify fiber interruption and deadlines remove pending/executing hover or code-action work exactly once and ignore late results.
- [x] 3.6 Commit authoritative worker snapshots and reuse handles before emitting observer events through a non-blocking sliding stream; verify blocked diagnostic/inspection delivery does not block queries or the next incremental analysis.
- [x] 3.7 Route watched-source and manifest invalidations through the source/project ledger while preserving open-buffer precedence; verify dependency-only changes advance the affected project generation and unrelated projects remain idle.
- [x] 3.8 Implement bounded engine shutdown that closes source entries, settles requests, retires workers, and ignores obsolete canceled work; verify shutdown completes under pending discovery, active analysis, failed workers, and canceled requests.

## 4. Make the language server a thin protocol adapter

- [x] 4.1 Replace server-owned project maps, `DocumentUpdates`, direct `ProjectSession` coordination, and global in-flight promise draining with one scoped `WorkspaceEngine`; verify `Server.ts` handlers no longer construct or inspect compiler/project snapshots.
- [x] 4.2 Convert every semantic request handler to the query catalog and propagate its LSP cancellation token and configured deadline; verify hover, completion, signature help, code actions/resolution, navigation, rename, inlay hints, symbols, formatting, semantic tokens, folding, call hierarchy, and inspection map all terminal outcomes correctly.
- [x] 4.3 Advertise and implement standard document pull diagnostics with stable result ids and unchanged reports, and remove ordinary `sendDiagnostics`; verify the exact previous-result id reuses a report while source/dependency generation changes invalidate it.
- [x] 4.4 Send standard diagnostic refresh after dependency/filesystem commits when supported; verify a changed imported module refreshes an unchanged open importer and direct text edits rely on the document pull lifecycle.
- [x] 4.5 Commit inspector invalidation after engine commit through the non-blocking event stream; verify a blocked or failed notification cannot prevent snapshot queries or later commits.
- [x] 4.6 Add real-stdio tests for cold rapid edits, healthy post-quiescence commit/readiness, intermediate diagnostic cancellation, same-document query cancellation, post-terminal requests, dependency refresh, non-cooperative worker replacement, and bounded shutdown; include a diagnostic-bearing commit → analysis defect → current empty/unavailable pull → valid recovery sequence and a two-project case where project B answers while project A is wedged, and verify all cases pass through `dist/bin.js` rather than a server-only callback fake.

## 5. Replace the extension lifecycle with one editor session

- [x] 5.1 Implement a `ServerProcess` actor that solely owns spawn, a detached `StreamInfo` transport, graceful stop, forced termination, and acknowledged exit while `LanguageClient` owns only protocol cleanup; verify normal exit, stop timeout, forced retirement, spawn failure, idempotent disposal, and no process double-termination with real child fixtures.
- [x] 5.2 Implement a stable `EditorSession` that privately owns client/process generations, serialized start/restart/stop, health, a server-authoritative acceptance barrier with built-in text-synchronization events used only as retry signals, immediate diagnostic retirement on edit/close, one generation-and-version-gated `EditorDiagnostics` collection, and stable event subscribers; verify `vscode-languageclient` is the sole `didOpen` sender and no consumer can retain the concrete client.
- [x] 5.3 Bind inspector invalidations through the stable session and remove `inspectorClient`/one-time concrete-client subscription; verify the inspector refreshes after initial activation and after multiple client replacements.
- [x] 5.4 Implement acknowledged restart ordering—retire/dispose/await exit, then spawn/initialize/bind, complete the server-authoritative ledger acceptance barrier, and become ready—and explicit unavailable state on failure; verify one delivery per open document, readiness when the server accepted a document missed by the local observer, timeout when the server never accepts it, and no authoritative overlap with a wedged old process.
- [x] 5.5 Add production-adapter tests that pass through `vscode-languageclient` built-in synchronization, server-authoritative acceptance barrier, standard pull-diagnostic transport/conversion, the extension-owned collection, and generation replacement; verify each open document is sent once, readiness follows server acknowledgement without requiring lossless notification observation, already-applied findings clear immediately on edit, retired/intermediate/older-version responses cannot update the current collection, and close removes the collection without late repopulation, while a controlled real-stdio defect test verifies the current empty result followed by valid recovery.
- [x] 5.6 Add an opt-in Extension Development Host launch test for the original edit/hover/quick-fix/restart sequence and document the launch-script acceptance procedure; verify the test or documented run observes current diagnostics and bounded request completion without window reload.

## 6. Remove superseded paths and verify pressure behavior

- [x] 6.1 Delete `DocumentUpdates`, the old `ProjectSession`, push-diagnostic publication/failure callbacks, exposed `LanguageClientLifecycle.current`, mirrored client globals, and superseded tests; verify repository search finds no remaining imports, aliases, fallbacks, or dual scheduling/diagnostic paths.
- [x] 6.2 Replace the pressure benchmark with end-to-end source acceptance, discovery, real compiler work, overlapping same-document queries, cancellation counts, worker replacement, memory, and final-commit observations; verify it records bounded pending generations and current final diagnostics without machine-specific correctness thresholds.
- [x] 6.3 Run `pnpm typecheck`, `pnpm exec biome check .`, and targeted LSP/extension/worker suites; fix all failures and record any unrelated pre-existing failure explicitly.
- [x] 6.4 Run `pnpm test`, `pnpm check`, and `pnpm release:candidate`; verify the complete repository and package contents pass after removal of the old interfaces.
- [x] 6.5 Run the launch-script reproduction in Cursor/VS Code through `effects → effec → Effect. → valid`, hover, quick fixes, dependency refresh, and server restart; verify diagnostics always describe current text and every loading state settles without a window refresh.
