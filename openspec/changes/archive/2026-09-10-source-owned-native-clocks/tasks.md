## 1. Contract and supplies

- [x] 1.1 Complete proposal, design, deltas and prescriptive reference; verify strict OpenSpec validation.
- [x] 1.2 Pin selected header/tool/prior-art inputs and hand-authored catalog records; compile independent C layout/signature fixtures on all three targets.

## 2. Source implementation and deletion

- [x] 2.1 Implement selected NativeClock declarations and shared read/resolution validation; verify analysis and independent source/C read fixtures.
- [x] 2.2 Migrate monotonic waits and system-clock consumers; verify direct GNU status, Darwin recomputation, checked arithmetic and unchanged provider requirements.
- [x] 2.3 Delete three clock intrinsics, lowering/reservations and generated C fragments; verify inventories and absence assertions.

## 3. Integration and evidence

- [x] 3.1 Replace clock boundary tests with deterministic source/C fixtures and run debug/optimized conformance on Darwin ARM64 and GNU x86-64/ARM64; record actual results and reject LTO.
- [x] 3.2 Update shared native corpus, portable availability checks, scheduler consumers and generated docs; verify no new Wasm clock imports and documentation checks.
- [x] 3.3 Run pnpm typecheck, format:check, lint, test, check and release:candidate; record exact outcomes and publish the verified layer with gh stack.
