## 1. Resolution

- [x] 1.0 Spike: with a receiver fact prepended at ordinal zero into the arguments result of an ordinary call to an inherent method, verify on the evaluator that `option.map(addOne)` (one written argument, two parameters) produces a call and not a section, that a `Self` receiver on a move-only local lowers to a `Move` and reports use-after-move at a later use rather than explicit-move-required, and that a receiver expression containing an error reports it exactly once. Keep the passing test; it anchors the design.
- [x] 1.1 Add `resolveMethodCandidate(subjectType, name, caller)` returning inherent receiver method, bound operation, ambiguous bounds, no-receiver, or missing, backed by `lookupAssociated` for nominal subjects and a new `boundReceiverOperation` inverted search for type-parameter subjects. Verify unit tests for each outcome on a nominal subject and a bounded parameter subject.
- [x] 1.2 Add the method-call branch at the top of the `CallExpression` arm, before the first argument pass: analyze the value subject once, check for a field (fall through unchanged if one exists), resolve the candidate, synthesize the receiver argument per parameter zero (shared loan, exclusive loan with the exclusive-root guard at the receiver's span and spelling, temporary root for an rvalue, move, or pass-through for a matching reference), analyze written arguments once against parameters one onward, prepend the receiver at ordinal zero, build `referencePath` from the resolved member, and finish through `finishDeclarationCall`. Verify `option.map(addOne)` records the same call target as `Option.map(move option, addOne)` and `option |> Option.map(addOne)`, that a callable-field call is unchanged, that `left.merge(&right)` accepts the borrow in position one, that a `&fixedArray` argument keeps its reference type with no duplicate diagnostic, that a local named `Option` resolves as a value, and that `referencePath` on `Option.some(2).map(addOne)` names `Option.map`.
- [x] 1.3 Verify receiver modes: a shared loan leaves the place usable, an exclusive loan conflicts with a live shared loan, an exclusive loan on a `let` binding reports exclusive-borrow-requires-mutable naming the binding at the receiver's span, a consumed receiver reports use-after-move, an rvalue chain `Option.some(2).map(addOne)` consumes the temporary, a `&Counter` receiver passes through without a second loan, and a `Box<Counter>` receiver is rejected without dereference.
- [x] 1.4 Add the no-receiver and must-be-called diagnostics: `counter.zero()` for an associated function, and bare `option.map` outside callee position from `analyzeProjection`'s unknown-field branch. Verify both diagnostics name `Owner.member` and that an unknown name still reports the field diagnostic.

## 2. Bound receivers

- [x] 2.1 Implement `boundReceiverOperation`: walk one type parameter's resolved bounds for a receiver operation named `name` and, for exactly one candidate, build the same bound reference `Bound.op(value)` builds (`interfaceOperationContract` over the bound's application) with the receiver at ordinal zero, finishing through `finishInterfaceOperationCall`. Verify `fn show<T: Printable>(value: &T) { return value.print() }` records the same bound operation as `Printable.print(value)` and specializes once per instantiation on the evaluator.
- [x] 2.2 Add the ambiguous-receiver-operation diagnostic naming the interfaces. Verify `T: Printable + Debug` reports both interfaces while `Printable.print(value)` still resolves, an unbounded `T` is rejected at declaration, and a bound operation with its own type parameters is an unknown member through both spellings.
- [x] 2.3 Verify a concrete receiver does not reach interface operations: `document.print()` with only a conformance reports an unknown member.

## 3. Tooling

- [x] 3.1 List fields and receiver methods after `value.` in `Completion` with receiver-bound signatures and method labels, excluding associated functions; include bound operations for a generic subject. Verify the `option.` and `T: Printable` scenarios in `MethodCall.test.ts`.
- [x] 3.2 Present receiver-bound hover on a called member and the complete contract on `Owner.member`, both with documentation. Verify the LSP acceptance fixture hovers both spellings.
      Verified at the analysis layer (`MethodCall.test.ts` hovers `value.read()` and `Counter.read`); the LSP acceptance fixture is unchanged because hover flows through the same `hoverSubjectAt`.
- [x] 3.3 Record receiver-syntax occurrences with the member's identity so definition, references, and rename cover them, and navigate a bound method call to the interface operation. Verify a rename test across all three spellings and a definition test on `value.print()` in a generic body.

## 4. Documentation and acceptance

- [x] 4.1 Add the receiver form to the functions/callables and generics/interfaces reference pages, showing direct, method, and pipeline forms together, stating the declared-receiver ownership rule and the parenthesized explicit spellings. Verify the docs snippet tests compile the new examples.
- [x] 4.2 Add a semantic fixture covering the matrix from the specs (three spellings, section-arity call, rvalue chain, field precedence, receiver modes, fixed-array argument, no-receiver rejection, bound-method rejection, bounded and unbounded generics, bound ambiguity, concrete receiver without interface reach). Verify it passes on the evaluator.
- [x] 4.3 Regenerate the diagnostic index and verify the staleness checks pass.
- [x] 4.4 Run `node scripts/turbo.mjs run test` and verify the full gate passes.
      Recorded: `pnpm check` on the final tree (format, build, lint, typecheck, and every package suite including the compiler's 245 files and native acceptance) green, 36 of 36 tasks.
