## 1. Contract and supplies

- [x] 1.1 Publish the prescriptive supply contract and pinned authority/prior-art record; validate this OpenSpec change strictly before implementation.
- [x] 1.2 Add immutable request, origin, capability and failure records; verify precedence, explicit failure, managed rejection and cross-host rules with structural tests.

## 2. Physical resolution

- [x] 2.1 Implement frozen tool/environment and Darwin/GNU component discovery, compatibility and content validation; verify missing/mixed/wrong-platform/deployment fixtures.
- [x] 2.2 Resolve complete ordered driver/linker plans, libraries, recursive scripts and stub/import closures; verify structural closure, group/weak/archive behavior and changed-input failures.
- [x] 2.3 Implement relevant C preprocessing/header snapshots and stage-specific identities; verify consumed-header and tool-byte invalidation and equivalent-path stability.

## 3. Integration

- [x] 3.1 Replace native finalization and feed complete plans into the existing cache-admission rule; verify Driver cache/entry/artifact tests and delete superseded ambient native paths.
- [x] 3.2 Wire project/artifact requests, CLI/build callers, diagnostics, inspection and public exports; verify configuration and inspection tests and browser-safe exports.

## 4. Conformance and delivery

- [x] 4.1 Add required pinned Darwin ARM64 and GNU/Linux x86-64/ARM64 CI lanes with real compile/link/inspection and C execution in debug/release; retain exact supplies/results and fail missing/skipped cases.
- [ ] 4.2 Run typecheck, format:check, lint, test, check and release:candidate in order; record exact outcomes and distinguish baseline failures.
- [ ] 4.3 Publish the JUL-126 stack branch with gh stack, with a concrete implementation and validation description.
