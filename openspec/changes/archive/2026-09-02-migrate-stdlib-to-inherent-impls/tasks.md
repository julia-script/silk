## 1. Standard-library source

- [x] 1.0 Convert the thirty conformance-mapped private targets (`os_filesystem`, `os_monotonic_clock`, `os_host_input`, `os_system_clock`, `os_child_process`, `os_random`, `os_standard_input`, `local_scheduler`, `logger`, `allocator`, `insecure_random`, `insecure_seed`) into private inherent members of the mapped owner so `name: Owner.member` resolves through membership. Verify the provider conformance tests pass with the projection still present.
      Tasks 1.1 through 1.7 are one atomic cut: the stdlib does not compile between them, and the gate is verified at 1.7 and 4.1.

- [x] 1.1 Migrate `option.silk`, `result.silk`, and `order.silk` (all six `Order.*` functions become associated functions of the `Order` interface with unchanged signatures, per the design's owner table): move public operations into `impl` blocks with owner binders on the head and `Self` for an owner-typed parameter zero, rewrite sibling calls to `Owner.`, keep private helpers free. Verify by reading each module against the owner table.
- [x] 1.2 Migrate the small collection and storage modules (`layout`, `raw_buffer`, `slot`, `box`, `shared`, `hash` (functions stay `Hash.*`), `metrics`, `unicode`, `unicode_tables`, `execution`, `bytes`, `string` including its `unsafe fn` member, `hash_map`, `hash_set`, `vector`). Verify by reading against the owner table.
- [x] 1.3 Migrate `writer.silk` and `format.silk` (about 75 unqualified sibling calls across fifteen names) into `impl Writer` and `impl Format`. Verify by reading against the owner table.
- [x] 1.4 Migrate `effect.silk` (thirty `pub effect fn` become associated functions of the zero-data `Effect` owner; parameter zero keeps its builtin `Effect<A ! E ? R>` type), then `logger` (functions stay `Logger.*`), `fiber`, `scheduler`, `local_scheduler`, `numeric`. Verify by reading against the owner table.
- [x] 1.5 Migrate `filesystem.silk` into `impl Path` (parameter zero `Path`) and `impl FileSystem` (the rest), then `os_filesystem`, `child_process`, `host_input`, `standard_input`, `system_clock` and `monotonic_clock` (functions stay under the service), random and seed modules (`InsecureSeed.*` stays), and their `os_*` providers. Verify by reading against the owner table.
- [x] 1.6 Rewrite every selective import of a migrated operation, every namespace import used to reach a migrated operation (including the clock aliases in `fiber`, `local_scheduler`, `monotonic_clock`), and every `Owner.RootType` type path reached through a selected type import (`Format.ParseError`, `Hash.Word`, ...) in stdlib, doc fences, tests, fixtures, examples, LSP tests, docs labs presets, and docs, to the owner-qualified call or the direct type selection; drop now-redundant aliases. Verify a repository grep over migrated modules (excluding `bool`, `char`, `static_text`, `static_sequence`, `reflect`, `target`, and primitives) finds no lowercase selective import of a migrated operation and no `[A-Z]\w*\.[A-Z]\w*` type path whose qualifier is a selected type.
- [x] 1.7 Verify the complete cut compiles: run the compiler test suite for stdlib acceptance (Option, Result, Order, collections, formatting, Effect, logging, fiber, service provision, native acceptance) and the doctest of stdlib examples, and confirm both `import silk.effect { Effect }` and `import silk.effect as EffectModule` behave per the specs.

## 2. Compiler

- [x] 2.1 Delete `NameResolution.scopedModule` and its callers in `NameResolution.lookupQualified`, `CallResolution`, both `ExpressionAnalysis` sites, and the three `Completion` sites, making the associated lookup's `Missing` outcome final. Verify the build has no remaining reference and the basename-match, namespace-does-not-expose-members, and type-import-is-not-a-namespace scenarios pass.
- [x] 2.2 Remove the declaring-module fallback for service and interface qualifiers so contract operations and inherent members are the only associated items of a contract. Verify `Logger.inMemoryProvider()` resolves as a member, an unknown member reports the associated-member diagnostic for both a service and an interface, and a bare contract operation with no bound in scope keeps its current diagnostic while the bounded and applied forms in `InterfaceBounds.test.ts` and `BoundOperationWitness.test.ts` pass unchanged.
- [x] 2.3 Rewrite the tests whose premise was the projection (`StdlibNamespaceAcceptance` scope-actor and nominal-union-exposes-module tests, `EditorIntelligence` file-named-union completion, `QualifiedBorrowArgument`, `NameResolution` namespace tests) to declare impls or select owners, and add the rename-a-module-without-changing-its-API test. Verify they pass.

- [x] 2.4 Land the rules the cut required (mentioned owner binders, refined owner binders, member-minted binders, local owner wins over a builtin spelling, rejected heads still close `Self`, constructor fold through an effect-free parameter drop) with their spec deltas in this change. Verify `InherentImpl.test.ts` covers the bounded-head-without-cascade and mentioned-binders cases, `MirNormalization.test.ts` covers the provided service operation fold, and the stdlib compiles with `Fiber.cancel`, `HashMap.get`, `Slot`, and `RawBuffer` in their migrated shape.

## 3. Documentation

- [x] 3.1 Replace `NAME-005` in `modules-names-and-visibility.md` with the associated-items rule and update the standard-library import section that cites it. Verify the docs snippet tests compile every fence on the page.
- [x] 3.2 Rewrite `STYLE-002` and `STYLE-003` in `style-guide.md` around inherent impls, receiver-first contracts, free functions for multi-type operations, and owner imports, and update the heading anchors cited from `documentation-style-guide.md` and `generics-interfaces-and-specialization.md`. Verify the snippet tests and the docs link check pass.
- [x] 3.3 Add `impl` blocks to the five pages whose fences declared a basename-matching type (`language/effects.md`, `language/stdlib/effect.md`, `language/tutorial.md`, `reference/functions-callables-and-control-flow.md`, `reference/style-guide.md`), and retire "scope actor" wording in favor of "owner" across docs and the `silk-documentation-model` renderer wording. Verify the docs snippet tests pass across `apps/docs/content`.
- [x] 3.4 Capture the resolved-link count of the committed stdlib reference before regenerating, then regenerate the standard-library documentation, stdlib source tables, and diagnostic index. Verify the staleness checks pass and the regenerated reference resolves at least as many `[`member`]` links as before.
      Recorded: stdlib `[`member`]` source links 641 → 623 and generated reference links 705 → 687; the 18 fewer links are the doc comments of the deleted service-operation wrappers, and the documentation policy reports zero unresolved links.

## 4. Acceptance

- [x] 4.1 Run `node scripts/turbo.mjs run test` and verify the full gate passes.
      Recorded: `pnpm check` on the final tree: format, build, lint, typecheck, and every package suite green (compiler 244 files and native acceptance 172 cases); the docs labs preset byte-identity test needed the scanner fixture in formatter-canonical import spelling, fixed and rerun green.
