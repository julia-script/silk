## 1. Contract and evidence

- [x] 1.1 Publish the complete prescriptive contract, exact authority/header pins and comparative design; validate this OpenSpec change strictly before code changes.
- [x] 1.2 Preserve and present ellipsis syntax with fixed-boundary and unsupported-definition diagnostics; verify parser, formatting and analysis fixtures.

## 2. Semantic and ABI pipeline

- [x] 2.1 Admit integer tails and preserve default promotions separately from fixed declaration identity; verify zero/additional operands, narrow signed/unsigned values and rejected categories through shared analysis snapshots.
- [x] 2.2 Thread variadic status through signature agreement, ABI manifests and inspection, updating all producers/decoders and goldens; verify missing/invalid serialized status, conflicts and inactive selected variants.
- [x] 2.3 Carry promoted call shapes through MIR and verify their canonical fixed/tail boundary and foreign contracts; reject invalid MIR fixtures.
- [x] 2.4 Emit true LLVM variadic declarations and per-call fatal-unwind guards with correct integer extension; verify IR types, call operands, guard reuse and attributes.

## 3. Conformance and delivery

- [x] 3.1 Add required separately compiled C va_arg and direct open/openat fixtures on pinned Darwin ARM64 and GNU x86-64/ARM64 supplies; compile/link/inspect/execute debug and optimized cases and retain results with no skips.
- [x] 3.2 Update affected catalogs, docs, examples and public exports together; verify generated artifacts and source example diagnostics.
- [ ] 3.3 Run typecheck, format:check, lint, test, check and release:candidate in order; record exact outcomes and distinguish any baseline failures.
- [ ] 3.4 Publish the JUL-140 PR above JUL-126 through gh stack with concrete validation and implementation details.
