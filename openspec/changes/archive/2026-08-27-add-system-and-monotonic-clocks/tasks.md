## 1. Portable clock contracts

- [x] 1.1 Add `packages/compiler/stdlib/silk/system_clock.silk` with private canonical `Instant` fields, trapping `make`, read-only component accessors, the exclusive `SystemClock` service, wrappers, and source documentation; verify focused analysis tests cover epoch zero, `{ -1, 999_999_999 }`, invalid fractions, accessor visibility, and an ordinary fixed provider.
- [x] 1.2 Add `packages/compiler/stdlib/silk/monotonic_clock.silk` with the public exclusive `MonotonicClock` service and all four wrappers over the shared `Instant`; verify focused analysis tests cover equal/advancing marks, independent provision, same-provider documentation, zero waits, past deadlines, and a stateful scripted provider.
- [x] 1.3 Register both portable modules in `packages/compiler/stdlib/manifest.json` with only portable dependencies, namespace `SystemClock` plus alias `Instant` for `silk/system_clock`, and namespace `MonotonicClock` for `silk/monotonic_clock`; verify auto-import metadata and an ordinary pure provider analyze for `wasm32-unknown-unknown` without an OS intrinsic or host import.

## 2. Native intrinsic and provider boundary

- [x] 2.1 Add the five closed `OsSystemClock*` and `OsMonotonicClock*` operation identities to `packages/compiler/src/Type.ts`, their unsafe Platform catalog entries to `packages/compiler/src/Intrinsic.ts`, and explicit `osConsumer` routing to the matching provider operations, with Evaluator/LLVM targets; verify `IntrinsicCatalog.test.ts` and `test/fixtures/intrinsic-inventory.json` pin signatures, consumers, targets, unsafe invariants, and inventory order rather than falling through to `silk/os_filesystem.*`.
- [x] 2.2 Add `packages/compiler/stdlib/silk/os_system_clock.silk` as a stateless ordinary-source provider whose constructor performs no host call, whose operations use only the two system-clock intrinsics, and whose invalid/false boundary results trap; verify source-level tests cover conformance, successful canonical construction, positive whole-nanosecond resolution, and fatal failure.
- [x] 2.3 Add `packages/compiler/stdlib/silk/os_monotonic_clock.silk` as a stateless ordinary-source provider using the three monotonic intrinsics, checked split-field deadline arithmetic, and source-derived `waitFor`; verify source-level tests cover carry, maximum-`u64` quotient conversion, deadline overflow trap, zero duration, and exactly one start read before absolute waiting.
- [x] 2.4 Register both provider modules as Evaluator/LLVM target providers with independent dependencies and exact namespaces `OsSystemClock` and `OsMonotonicClock`; verify manifest/auto-import closure tests show importing or constructing one provider neither reads a clock nor retains the other provider's intrinsic identities.
- [x] 2.5 Regenerate `packages/compiler/src/Stdlib.generated.ts` after all four modules and intrinsic identities exist, and verify `pnpm --dir packages/compiler stdlib:check` reports deterministic dependency order with unchanged unrelated embedded source.

## 3. Exact evaluator hosts

- [x] 3.1 Add actor modules `packages/compiler/src/SystemClock.ts` and `packages/compiler/src/MonotonicClock.ts` with browser-safe synchronous `bigint` contracts, validated fixed/scripted constructors returning explicit construction-failure data, separate failing hosts, and immutable wait-event snapshots; verify actor tests preserve values beyond JavaScript's exact `number` range, accept exact `i64`/positive-`u64` endpoints, reject values one step outside those ranges plus malformed fractions/decreasing scripts without wrapping or throwing, record past waits without moving backwards, and advance only the scripted monotonic timeline.
- [x] 3.2 Export both host actors from `packages/compiler/src/index.ts` and explicit compiler package subpaths, then verify `pnpm --dir packages/compiler exec tsc -p tsconfig.json --noEmit` resolves the namespace and deep-import APIs without Node-only globals.
- [x] 3.3 Thread optional independent clock hosts through `BootstrapEvaluation.Options`, evaluator state, and `BootstrapOsIntrinsics.ExecutionContext`; dispatch all five clock calls before the existing filesystem-style final-status-output assumption, translate a provider throw to false while retaining its original `cause` in the `OsCall` trace, and verify focused tests distinguish successful output, explicit/throwing boundary failure, and no partial output commit.
- [x] 3.4 Add `MissingSystemClock` and `MissingMonotonicClock` blocked reasons plus exhaustive flow-model and project-inspector presentation; verify evaluator tests retain the preceding trace, block only the absent matching host, and never consult `Date`, `performance`, or the other clock.

## 4. Reachability-selected POSIX runtime

- [x] 4.1 Make `ToolchainPlan.shimSource` the sole owner of a translation-unit preamble containing `_DARWIN_C_SOURCE`/`_GNU_SOURCE` and `#define _POSIX_C_SOURCE 200809L` before every system header, then make `OsRuntime.source(selected)` return only minimal selected filesystem, standard-input, child-process, host-input, and clock fragments without changing existing symbols; replace the direct `OsRuntime.source` strict-C compile case in `OsFileSystem.test.ts` with a complete generated-shim test, and verify existing runtime tests pass, a clock-only snapshot contains no unrelated helpers, and combined clock+standard-stream and clock+typed-failure shims expose clock declarations under strict C11.
- [x] 4.2 Implement `silk_os_system_clock_now_v1` and `silk_os_system_clock_resolution_v1` with `CLOCK_REALTIME`, atomic output commit, canonical `timespec` validation, `time_t` round-trip checks, and checked full-resolution conversion; verify a deterministic substituted-C harness covers pre-epoch normalization, `time_t` round-trip rejection, multi-second and zero resolution, malformed fractions, syscall failure, and unchanged outputs on every failure.
- [x] 4.3 Implement `silk_os_monotonic_clock_now_v1` and `silk_os_monotonic_clock_resolution_v1` with `CLOCK_MONOTONIC` and the same atomic conversion rules; verify the substituted-C harness covers non-decreasing/equal values, positive whole-nanosecond resolution, malformed output, and syscall failure.
- [x] 4.4 Implement `silk_os_monotonic_clock_wait_until_v1` with Linux `clock_nanosleep(CLOCK_MONOTONIC, TIMER_ABSTIME, ...)`, retrying the unchanged deadline on its directly returned `EINTR`; verify the Linux harness rejects negative seconds, negative fractions, and a fraction of one billion before any sleep, covers past deadlines, repeated interruption and non-`EINTR` positive errors, and confirms `errno` is not used for the return code.
- [x] 4.5 Implement the non-Linux POSIX/macOS absolute-deadline fallback using repeated `CLOCK_MONOTONIC` reads, normalized positive remainder, and `nanosleep`, looping back to the original deadline after both zero return and `EINTR` while discarding the relative remainder; verify the fallback harness covers malformed inputs before sleep, past deadlines, borrow, signal-handler time, repeated interruption, early successful wake, and unrecoverable read/sleep failure without wall-time thresholds.
- [x] 4.6 Extend native symbol/source selection and cache tests for all five operations, pass `artifact.nativeRuntimeSymbols` into the final native-executable artifact-cache shim key, and compile complete generated shims with strict C11 warnings; verify distinct symbol sets have distinct cache keys, the current `glibc >= 2.17` Linux baseline and macOS need no new link library, and an unselected operation contributes neither declaration nor C definition.

## 5. Cross-engine and target behavior

- [x] 5.1 Add a shared `packages/compiler/test/Clock.test.ts` suite using `@effect/vitest` and one analysis snapshot per source program; verify portable replacement, exact evaluator hosts, blocked reasons, fatal cases, MIR `OsCall` names, and trace order without `Effect.runPromise`, timing assertions, or duplicate compiler pipelines.
- [x] 5.2 Add direct-Wasm reachability cases proving unused provider imports remain valid and each reachable clock intrinsic produces the existing target-availability diagnostic before emission; verify no invented Wasm import, WASI shim, or partial module is produced.
- [x] 5.3 Add the smallest native acceptance coverage for clock ABI/link/run: canonical system and monotonic reads, positive resolutions, a past deadline, and one tiny positive wait with no elapsed-time assertion; if a pure cross-engine result case is needed, add it once to `test/support/corpus.ts` rather than compiling it in a feature-local loop.
- [x] 5.4 Add a focused `macos-15` arm64 CI job that builds the compiler and runs the forced-fallback C harness plus native clock acceptance on the repository's `aarch64-apple-darwin` host target; verify the durable job fails on missing Apple clock declarations or ABI/link/run regressions.

## 6. Generated documentation

- [x] 6.1 Generate the four standard-library reference pages and indexes from canonical `//!`/`///` comments, update authored stdlib inventories only where required, and verify `pnpm --dir packages/compiler documentation:policy`, `documentation:check`, and `documentation:examples` cover units, epoch/origin, canonical fractions, nominal-resolution lower-bound semantics, provenance, native blocking versus virtual advancement, fatal behavior, explicit provision, Unix/glibc limits, and direct-Wasm exclusion.

## 7. Repository verification

- [x] 7.1 Run the required sequence `pnpm typecheck`, `pnpm exec biome check .`, and `pnpm test`; fix every regression caused by the clock change and record any demonstrably pre-existing failure with its exact command and output.
- [x] 7.2 Run `pnpm check` after the focused suites pass and verify the complete compiler, documentation, script, and native-acceptance graph succeeds.
- [x] 7.3 Run `pnpm release:candidate` because compiler package exports and packaged standard-library contents change, and verify the release artifact contains the new public host subpaths and four canonical clock modules without an ambient provider or compatibility path.
