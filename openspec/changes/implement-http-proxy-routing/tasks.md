## 1. Bounded proxy policy

- [x] 1.1 Add `silk.http_proxy` configuration, auth, bypass, route, route-key, and error values with checked owned-capacity validation plus caller-supplied nonsecret `ProxyConfigId`/`ProxyAuthContextId` copy and exact-equality semantics; encode malformed URI/auth/capacity input, credential-context rotation, and configuration-time encrypted-proxy refusal in the shared support source for analysis and MIR verification, with behavior execution deferred to 4.2's one corpus run.
- [x] 1.2 Implement exact normalized-origin bypass and pure Direct/Forward/Tunnel selection with no encrypted-proxy route variant and with pure recomputation hooks; encode the admitted route matrix, identity retention, and equivalent numeric host identities in that same analysis/MIR support source, with behavior execution deferred to 4.2.

## 2. Proxy wire preparation

- [x] 2.1 Extend streaming request admission with distinct logical-origin and physical-peer identities and an explicit routed mode; retain direct `Proxy-Authorization` rejection and encode distinct-peer, tunneled-origin replay, and direct-rejection scenarios in the shared analysis/MIR support source for execution under 4.2.
- [x] 2.2 Implement Forward absolute-form preparation with logical-origin `Host`, proxy-peer admission, configured-only `Proxy-Authorization`, preserved encoded path/query, and caller-override rejection; encode its exact emitted head and plaintext credential boundary in the compact corpus source whose single execution is integrated under 4.2.
- [ ] 2.3 Implement body-free authority-form CONNECT preparation plus exact rejection metadata ownership: ordered duplicate `Proxy-Authenticate` fields for 407, ordered full fields otherwise, the stated field/name/value/aggregate limits and reachable 32768-byte total-owned limit, and parser-before-copy failure precedence; exercise only proxy-distinct any-2xx, 101, 407, non-2xx, metadata-overflow, and shared head-limit signals without recreating the parser matrix.

## 3. Scoped route composition

- [ ] 3.1 Add the single-use, allocation-free tunnel transfer that publishes a complete affine `ByteDuplex`, disarms the outer owner only at publication, serves the retained suffix first, preserves concrete read/write/flush/write-shutdown authority, and closes terminally once; prove invalid/repeated transfer, success, and protected callback failure with one release-counter signal in the compact corpus.
- [ ] 3.2 Implement target-neutral `withRoute(client, route, preparedTrust, deadline, handler)` with the specified higher-ranked success/failure/requirement preservation, trust-presence preflight, and acquisition adapter carrying explicit HTTP limits, version/ALPN policy, TLS limits, and handshake duration without silent defaults; then compose Tunnel with caller-prepared trust, the caller's unchanged pre-load absolute deadline, and original-origin TLS; reuse exactly one existing TLS vector to prove TLS starts only after CONNECT, rejects proxy-only identity, and emits no proxy credential inside the tunnel.
- [ ] 3.3 Add `http_client_native.withProxyRoute(route, options, limits, preparedTrust, handler)` with the specified generic channels, resolving/connecting only the selected physical endpoint; prove endpoint choice, unsupported targets, and synchronous-hostname deadline refusal through structural native preflight assertions rather than additional physical compiler executions.

## 4. Registration, documentation, and durable evidence

- [ ] 4.1 Register `silk.http_proxy`, regenerate canonical standard-library/catalog surfaces, and publish a prescriptive proxy reference with routed admission, the route matrix, caller-owned identity/trust rules, plaintext credential warning, exact metadata limits, deadlines, ownership, errors, and exclusions; ensure generated output matches the manifest and examples type-check.
- [ ] 4.2 Integrate the fixed evidence budget: one shared `Analysis` snapshot, one compact shared runtime corpus program, one reused TLS vector, structural native preflight, and exactly one LLVM-to-Wasm leg named for routed-preparation-and-scripted-tunnel portability; add no stress/timing suite, per-case compilation, fresh-process matrix, duplicate TLS matrix, or new standalone test worker.
