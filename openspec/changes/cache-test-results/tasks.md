# Tasks

## 1. Per-test execution identities

- [ ] 1.1 Attribute residual/static-evaluation dependencies to the exact specializing application, including demanded helper bodies, parameter defaults and predicates, and profile-bootstrap dependencies; extend focused semantic/residualization fixtures to prove same-result helper edits remain observable without invalidating unrelated applications.
- [ ] 1.2 Add the compiler `TestExecution` actor and `Instances` rooted execution-closure projection covering runtime helpers, hidden callable/Effect bodies, cleanup/finalizer targets, provider selections/implementations, intrinsics, and foreign calls; extend `TestDiscovery`/instance tests to prove exclusive versus shared dependency invalidation, same-file independence, trivia stability, and per-test ineligibility when completeness is unavailable.
- [ ] 1.3 Derive the external native-execution identity without generated program-object, destination, whole-artifact, or unrelated-test inputs, then publish the completed catalog-order manifest on successful test-purpose `Driver.Compiled` outcomes; verify changes to published profile/bootstrap, runner/runtime, compiler distribution, target/CPU/safety/optimization, resolved native files, or supply identity invalidate the affected manifest while unrelated authored tests remain independently keyed.

## 2. Pass records and isolated runner exchange

- [ ] 2.1 Add the CLI `TestResult` actor with a strict 4096-byte pass-only codec and Storage-backed lookup/publication at `<build.output-dir>/.silk-cache` / `test-results-v1`; cover cross-service restart reuse, exact-key/payload/integrity admission, corrupt/oversize data, typed read/write failures, pass-only publication, and preserved interruption/defects with focused record/storage tests.
- [ ] 2.2 Add the closed binary `TestExchange` plan/receipt codec with nonce and plan binding, canonical per-declaration dispositions, checked `4096 + 256 * discoveredCount` bounds and 16 MiB ceiling; cover round trips plus duplicate, missing, reordered, mismatched, truncated, oversized, and wrong-version rejection.
- [ ] 2.3 Add `Program.runTest` as the sole native-process boundary for a scoped private plan/result scratch directory while retaining inherited stdin/stdout/stderr; verify cleanup on success, typed failure, defect, and interruption, normal-exit receipt validation, abnormal termination preservation, and that stdout/stderr lookalikes cannot influence the returned receipt.
- [ ] 2.4 Update the ordinary bundled Silk runner and generated standard-library artifacts to validate a complete plan before execution, skip only exact cached declarations, emit one complete receipt, and report discovered/selected/cached/executed/passed/failed counts; extend existing runner/native acceptance cases for mixed hits/misses/failures, zero selection, cleanup/order, invalid plans, and traps without adding process-per-test isolation or compiler-known runner policy.

## 3. Default-on CLI workflow

- [ ] 3.1 Add `--no-cache` to `TestCommand` and result-cache policy to `Workflow.TestSelection`, keeping default-on reuse separate from `Driver.CompileRequest.cache`; extend command parsing/help tests to prove the flag affects only test-result reads/writes and adds no watch option.
- [ ] 3.2 Integrate manifest lookup, all-execute fallback, `Program.runTest`, complete-receipt admission, executed-pass-only publication, cache-event reporting, and authoritative exit propagation in `Workflow.test`; extend workflow/CLI fixtures to prove cross-process hits, failed-test reruns, per-test same-file invalidation, shared-dependency invalidation, filter stability, `--no-cache` read/write bypass, cache failure degradation, invalid exchange status 2, and unchanged compilation/diagnostic semantics.
- [ ] 3.3 Update the testing and CLI reference documentation to distinguish local authored fingerprints from execution identities, describe cached/executed reporting and `--no-cache`, and state that arbitrary runtime files/environment/network/time and earlier-test side effects are not tracked; verify all added Silk examples through the existing documentation example checks.
