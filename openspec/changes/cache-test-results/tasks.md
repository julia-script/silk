# Tasks

## 1. Per-test execution identities

- [x] 1.1 Attribute residual/static-evaluation dependencies to the exact specializing application, including demanded helper bodies, parameter defaults and predicates, and profile-bootstrap dependencies; extend focused semantic/residualization fixtures to prove same-result helper edits remain observable without invalidating unrelated applications.
- [x] 1.2 Add the compiler `TestExecution` actor and `Instances` rooted execution-closure projection covering runtime helpers, hidden callable/Effect bodies, cleanup/finalizer targets, provider selections/implementations, intrinsics, and foreign calls; extend `TestDiscovery`/instance tests to prove exclusive versus shared dependency invalidation, same-file independence, trivia stability, and per-test ineligibility when completeness is unavailable.
- [x] 1.3 Derive the external native-execution identity without generated program-object, destination, whole-artifact, or unrelated-test inputs, then publish the completed catalog-order manifest on successful test-purpose `Driver.Compiled` outcomes; verify changes to published profile/bootstrap, runner/runtime, compiler distribution, target/CPU/safety/optimization, resolved native files, or supply identity invalidate the affected manifest while unrelated authored tests remain independently keyed.

## 2. Pass records and isolated runner exchange

- [ ] 2.1 Add the CLI `TestResult` actor with a strict 4096-byte pass-only codec and Storage-backed lookup/publication at `<build.output-dir>/.silk-cache` / `test-results-v1`; cover cross-service restart reuse, exact-key/payload/integrity admission, corrupt/oversize data, typed read/write failures, pass-only publication, and preserved interruption/defects with focused record/storage tests.
- [ ] 2.2 Add the closed binary `TestExchange` codec with explicit `PerTest` and `Uncached` modes, nonce and plan binding, canonical per-declaration dispositions only in `PerTest`, and preflight of actual plan plus maximum complete receipt against checked `4096 + 256 * discoveredCount` bounds and the 16 MiB ceiling before allocation or lookup; implement the specified 256-byte compact plan/aggregate receipt, catalog digest, exact unsigned 64-bit counts, zero padding, and no per-test authority in `Uncached`. Cover round trips and duplicate, missing, reordered, mismatched, truncated, oversized, wrong-mode/version, invalid padding/count/status, and arithmetic overflow rejection, plus long identities with no hits, large catalogs, and receipt-only overflow selecting compact mode.
- [ ] 2.3 Add `Program.runTest` as the sole native-process boundary for a scoped private plan/result scratch directory while retaining inherited stdin/stdout/stderr; verify cleanup on success, typed failure, defect, and interruption, normal-exit receipt validation, abnormal termination preservation, and that stdout/stderr lookalikes cannot influence the returned receipt.
- [ ] 2.4 Update the ordinary bundled Silk runner and generated standard-library artifacts to validate a complete mode-specific plan before execution, including compact catalog count/digest binding, skip only exact `PerTest` hits, emit the mode-specific complete receipt after cleanup, and report discovered/selected/cached/executed/passed/failed counts; extend existing runner/native acceptance cases for mixed hits/misses/failures, compact all-execute filtering with cached zero, zero selection, cleanup/order, unchanged streaming output, invalid compact plans before body effects, and traps without adding process-per-test isolation or compiler-known runner policy.

## 3. Default-on CLI workflow

- [ ] 3.1 Add `--no-cache` to `TestCommand` and result-cache policy to `Workflow.TestSelection`, keeping default-on reuse separate from `Driver.CompileRequest.cache`; extend command parsing/help tests to prove the flag affects only test-result reads/writes and adds no watch option.
- [ ] 3.2 Integrate preflight mode selection before manifest lookup, compact uncached fallback and direct `--no-cache` selection, `Program.runTest`, mode-bound complete-receipt admission, `PerTest` executed-pass-only publication, cache-event reporting, and authoritative exit propagation in `Workflow.test`; extend workflow/CLI fixtures to prove cross-process hits, failed-test reruns, per-test same-file invalidation, shared-dependency invalidation, filter stability, `--no-cache` read/write bypass, cache failure degradation, invalid exchange status 2, and unchanged compilation/diagnostic semantics. Cover long identities without hits, large discovered counts with few or zero selected tests, receipt-only overflow, no result-cache reads/writes or publication in compact mode even after success, and preserved all-discovered compilation, output, counts, status, and single execution of each selected body.
- [ ] 3.3 Update the testing and CLI reference documentation to distinguish local authored fingerprints from execution identities, describe cached/executed reporting and `--no-cache`, and state that arbitrary runtime files/environment/network/time and earlier-test side effects are not tracked; verify all added Silk examples through the existing documentation example checks.
