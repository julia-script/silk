## 1. Planning Completion and Regression Baseline

- [x] 1.1 Add the deferred delta specs for syntax/formatting, semantic facts/module surfaces, intrinsic boundary, diagnostics, HIR/instances/MIR, Silk stdlib, and standard-library documentation, then pass strict OpenSpec validation before implementation begins.
- [x] 1.2 Add an analysis regression proving `Effect.provideMut` with `&mut Clock | &mut Logger` and `StdoutLogger` preserves only `Clock`, alongside the agreeing direct intrinsic case.
- [x] 1.3 Add baseline cases for row-order permutations, no provider match, multiple capability matches, multiple roles, same-role multiple capabilities, shared requirement with exclusive provider, and explicit complete-member selection.
- [x] 1.4 Add baseline generic-body cases proving a wrapper with an enclosing provider-selection constraint type-checks while the same body without that constraint does not.
- [x] 1.5 Add baseline shared, exclusive, reference-parameter, owned-Copy, owned-affine/take-once, pipeline, partial-application, and stored-intermediate binding cases with current expected failures documented.

## 2. Deterministic Concrete Row Domains

- [x] 2.1 Implement a pure generic `FiniteRow` actor with canonical keys/order, collision merging, exact membership/subset, intersection, total difference, and deterministic encoding under a supplied domain policy.
- [x] 2.2 Add value-union and failure-set policies using canonical nominal identity, and route existing closed normalization through `FiniteRow` without exposing value-union subtraction syntax.
- [x] 2.3 Add the requirement-row policy keyed by capability-role with the explicit Shared/Exclusive union-join matrix and exact stored-access membership/difference matrix.
- [x] 2.4 Add exhaustive requirement access tests for all union, exact intersection, membership, subset, difference, and provider-compatibility pairings, including `&C | &mut C`, shared/exclusive intersection in both orders, weaker subtraction, stronger subtraction, and exact selected-member removal.
- [x] 2.5 Add concrete row tests for canonical ordering, duplicate merging, singleton and set-to-set difference, absent-member no-op, stable keys, and substitution-created nominal/access collisions.

## 3. Forward-Only Symbolic Row Algebra

- [x] 3.1 Implement `RowAlgebra` expressions for concrete rows, distinct row parameters, domain-specific symbolic singleton member shapes (including failure type parameters and fixed-access/fixed-role requirement shapes with a parameterized capability), union, and `Without`, with kind preservation, deterministic definitional keys, parameter discovery, and an occurs check.
- [x] 3.2 Implement domain-sound open simplification with a `MemberWellFormedKey -> OrderedSet<SourceSpan>` map, span-independent semantic keys, canonical provenance union, one-diagnostic-per-key primary/secondary span selection, and obligation-aware substitution/occurs-check/parameter discovery; residual/concrete/invalid symbolic-member substitution followed by singleton-domain validation and renormalization; concrete extensional equality, total difference, encoding, presentation, and finite concretization.
- [x] 3.3 Prevent inference from binding a row or lifted-member operand solely through `Without`, membership, or subset; require an independent whole-row, supplied value-argument, explicit-argument, or provider unique-selection source and emit an underconstrained-row result otherwise.
- [x] 3.4 Add both failure and requirement-domain symbolic tests for ACI union, empty and identical operands, residual/valid/invalid generic member substitution (including fixed-role `&mut P@Audit`), retained validation and parameter discovery through `Without<S, S>` and empty-left subtraction, repeated-obligation origin coalescing/ordering/primary-span behavior under expression and substitution permutations, safe failure-set cancellation, deliberately unreduced access-labelled cases, span-independent definitional-key stability, and concrete equality after distinct open expressions specialize identically.
- [x] 3.5 Replace flat Effect, failure-row argument, and requirement-row argument member/parameter fields with symbolic rows, with no compatibility aliases left in the final representation.
- [x] 3.6 Update Type construction, equality, keys, encoding, generic-argument identity, representation checks, substitution, and presentation to traverse symbolic rows plus retained member-well-formedness obligations and renormalize after substitution.

## 4. Syntax, Constraints, Facts, and Tooling

- [x] 4.1 Add contextual `Without<R, S>` parsing in failure and requirement positions and allow contiguous kind-correct explicit call prefixes containing failure/requirement rows, with same-kind recovery, union precedence, omitted-channel presentation, and wrong-kind diagnostics.
- [x] 4.2 Add one comma-separated `where` clause with kind-directed `S in R` (ordinary nominal singleton membership versus row-kind subset), `&P provides S from R`, `&mut P provides S from R`, and owned `P provides S from R`; keep `where`, `in`, `provides`, and `from` contextual outside constraint grammar.
- [x] 4.3 Add parser/formatter round trips and malformed recovery for multiple constraints, singleton and multi-member rows, nested expressions, empty normalization, fixed shared/exclusive/owned provider modes, selected-row-first calls, and keyword-as-identifier cases.
- [x] 4.4 Store source-shaped row expressions and constraints in declaration facts, including binder references and complete provider-mode/selected-row/source-row terms.
- [x] 4.5 Update declaration collection, resolution, copying, dependency analysis, semantic occurrences, navigation, signature help, and binder validation for row expressions and constraints.
- [x] 4.6 Encode symbolic rows, constraints, neutral provider-match/witness identities, branded selection evidence, and their deterministic definitional forms in semantic presentation and module surfaces; add source/intrinsic, conditional-generic, round-trip, and determinism snapshots.

## 5. Callable Contracts, Givens/Wanteds, and Evidence

- [x] 5.1 Introduce one canonical `CallableContract` for source and intrinsic callables containing function kind, generic binders, parameter modes, parameter/result types, constraints, and capture relationships.
- [x] 5.2 Introduce lower-level immutable constraint and `ConstraintEvidence` data with assumed, nominal-member, exact-subset, and exact-wanted-branded concrete requirement-selection variants; add declaration-index-neutral `ProviderMatch = Identity | Conformance(WitnessIdentity)` with canonical, serialization-safe, specialization-complete source/intrinsic witness identity and typed `NoMatch | Unique | Ambiguous | Invalid` conformance outcomes without importing declaration indexing.
- [x] 5.3 Implement stratified solving over existing directed call compatibility: bind a contiguous explicit generic prefix, infer its suffix from supplied arguments plus only constraint relations with an explicit binding rule, keep membership/subset checking-only, apply definitionally exact given entailment and concrete checked constraints, then normalize results; never invert `Without` or infer from an expected result.
- [x] 5.4 Put declaration constraints in the generic-body given environment and discharge only definitionally equivalent specialized callee wanteds into symbolic assumed evidence.
- [x] 5.5 Extend semantic callable values with nested quantified binders, residual constraints/evidence, substitutions, equality/keys, hidden/environment identity, representation checks, module surfaces, and later application; erase them when unapplied, carry them through closed statically visible forwarding chains that end in complete application, and after closed-world identity-flow analysis reject opaque-boundary/runtime/indirect escapes before runtime representation, row-dependent consumers, or lowering.
- [x] 5.6 Implement shared, exclusive, and owned provider selection with exact or uniquely witnessed conformance, exact-one cardinality for every independently bound first `S`, post-substitution semantic-key grouping with canonical occurrence-origin union, complete canonical per-key `Unique | Ambiguous | Invalid` maps and fixed-point key intersection for constraints sharing unbound `S`, order-independent relation-local no-match/surviving-status/conflict/unique/ambiguity outcomes with selector/application primary spans and relation secondary spans, and exact stored-member evidence branded to each complete solved source obligation; do not truncate candidate maps.
- [x] 5.7 Extract or adapt the current declaration-index conformance witness into neutral `ProviderMatch`/`WitnessIdentity` data, inject observational typed outcomes into selection without emitting diagnostics during lookup, and preserve no-match, surviving witness ambiguity, and surviving invalid mapping as distinct solver-emitted outcomes.
- [x] 5.8 Add tests for multi-wrapper evidence forwarding, exact versus weaker/role-mismatched givens, multiple concrete instantiations, valid/invalid generic nominal-member wrappers, direct/pipeline first-position row `S`, membership/subset non-inference, expected remainders that do not bind `S`, explicit and independently inferred empty/multi selector rejection, complete late common candidates such as `{A,B,C} ∩ {C}`, overlapping/disjoint/multiple-empty shared-variable maps, eliminated versus surviving ambiguous/invalid statuses, structural proof that observational lookup cannot emit for an eliminated status, textual and substitution-created constraint-key coalescing with origin/source/substitution permutations, unequal overlapping-map ambiguity and conflict semantic payload schemas with separately asserted primary/secondary location records, direct `ProviderMatch.Identity` versus `Conformance(WitnessIdentity)` branches, same-declaration conditional witnesses with different generic arguments, source/intrinsic local-identity collisions, evidence encode/decode/module-surface round trips, diagnostic multiplicity/spans, and constraint/source-row permutations, cross-obligation evidence-reuse rejection, deterministic zero/one/multiple results, constrained sections passed through a closed static chain or dropped, and opaque-boundary/runtime/indirect escape rejection.

## 6. Uniform Intrinsic Requirement Binding

- [x] 6.1 Replace the text-only bind signature in `Intrinsic.ts` with selected-row-first canonical effect-function contracts for shared `bindRequirement`, exclusive `bindRequirementMut`, and owned `bindRequirementOwned`, all returning `Without<R, S>` through their fixed provider constraints.
- [x] 6.2 Render intrinsic inventory, signature help, and diagnostics from that same contract object and remove the old role-only selector from syntax, tests, generated inventory, and callers.
- [x] 6.3 Route direct calls, pipelines, explicit generic arguments, malformed arity, unavailable arguments, partial application, and stored intermediates through the common call-normalization and contract solver path.
- [x] 6.4 Remove `EffectRule.BindRequirement` as a typing rule and delete the early `isEffectBindRequirementTarget` invocation-analysis branch.
- [x] 6.5 Refactor the sealed post-contract binding hooks to require proof of the wanted (`Assumed` or concrete `RequirementSelection`), retain only fixed-mode place/move legality and capture/HIR construction, and store symbolic proof without requiring concrete capability/role/provider/witness fields in generic HIR.
- [x] 6.6 Delete intrinsic candidate enumeration, parameter-as-candidate permissiveness, role/access inference, row filtering/subtraction, and `Type.effect` result reconstruction.
- [x] 6.7 Use ordinary effect-call capture finalization for each fixed mode; store provider-selection access separately from expression capture access and verify owned Copy remains repeatable while owned affine becomes take-once.
- [x] 6.8 Add structural tests proving inventory and admission share one contract, the post-hook cannot run without evidence, and no intrinsic binding helper can enumerate/subtract rows or construct a result Effect.
- [x] 6.9 Keep `Intrinsic.catchFailure` in the ordinary executable intrinsic inventory for evaluator, WebAssembly, and native targets, remove the temporary catch-specific `AnalysisOnly`/`SEM0098` path, and verify invalid semantic calls stop before target selection.

## 7. Instance Concretization and Compiler Migration

- [x] 7.1 Carry symbolic rows, lifted ordinary member parameters, and assumed constraint evidence through generic HIR, callable identities, and specialization bookkeeping.
- [x] 7.2 Add one complete-application instance frontier that substitutes row and member parameters, validates member singleton domains, and upgrades every used assumed proof to concrete evidence as soon as that application's substitution is known.
- [x] 7.3 Reject residual application parameters, symbolic members, member-well-formedness obligations, differences, assumed proof, unsatisfied constraints, and ambiguity before dependency discovery, witness reachability, layout, row-dependent ownership/cleanup specialization, or lowering, while preserving nested quantified constraints on unapplied callable schemas.
- [x] 7.4 Introduce a concrete specialized contract/evidence bundle required by row-dependent instance consumers so symbolic inputs are unrepresentable.
- [x] 7.5 Compute instance identity from concrete extensional row keys after the frontier and test that definitionally distinct open expressions specializing to one row deduplicate.
- [x] 7.6 Migrate declaration indexing, conformance goals/heads, instance discovery, ownership, layout, callable-field realization, provisional/suspension MIR, MIR, evaluation, native, and WebAssembly consumers in dependency order.
- [x] 7.7 Verify that generic ownership projections remain conservative and that no post-frontier consumer performs symbolic difference, checked subset, provider selection, or evidence entailment.

## 8. Ordinary Silk Binding, Provision, Acquisition, and Catch

- [x] 8.1 Rewrite public selected-row-first `Effect.bindRequirement`, `Effect.bindRequirementMut`, and `Effect.bindRequirementOwned` with fixed shared/exclusive/owned constraints, whole row `R`, and result `Without<R, S>`.
- [x] 8.2 Rewrite `Effect.provide` and `Effect.provideMut` as shared/exclusive specializations whose bodies discharge the intrinsic wanted from their enclosing constraint given.
- [x] 8.3 Rewrite acquisition helpers and `Effect.provideWith` with distinct provider implementation `P`, selected capability row `S`, acquisition failures/requirements, scoped exclusive binding, and unchanged cleanup ordering.
- [x] 8.4 Rewrite selected-nominal-first `Effect.catch<S>` over canonical executable `Intrinsic.catchFailure<S>`, allow ordinary handler-driven inference or an explicit first-position `S`, enforce common nominal singleton membership `S in E`, return `Without<E, S>`, and remove standard-library-name recognition and manual failure filtering/result reconstruction.
- [x] 8.5 Update all old intrinsic role-selector call sites to explicit complete selected-row arguments and delete obsolete compatibility syntax.
- [x] 8.6 Add standard-library analysis/evaluation tests for custom services, conforming nonidentical providers, all fixed provider modes, Copy/affine ownership, access/role ambiguity, exact call syntax for explicit selection, acquisition composition/cleanup, and the Logger/Clock regression.
- [x] 8.7 Remove the whole-row `Effect.catch` alias and migrate every whole-row caller, fixture, corpus case, acquisition/cleanup test, and document to executable `Effect.catchAll`, while preserving ordinary inferred singleton calls.
- [x] 8.8 Add direct, wrapper, and pipeline singleton catch tests for explicit and handler-inferred nominal selection, open generic failure rows, absent selection, `never`/union/non-nominal rejection, success bypass, selected dispatch, nonmatching runtime propagation, handler-failure union, row-order independence, generic-body member-evidence forwarding, and evaluator/WebAssembly/native parity; keep set-to-set difference in row-algebra tests.
- [x] 8.9 Add target-availability and execution tests for reachable direct and wrapped calls, nested user wrappers, multiple source calls reaching one deduplicated instance, all execution targets, invalid-call diagnostic precedence, and the absence of catch-specific `SEM0098` emission.
- [x] 8.10 Add negative failure-row and requirement-row wrappers proving constraints alone cannot return a subtracted Effect without executing the corresponding catch/binding operation.
- [x] 8.11 Verify equivalent user-defined wrappers compile from the same constraints and that no compiler phase recognizes binding/provision combinators or `Effect.catch` by actor/name/origin.

## 9. Diagnostics, Generated Artifacts, and Verification

- [x] 9.1 Add generated diagnostic catalog entries and structured payloads for kind mismatch, invalid nominal singleton selection, exact-access mismatch, checked absence, underconstrained rows, provider no-match, joint provider-selection conflict, provider ambiguity, selected-row cardinality, conformance ambiguity, invalid conformance, cycles, and non-concrete specialization; define precedence, remove catch-specific `SEM0098`, and keep `SEM0071` run-only.
- [x] 9.2 Add diagnostic tests asserting assigned codes; span-free semantic payload identity separately from selector/application primary and ordered relation/obligation secondary location records; canonical common-candidate and full relation-set payloads; duplicate/specialization-collision multiplicity; origin-local source/intrinsic parity; and row/constraint/substitution/source-location permutations rather than wording.
- [x] 9.3 Regenerate `Stdlib.generated.ts`, intrinsic inventory, module surfaces, standard-library reference pages, language reference material, diagnostic documentation, and affected fixtures.
- [x] 9.4 Run `pnpm typecheck` and repair all representation/exhaustiveness errors without casts, non-null assertions, or lint suppressions.
- [x] 9.5 Run `pnpm exec biome check .` and repair formatting or static-analysis failures.
- [x] 9.6 Run `pnpm test`, using shared `Analysis` snapshots and the cheapest engine that can falsify each claim.
- [x] 9.7 Run `pnpm check`, then `pnpm release:candidate` because compiler/stdlib surfaces change, and report pre-existing failures separately.

## 10. Post-implementation audit remediation

- [x] 10.1 Make singleton `Effect.catch<S>` and `Intrinsic.catchFailure<S>` executable on every supported engine, update the proposal/design/delta specs to describe the runtime contract, and remove the temporary analysis-only availability path and `SEM0098` catch diagnostics.
- [x] 10.2 Preserve symbolic failure and requirement row expressions, `Without` nodes, and retained member obligations through executable-owner specialization, presentation, callable identities, and module-surface round trips.
- [x] 10.3 Associate solved provider evidence by semantic wanted identity after canonical grouping rather than by source position, split span-free diagnostic payloads from source locations, and centralize constraint-proof checking.
- [x] 10.4 Lower and execute owned requirement bindings for both Copy and affine providers, preserving take-once ownership and cleanup.
- [x] 10.5 Carry constrained callable identity/evidence through closed static forwarding chains into MIR and every execution engine, and reject only opaque or runtime escapes.
- [x] 10.6 Regenerate accurate signatures and reference documentation for `Without`, constraints, provider selection, executable singleton catch, and the Logger/Clock plus set-to-set examples.
- [x] 10.7 Add compiler package subpath exports for every new public actor and replace wording assertions or thrown test invariants with structured diagnostics and `unreachable`.
- [x] 10.8 Run focused regressions, `pnpm typecheck`, `pnpm exec biome check .`, `pnpm test`, `pnpm check`, `pnpm release:candidate`, and strict OpenSpec validation before updating the pull request.
