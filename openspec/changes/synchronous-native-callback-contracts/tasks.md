## 1. Source contracts and identity

- [x] 1.1 Publish the prescriptive callback contract and pin the exact consumer/ABI/prior-art authority records; verify strict OpenSpec validation and recorded source digests.
- [x] 1.2 Add callback invocation sets, foreign pointer behavioral clauses and explicit nonnull identity; verify canonicalization, parser/formatter round trips and invalid declaration diagnostics with shared analysis snapshots.
- [x] 1.3 Admit exact declared export contracts and borrowed callback parameters, rejecting stronger expected contracts, unsupported escape/thread/unwind and enclosing-loan access conflicts; verify semantic code/span assertions.
- [x] 1.4 Carry complete contracts through semantic surfaces, classified ABI, manifests, headers and caches; verify equivalent and mismatched supplied interfaces and valid C headers.

## 2. Native invocation and ownership

- [x] 2.1 Implement unsafe foreign pointer application through checked expression facts, HIR/MIR and native lowering; verify true runtime address invocation with exact scalar/pointer signatures and native-only admission.
- [x] 2.2 Reuse complete-call source loans for borrowed indirect arguments; verify nested independent access succeeds and overlapping exclusive access diagnoses without inferring raw-pointer provenance.
- [x] 2.3 Generalize the existing fatal guard for indirect calls and callback entry; verify optimized IR/object unwind structure and distinguish termination before an outer C++ catch.

## 3. Consumer migration and independent evidence

- [x] 3.1 Migrate qsort and every affected caller, fixture, diagnostic/interface artifact and reference page; verify no obsolete callback contract representation remains.
- [x] 3.2 Extend the independent native fixture lane for exact C addresses, same-thread/dynamic-extent and nested invocation; run Darwin ARM64 and GNU x86-64/ARM64 with pinned supplies and fail missing lanes.
- [x] 3.3 Verify ordinary/typed-failure source-wrapper cleanup in the shared acceptance corpus without promising fatal-trap cleanup; measure focused test runtime against the verified base.

## 4. Review and delivery

- [x] 4.1 Run pnpm typecheck, pnpm format:check, pnpm lint, pnpm test, pnpm check and pnpm release:candidate in the required order; record exact outcomes and resolve change-related failures.
- [ ] 4.2 Obtain independent code review and a separate mandatory test-economics approval on the final committed issue diff; verify findings and record timing deltas.
- [ ] 4.3 Push the approved issue branch, create and confirm a draft PR targeting main, and update JUL-142 with the exact PR-head review baseline and verification evidence; read back draft and In Review states.
