## 1. Canonical Test Actor

- [ ] 1.1 Add canonical ordinary `silk.test` source exposing opaque Function and StackPath, public StackFrame view fields, checked pathLength/pathFrame inspection, Outcome, allocation-free `isFailed`, inventory, ID, and run wrappers; verify exact canonical function/source bytes and half-open offsets, None for out-of-range lookup, copied handles, navigation reaches source, and a renamed equivalent wrapper behaves identically, plus compile-fail coverage for wrapper construction, inspection, mutation, calling/conversion, and erased failure access.
- [ ] 1.2 Update the standard-library manifest and regenerate the shipped source table, then verify generated embedding, package contents, and public module resolution include canonical `silk.test` and `silk.test_runner` deterministically with a stable default runner identity.
- [ ] 1.3 Add source documentation for every public and user-observable initial testing behavior and limitation, including opacity, checked paths, Outcome ownership, assertions, reporting, filtering, the runner root, statuses, presentation, and traps; verify generated documentation promises no erased value, callable conversion, mismatch data, filesystem path, skip, generic equality, or compiled mode.

## 2. Assertions and Reporting

- [ ] 2.1 Implement silent ordinary-source `Test.assert(bool)` and verify true returns unit, false yields only `Test.AssertionError`, internal recovery passes, and no output, event, requirement, allocation, or retained value is introduced.
- [ ] 2.2 Implement ordinary-source `Test.equalBytes(&[u8], &[u8])` and verify equal, length-mismatch, first-element-mismatch, and later-mismatch cases short-circuit through Test.assert without preserving compared bytes or an index.
- [ ] 2.3 Add public PassedCase, FailedCase, and Event payload fields, public constructible empty ReportError, the mutable Reporter service, `Test.event`, and `Test.report`; verify an external custom reporter constructs ReportError and reads every event field, shared state updates once, omitted reporting needs no provider, ReportError cannot rewrite the completed outcome, and the Outcome-to-Event-to-Reporter path reclaims the owned StackPath exactly once on report success and failure.

## 3. Standard Selection and Runner

- [ ] 3.1 Implement bytewise ASCII-only folding and substring matching in ordinary source and verify an explicit zero-filter list selects a complete nonempty mixed inventory, mixed ASCII case, exact non-ASCII, invalid UTF-8 bytes, the empty substring, literal `*`, `.`, `[`, `!`, and `tag:` bytes, multiple-filter OR including one miss plus one match, overlap de-duplication, aggregate no-match, and canonical relative order.
- [ ] 3.2 Implement the standard runner core over the borrowed inventory using `Test.isFailed`; verify every selected handle runs once in canonical order, zero filters run a complete nonempty inventory, empty inventory plus zero filters emits `0 passed; 0 failed` and returns 0, per-case StandardReporter state is fresh, suite aggregation returns 0 for pass and 1 for any selected failure, a mid-suite ReportError stops later cases, and infrastructure status 2 overrides accumulated case status.
- [ ] 3.3 Implement canonical `silk.test_runner` with ordinary `pub fn main() -> i32` plus the closed OS-host edge with ordinary OsHostInput, Allocator, and output provider construction; verify main delegates once, runner-root selection does not contaminate inventory, argument zero is skipped, indices one onward are copied unchanged in exact order under lexical provision, partial acquisition rolls back, all filters/format/output state cleans on every exit, no-match/input/allocation/output/ReportError paths return 2, and fatal traps remain outside runner outcomes.
- [ ] 3.4 Implement exact `PASS <id>`, `FAIL <id>`, canonical logical frame, `0 matched`, and aggregate summary output through ordinary output actors; keep filesystem resolution out of the testing primitive, close formatting/allocation/output failures into sole ReportError, and add a golden path containing the four exact omitted canonical identities plus a same-named user `runSuite` helper to verify only those identities disappear, every other frame remains in outer-to-inner order, and the Event retains and reclaims the complete unchanged StackPath exactly once.

## 4. Privilege and Verification

- [ ] 4.1 Inspect compiler artifacts with Test, Reporter, AssertionError, StandardRunner, and equalBytes renamed fixtures and verify no phase recognizes any public actor by spelling or selects Reporter service identity.
- [ ] 4.2 Run focused standard-source resolution, assertion, filter, reporter, runner, presentation, allocation, and cleanup tests, then `pnpm typecheck`, `pnpm format:check`, `pnpm lint`, `pnpm test`, `pnpm check`, and `pnpm release:candidate`; record every exact result and identify any pre-existing failure before handing off command integration.
