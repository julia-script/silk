# Tasks

## 1. Complete checked-unit queries

- [x] 1.1 Define stable body and checked-unit identities plus position-independent answer projections, verified by focused identity and source-movement fixtures.
- [x] 1.2 Route complete checked-unit construction, including hidden bodies and diagnostics, through the shared semantic query runtime, verified by focused body reuse and hidden-body invalidation tests.
- [x] 1.3 Present admitted checked units against the current syntax revision and delete the body-specific validity scanner and reuse authority, verified by current-span diagnostic and forced-fresh tests.

## 2. Evaluation and residual queries

- [x] 2.1 Define stable static-application identities and move compile-time evaluation into its own shared query family, verified by focused cross-revision evaluation hit and invalidation tests.
- [x] 2.2 Move residual-program construction into a distinct shared query family that records only its actual dependencies, verified by a fixture where evaluation reuses while residual construction invalidates.
- [x] 2.3 Record deterministic evaluation work cost and normalized policy inputs so a valid hit charges exactly once and validation remains free, verified by focused budget hit, miss, and policy-change tests.

## 3. Ownership queries

- [x] 3.1 Route ownership derivation over complete checked units through the shared validator and current presenter, verified by focused ownership reuse and ownership-relevant invalidation tests.
- [x] 3.2 Delete independent ownership reuse maps and obsolete project snapshot fields, then update every caller, verified by compiler typechecking and focused project-analysis tests.

## 4. Coherence and documentation

- [x] 4.1 Preserve abort, cycle, parser-recovery, and nested forced-fresh semantics across the new query families, verified by focused semantic-query lifecycle fixtures.
- [x] 4.2 Document the unified body/evaluation/ownership revision model and complete checked-unit boundary, verified by strict OpenSpec validation and documentation review.
