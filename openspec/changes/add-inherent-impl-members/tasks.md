## 1. Syntax and formatting

- [x] 1.1 Make `for` optional in `parseImplDeclaration`: parse `impl [<Binders>] Type` then branch on `for` versus `{`; keep one `ImplDeclaration` node; add `pub` and `static` to the impl-body member-start and recovery tables. Verify parser fixtures cover `impl Counter { }`, `impl<T> Option<T> { pub fn ... }`, a mixed file with both forms, an unclosed member body, and a malformed binder list, each with lossless reproduction and local recovery.
- [x] 1.2 Render inherent impls in `SyntaxFormatter`: branch on the `for` token so members are sliced after one type node, then reuse the impl and function layouts. Verify an idempotent formatting test with a generic head, two members, and attached doc comments, and that conformance formatting is unchanged.
- [x] 1.0 Fix `collectTypeParameters` so a member with no `<...>` list still sees the enclosing binders by name (environment only; the caller decides which binders join the member's list). Verify the conformance suite stays green.
- [x] 1.3 Add the inherent form to the syntax inspector and DocBlock attachment tables. Verify the inspector fixture renders it and a `///` block above a member attaches to that member.

## 2. Declaration facts and index

- [x] 2.0 Add `InherentImplFact` to `ModuleHeaders`, resolve each head's owner in `DeclarationCompletion` with `resolveDeclaredType`, give `closeConformanceSelf` an explicit `self` parameter, and close `Self` on the head's members. Verify `fn zero() -> Self`, `fn map(self: Self, ...)`, and a member whose bound mentions `Self` complete to `Option<T>` in their declared types, bounds, failure rows, and requirement rows.
- [x] 2.1 Classify each `ImplDeclaration` in `DeclarationCollection` as inherent or conformance by the `for` token; route inherent members through the inline-operation elaboration path with canonical name `Owner.member`, bare `name.spelling`, binders `impl ++ fn`, and an `associatedMember` back-reference carrying head ordinal, owner, and receiver; key `memberIndex` by canonical name for canonical members. Verify a collection test records `map` with binders `[T, U]`, parameter zero `Option<T>`, and receiver set, `unwrapOr` (no local list) with binders `[T]`, `zero` with no receiver, a `self: once Effect<A ! E ? R>` member of a zero-data `Effect` owner classified as an associated function, an `unsafe fn` member, and a private member resolving as `Owner.member` inside its module; verify `byCanonical` finds `Option.map` while `member(module, 'map')` finds only a root `map`.
- [x] 2.2 Validate inherent heads syntactically at collection (specialized, repeated, or short argument list; bounded binders) with a new diagnostic at the head and no published member; validate foreign owner and alias owner (leading identifier looked up before erasure) in `DeclarationCompletion` beside the provider-locality check, unpublishing the head's members. Verify one test per rejection plus acceptance of `impl Widget`, `impl<T> Option<T>`, `impl<A, B> Pair<A, B>`.
- [x] 2.3 Reject a mapped `name: path` operation and a `fn drop` hook member inside an inherent body at collection with member-scoped diagnostics. Verify the remaining members still publish and no `drop@impl#N` hook is minted.
- [x] 2.6 Exclude facts with `associatedMember` from the module-level projections `DeclarationFacts.lookup`, `Analysis.declarationByName` / `memberByName`, `Completion.namespaceCandidates`, LSP document symbols (nest under the impl instead), and the docgen module model, while leaving elaboration's body walk and the parameter-identity walks untouched. Verify `import silk.option as O` then `O.` does not list `map`, the document-symbol fixture nests members under their impl, and hover on a member's own parameter still works.
- [x] 2.4 Detect duplicate member names across the impl blocks of one owner at collection, giving both facts a `Duplicate` state with the shared would-be identity, and reject at completion a member whose name equals a field, variant, enum member, generated operation, or contract operation of the resolved owner. Verify tests for a duplicate across two blocks (both diagnosed, both related, `byCanonical` finds neither), disjoint members across two blocks, a field collision, and a service-operation collision.
- [x] 2.5 Verify a service owner and an interface owner produce identical fact shapes and diagnostics for the same inherent member, and that a root `fn transform(self: Counter)` creates no member.

## 3. Qualified resolution

- [x] 3.1 Add `NameResolution.lookupAssociated` and consult it in `lookupQualified` before `scopedModule`, following selected-import aliases and transparent type aliases to the canonical owner and applying visibility. Verify tests for a selected-import call, a differing file name, an alias qualifier, a private member from another module, and a declared member outranking a same-named root function in a basename-matching module.
- [x] 3.2 Consult `lookupAssociated` in `CallResolution.resolvedFunctionReference` (which today returns undefined for every non-namespace qualifier) and in the argument-expected-type branch, producing a `Resolved` reference. Verify `Option.map` as a bare function item resolves and a borrow argument to `Counter.value(&counter)` gets its expected type.
- [x] 3.2b Consult `lookupAssociated` in the `ExpressionAnalysis` qualified-call dispatch above the service branch (admitting `EnumDeclaration` and erasing an `AliasDeclaration` qualifier to its owner), and before `analyzeEnumMember` on the function-item path, producing a `Resolved` reference; fold the service-only early diagnostic and the interface-only declaring-module escape into one branch that falls through to the legacy projection only on `Missing`. Verify `Logger.inMemoryProvider()` declared in `logging.silk` and `HashKey.describe()` resolve identically, `Status.describe()` on an enum owner resolves while `Status.Ready` keeps its meaning, `Maybe.some(2)` through an alias resolves, that navigation classifies members as value declarations, and that the bound-operation path is unchanged.
- [x] 3.3 `Effect` binds as the ordinary imported `Effect` struct, so `Effect.member` reaches an inherent member through the same qualified path as any owner; the intrinsic-qualifier module lookup is unreachable for `Effect` and is deleted by the migration change. Verify existing Effect call tests pass unchanged.
- [x] 3.4 Reject `import module { member }` when `member` is an inherent member, with the member as a related candidate. Verify the diagnostic names the owner.

## 4. Generics and callables

- [x] 4.1 Bind explicit generic arguments on a bare qualifier as a prefix over owner-then-local binders. Verify `Option.map<i32, i64>(move value, widen)` and full inference from arguments.
- [x] 4.2 In the applied-qualifier dispatch, before the service short-circuit and the interface gate, detect an inherent member on the applied owner and rewrite the call to the bare-qualifier form with the owner arguments prepended to the explicit generic prefix. Verify `Option<i32>.map<i64>(...)`, `Widget<u8>.make()` on a service owner, rejection of an over-long local list, a receiver mismatch diagnostic when the qualifier and argument disagree, and that `Option<i32>.None` still constructs a variant.
- [x] 4.3 Verify function-item, section, and pipeline forms for members: `Option.map` satisfies an explicit-receiver callable contract, `Option.some(2) |> Option.map(addOne)` resolves to the same member as the direct call, and `Pair.make(2)` forms a unary section. Run the section tests on the evaluator and at least one compiled backend.

## 5. Module surface

- [x] 5.1 Encode inherent head facts in `ModuleSurface` beside conformances (members are already encoded as module members). Verify a surface round-trip of a generic receiver method preserves owner-then-local order and that `ModuleSummary` is untouched.
- [x] 5.2 Verify invalidation: changing a public member's result type recomputes importers, and editing a member body leaves the surface equal.

## 6. Tooling

- [x] 6.1 List associated items after `Owner.` in `Completion` with kind labels (variant, associated function, method) and visibility filtering. Verify the `Option.` and private-member scenarios in `EditorIntelligence.test.ts`.
- [x] 6.2 Present inherent members in LSP document structure and hover with the full contract including parameter zero. Verify the LSP acceptance fixture lists an impl member symbol and hovers it.
- [x] 6.3 Make definition, references, and rename treat a member as one identity across declaration, call, section, and applied-qualifier forms; check the `SemanticOccurrence` spelling splits against the applied form's `Type.encode` spelling. Verify a navigation test from a pipeline section, one from an applied-qualifier call, and a rename test that leaves a same-named root function untouched.
- [x] 6.4 Emit inherent members grouped under their owner in docgen module references and the documentation JSON, and make `Project.targetOf` resolve `[`member`]` links through the module's owners' associated members before the root lookup. Verify a generated reference for a module with `impl` lists the member as `Owner.member` and a docgen test asserts a member link resolves to the member.
- [x] 6.5 Verify TextMate and CodeMirror highlighting fixtures for `impl Type {` without `for` (expected: no grammar change; only the fixture assertion).

## 7. Acceptance

- [x] 7.1 Add a semantic fixture exercising the full matrix from the specs: both call forms, section, applied qualifier, alias qualifier, foreign owner, alias owner, specialized head, duplicate across blocks, field collision, service and interface owners, root-function non-attachment. Verify it passes on the evaluator.
- [x] 7.2 Regenerate the diagnostic index and verify the staleness checks pass.
- [x] 7.3 Run `node scripts/turbo.mjs run test` and verify the full gate passes.
