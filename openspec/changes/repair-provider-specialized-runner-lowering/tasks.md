## 1. Preserve the Exact Failure

- [x] 1.1 Reduce the success-plus-truncation TLS diagnostic to the smallest ordinary user-service
      program that still realizes two distinct caller/callback environments for one generic
      provided Effect operation, place it in an existing compiler test file, and verify one shared
      analysis snapshot reproduces the missing runner without LLVM, WebAssembly, native execution,
      or the TLS fixture's approximately 116-second default-suite cost.
- [x] 1.2 Introduce one discriminated internal generated-runner lowering outcome carrying the
      canonical generated runner, open base, owner instance, and first exact expression provenance
      or containing-statement fallback, and verify a producer-built negative passed through the
      pure post-pruning selection seam observes those fields without a second source-analysis pass.
- [x] 1.3 Make the generated-runner assembly fail closed on an unavailable reachable outcome rather
      than silently omitting it after fixed-point reachability, propagate any canonical Effect
      capture identity already resolved by layout into runner parameter lowering without changing
      field count, prove an unavailable unreachable runner remains pruned, and verify no backend
      receives a partial module while ordinary source-semantic failures retain their existing
      diagnostics and availability boundary.

## 2. Repair Second-Specialization Lowering

- [x] 2.1 Use the recorded first failure to recontextualize only a canonical runtime-equivalent
      callable environment, prefer exact semantic call selection and otherwise require one unique
      runtime-equivalent retained call shape, and require direct Effect runs to emit that complete
      discovered target rather than authored-only arguments. Preserve effect identity,
      substitution, stored realization, provider witness/access, result and requirement rows,
      ownership loans, and cleanup plan; verify both caller orders lower successfully with the same
      canonical emitted runner set.
- [x] 2.2 Require the minimized positive regression to publish every referenced runner exactly once
      and explicitly connect both callers' run targets, captures, providers, and canonical runner
      functions before passing strict MIR verification, while the existing deliberately malformed
      runner control still reports `InvalidEffectOperation`.
- [x] 2.3 Keep generated-runner discovery and pruning demand-driven and target-neutral, and verify
      focused structural inspection finds no global retention, retry-all fallback, forced
      suspension classification, source-name recognition, new intrinsic, backend path, or runtime
      ABI change.
